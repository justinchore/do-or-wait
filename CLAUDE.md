# Do or Wait — Project Brief for Claude

## ⏸️ Session handoff (last updated 2026-07-22) — pick up here first
**Nothing below this note is done yet — Justin stepped away mid-session (headed to Reyes) before either deploy step happened.** The most recent work was building **n8n workflow 26** (Nearby Prospect Research & Draft — Claude web research + 4-hook draft for the 📍 Nearby Prospects tab, forked from Outreach's workflow 13). Both the workflow file and the `index.html` app-side UI (research button + draft variant tabs in `openNPModal`) are written and syntax-validated, but:
- Workflow 26 has **not been imported into n8n** yet (needs the Anthropic credential picked + activation).
- `index.html`/`CLAUDE.md`/the new `n8n/26_*` files have **not been git-pushed** yet.
- No real test run has happened — the 4-variant draft has never actually been generated against a live business.

Full deploy checklist and exact git commands are in the **"2026-07-22 — 📍 Nearby Prospects gets a research/draft engine (n8n workflow 26)"** section near the end of this file. Start there. Also still outstanding from the prior session (2026-07-20/21): **n8n workflow 25** (Site Outreach Discovery, the CoreClaw scraper itself) is verified and test-confirmed working, but neither its live-app button has been clicked for a real full 9-category run, nor has anyone confirmed the workflow is toggled Active long-term in n8n — worth a quick check before assuming either 25 or 26 "just works."

---

## What this is
A web app for Justin Cho (Justin.Cho@cubework.com) at Cubework. Tabs (numbered in actual tab-bar order — verified against `index.html`'s `data-tab="..."` attributes 2026-07-21):
1. **Tasks** — DO/WAIT topic threads (digital sticky note system)
2. **Leads** — Sales pipeline for leasing prospects, same DO/WAIT thread model  
3. **📧 Outreach** — Apollo-sourced cold-outreach prospect list (separate from Leads). Spreadsheet-style table seeded from `outreach-seed.js`, overlaid live by the Firestore **`outreach`** collection (written by n8n workflow 12). Columns: Company, Contact, Email, Phone, Location, Industry, TEU, Tier, Score, Status. Industry + status + sort dropdowns (`setOutreachIndustry`/`setOutreachSort`/`setOutreachFilter`), defaults to "Has email". Click a row → detail modal (`openOutreachModal`) with company intel, a research brief, the draft email (Open-in-email / Copy), an editable **Status** (`saveOutreachStatus`), a **🔎 Research & draft** button (`researchProspect` → n8n workflow 13; **`res.ok`-checked 2026-06-25** — a 404/500 from the webhook now alerts with the status and resets the button instead of hanging silently on "Researching…"), a **✕ Invalid** flag (`markOutreachInvalid`) that hides bad-fit prospects from normal views (review/restore via the "Invalid" filter), and a **⬇ JSON export** button (`exportOutreachJson`, added 2026-06-25) that downloads the prospect's full record (seed merged with the live `outreach/{id}` doc) as `outreach-<id>-YYYY-MM-DD.json` via the shared `downloadJSONFile` helper. **Apollo subscription is now cancelled (see 2026-07-20 session) — this tab's live prospecting pipeline is effectively dead; don't start new Apollo-based campaigns.** See the 2026-06-24 section below for the full pipeline.
4. **🏢 Prop Outreach** — property-tied call-sheet backlogs (added 2026-07-09), separate from Outreach (which is unattached Apollo/industry prospecting). Reads Firestore **`property_outreach`** (`location` is a hard property id matching `list_locations`/`availMap`; no bundled seed, live-only like Roster/Rate Bands). First dataset: the Ontario Airport (`3950-airport-ontario`) 289-record Yardi call sheet, migrated from a stray `outreach` copy — see the 2026-07-09 migration session below. Now covers 4 properties total (Ontario Airport, 2720 Pellissier, 2680-2690 Pellissier, CA Banana/Fontana — see the 2026-07-20 sessions below for the 3rd/4th batches). `renderPropertyOutreach()` groups by property, then by **call wave** (fixed priority metadata baked into the sheet at build time, NOT computed live — Wave 1 = active pipeline is always shown first and tagged 🔥 hottest even though later waves, e.g. Wave 2's "5,000+ SF" leads, often sum to more total square footage; see `PO_WAVE_INFO`). Click a row → detail modal (`openPOModal`) with: editable **Contact** fields (`savePOContact` — name/phone/email, not in the Yardi export by design, Justin fills in once pulled from Yardi, with 📞 Call/💬 Text/✉️ Email quick-launch links once present), a **Cold outreach draft** section (see below), a **Status** dropdown (`savePOStatus`), one-tap **Log a call** outcome buttons (`logPOAttempt` → No answer/Voicemail/Callback requested/Interested/Not interested/Bad number/Do not call, appends to `attempts[]` and auto-sets status), the call-sheet **source_notes**, full **call history**, and a **🎯 Promote to lead** button (`promotePOToLead`) that creates a real lead. All writes go directly to Firestore from the browser (no n8n involved for these) — `property_outreach`/`property_outreach_context` fall under the existing full-access rule in `firestore.rules`, no rules change was needed.
    - **Cold outreach draft (added 2026-07-09).** Manual, per-lead, **never auto-triggered** — deliberately separate from the automatic 3-day-cold Follow-up Scanner on Leads. Two-part design: a **per-property "current talking points" panel** (`savePOContextField`, Firestore `property_outreach_context/{locationId}` = `{available_space, discount, other}`) shown once above each property's wave groups in the tab — set it once (e.g. "12,000 SF now open", "10% off 6mo") and it pre-fills every lead's draft at that property until changed — plus a **per-lead override**: the same 3 fields appear pre-filled in the lead's modal, editable inline just for that one email without touching the shared default. Hitting **✨ Generate draft** (`generatePODraft`) POSTs to `PO_DRAFT_WEBHOOK` (n8n workflow 22, `webhook/property-outreach-draft`), which drafts via Claude (no web research, unlike workflow 13 — most `company` values here are actually a person's name straight from Yardi's Tenant/Company column, so there's rarely a real business/domain to research) and writes `cold_draft:{subject,body,generated_at}` + `draft_context:{available_space,discount,other}` back onto the record. Same `res.ok` 404/500 error-surfacing pattern as `researchProspect`/`generateFollowup`. Rendered with Open-in-email (only once an email is on file) / Copy / Dismiss (`copyPODraftMsg`/`dismissPODraft`). **Workflow 22 status: still not confirmed imported/active in n8n as of 2026-07-20 — verify with Justin before assuming this button works end to end.**
    - **Collapsible per-property groups (added 2026-07-09, when Pellissier joined Ontario Airport as a 2nd property).** Each property is now its own collapsible card (`poExpandedProps` Set of EXPANDED location ids, `togglePOProp(loc)` toggles + re-renders — same Set-of-ids pattern as Avail's `expandedAvailProps`/Waitlist's `wlCollapsed`). **Defaults to collapsed** (empty Set = nothing expanded) — first shipped defaulting to expanded, but with real data volume (289 Ontario + 75 Pellissier leads) that rendered as a wall of tables, so it was flipped same day to match Avail's own "none open by default" convention. Collapsed state shows a compact one-line summary (lead count + a small wave-count chip per wave present, e.g. `W1 · 22`) so priority still reads at a glance without opening it.
    - **Auto-archive on repeat decline (added 2026-07-09).** `logPOAttempt` counts `not_interested` outcomes in `attempts[]`; the **2nd** "❌ Not interested" log auto-sets `archived:true` (the 1st just records the outcome normally — Wave 4 reactivations are allowed one more no before being put away). Archived leads are soft-hidden (not deleted): `poRows()` now returns everything, and `renderPropertyOutreach()` shows non-archived by default, or **archived-only** when the status filter is set to the new **"Archived"** option. The modal shows a 🗄 Archived banner + **↩ Restore** button when viewing one, and a manual **🗄 Archive** button (`setPOArchived`) is also available any time a lead needs to be put away sooner than two declines.
5. **📍 Nearby Prospects** — per-site discovery of NEW nearby businesses to cold-outreach (added 2026-07-20), distinct from both 📧 Outreach (dead Apollo-based industry prospecting — the Apollo subscription is cancelled, do not use or reference it going forward) and 🏢 Prop Outreach (Yardi call-sheet backlogs of EXISTING tenant leads at a property, not new-business discovery). Uses **CoreClaw** (coreclaw.com), a pay-per-successful-result web scraping platform ($1.20/1,000 successful results, confirmed 2026-07-21 — the earlier "$3/1,000 free tier" figure was a build-time guess), and specifically its **Google Maps Scraper Worker** (slug `01KPD6M5YQADCQKGVKPDZVYC63`) with **business leads enrichment** (controlled by `max_leads_per_place`, not a boolean toggle — confirmed 2026-07-21) on, which returns a named contact (name/title/email/mobile/LinkedIn) alongside each business's base record (name/address/phone/website/category/rating) — solving the "who do I actually email" gap a raw Places API lookup would leave open. Reads Firestore **`site_outreach/{place_id}`** (doc id = the real Google `place_id` — there is no `cid` field on this Worker's output, confirmed 2026-07-21; re-running discovery for a site naturally dedupes; no bundled seed, live-only like Roster/Rate Bands/Prop Outreach). `renderNearbyProspects()` groups by property, then by **search category** (one CoreClaw run per category — `importer`/`distribution company`/`wholesale distributor`/`3PL logistics company`/`freight forwarder`/`ecommerce fulfillment center`/`trucking company`/`manufacturer`/`moving and storage company` in v1, tunable), collapsible per property (`npExpandedProps`, defaults collapsed — same convention as `poExpandedProps`). A **🔎 Discover nearby companies** button (top toolbar, works on any known Avail location even before it has any records yet, plus an inline "Re-discover this property" button once a property card is expanded) calls `discoverNearby()` → `SITE_OUTREACH_DISCOVER_WEBHOOK` (n8n workflow 25) — a real scrape, so the button shows a "this can take a few minutes" note rather than looking stuck. Click a row → detail modal (`openNPModal`) with website/phone links, the discovered contact block (gracefully shows "No named contact found — try the main line" when enrichment came back empty; shows a `📱 mobile (direct — no email on file)` line when a contact has a phone but no email, since CoreClaw's LinkedIn-sourced contacts often lack a verified email), a **Status** dropdown (`saveNPStatus`), one-tap **Log a call** outcome buttons (`logNPAttempt` → No answer/Voicemail/Interested/Not interested/Bad number/Do not call, appends to `attempts[]`, auto-archives on a 2nd "Not interested" same as Prop Outreach), free-text **Notes** (`saveNPNotes`), full **call history**, an **Archive/Restore** toggle (`setNPArchived`), and a **🎯 Promote to lead** button (`promoteNPToLead`) that creates a real lead. All writes go directly to Firestore from the browser — `site_outreach` falls under the existing catch-all full-access rule in `firestore.rules`, no rules change was needed. **CoreClaw's Worker input/output field names were verified 2026-07-21 against a real account (see that session below) and workflow 25 was corrected accordingly; the workflow itself is still not confirmed imported/active in n8n** — verify with Justin before assuming the button works end to end.
    - **Research & draft (added 2026-07-22).** Same Claude web-research + 4-hook drafting engine Outreach uses (n8n workflow 13), forked as **n8n workflow 26** for site_outreach's own field shape. A **🔎 Research & draft** button in `openNPModal` (`researchNearbyProspect()` → `NP_RESEARCH_WEBHOOK`) sends the business's name/website/category to Claude, which researches it live on the web and writes back `research_brief` + 4 subject/opener variants (mismatch/forced-bet/freedom/commitment) + a confidence score — identical field names to `outreach/{id}`, so the modal reuses Outreach's exact variant-tab/confidence-chip UI (`setNPVariant`/`useNPVariant`/`copyNPDraft`, mirroring `setOutreachVariant`/`useOutreachVariant`/`copyOutreachDraft`). Fit tier is derived from `search_category` (a small 9-entry map, `GROUPS` in workflow 26) rather than wf13's broad Apollo-industry taxonomy, since discovery already pre-filters to warehouse-relevant categories. **Workflow 26 is new, not yet imported** — see the 2026-07-22 session below.
6. **📞 Playbook** — cold-call flow script + a searchable **Battle Cards** quick-reference at the top (`PLAYBOOK_CARDS` / `renderPlaybookCards`): Triple-A value, objection rebuttals, vs-traditional-lease, pricing quick-ref, what's-included, TCO story, sell-by-decision-maker, products, taglines — distilled from the Cubework Sales Playbook `_kb` (in the `Sales/` project folder). Collapsible; searching auto-expands matches.
7. **🏭 Avail** — Real-time availability dashboard for Cubework warehouse locations. Has an **⬇ Export JSON** button (toolbar) that opens a location-picker modal (`openAvailExport` / `doAvailExport`, overlay `#availexp-overlay`): choose which sites to include, then it **always syncs-first for client-fresh data** — it POSTs `AVAIL_WEBHOOK` (n8n workflow 4) per chosen location, waits for the Firestore write-back by polling each doc's `synced_at` against a pre-trigger baseline (deadline scales with location count, capped 5 min), then pulls a `getDocsFromServer` snapshot and downloads the selected docs as `do-or-wait-availability-YYYY-MM-DD.json` (array of `{_id, ...doc}`, same shape the listener consumes). Locations whose sync doesn't confirm in time are still exported (last-known) with a ⚠ note in the modal status.
8. **🗂️ Roster** — Staff contact directory (who to call for a location), added 2026-07-07. Read-only, grouped by state, searchable. Shows Location, a status badge when present (For Sale/Moved Out/UNIS variants), Local Staff (tour support), and National Facility Manager, plus a manual **⟳ Sync now** button (the source sheet changes rarely, so no cron). Reads `roster/current` (n8n workflow 19, manual-webhook only); no snapshot fallback. **Visible to the Avail-only restricted role (Lucy) too** — unlike Pricing/Rate Bands, this is just a contact directory, not sensitive negotiating data (see `lockToAvailReadOnly` and the 2026-07-07 session below). **Workflow 19 status: not confirmed imported/active in n8n — verify with Justin.**
9. **🔗 Links** — SharePoint/Google Sheet link directory (added 2026-07-20), so Justin doesn't have to keep hunting down the same links across different SharePoint folders. Backed by Firestore **`sharepoint_links/{id}`** = `{id, label, url, category, notes, createdAt, updatedAt}` — full CRUD (add/edit/delete), not read-only, same interaction shape as Notes. `renderSplinks()` — search box + category dropdown (built from whatever categories exist in the data) + grouped, alphabetized rows; each row opens the URL in a new tab, with optional notes and Edit/Delete buttons. Seeded once (`SPLINKS_SEED`, `seedSplinksIfEmpty`) from links already collected in this repo (the 8 per-location `sharepoint_location_links.txt` entries + the Renewals Google Sheet URL) the first time the collection loads empty — a client-side one-time seed, not an n8n workflow. **Not** opened up to the Avail-only restricted role (Lucy) — `lockToAvailReadOnly()` only allow-lists `avail`/`roster`, so this tab is hidden for her, same as Leads/Notes/Outreach/Pricing/Rate Bands. See the 2026-07-20 session below for the full design.
10. **💲 Pricing** — National rate card (read-only). Searchable table grouped by state; click a row to expand full pricing detail. Reads `pricing/current` (n8n-synced), falls back to `pricing-seed.js` snapshot. **Workflow 7's auth pattern was flagged 2026-07-07 as never actually verified live** (still had a `REPLACE_WITH_CREDENTIAL_ID` placeholder) — worth checking if this tab's data ever looks stale.
11. **📊 Rate Bands** — Internal Sales/BD negotiating tool (staff only), added 2026-07-07. Fully separate from Pricing — reads a different sheet with different, non-overlapping info (see that session below). Searchable, grouped by state, with a Cubework/UNIS/All scope filter and a manual **⟳ Sync now** button. Each building is a collapsible card: Current/Recommended/Floor–Ceiling price bands per size tier with a RAISE/HOLD/LOWER action pill, occupancy %, below-break-even $ flag, active promo banner, add-ons/ancillary pricing, and free-text notes. Reads `pricing_bd/current` (n8n workflow 18); no snapshot fallback (internal-only, no bundled seed). **Workflow 18 status: not confirmed imported/active in n8n — verify with Justin.**
12. **💡 Notes** — Ideas/issues notebook (two editable fields per note, filter by date created)
13. **🤖 Assistant** — in-app Claude chat (added 2026-07-10), backed by Anthropic's native **MCP connector** against the do-or-wait Cloud Run remote MCP server (`Sales/DoOrWait_MCP/remote-server.js`), NOT Claude Desktop/claude.ai conversation mode. Built specifically because conversation mode narrates every tool call in the transcript — that's a UI choice of that surface, not an API requirement, so owning the chat panel means only the final reply ever renders. One persisted thread (`assistant_threads/main`, live-synced like every other collection) with a plain `{role,text}` message list; `sendAssistantMessage`/`renderAssistant`/`newAssistantThread` in `index.html` POST to `ASSISTANT_CHAT_WEBHOOK` (n8n workflow 24, **imported and confirmed live 2026-07-10** — including two post-import fixes, hardcoded MCP URL + prompt caching, applied directly in the n8n UI) and render nothing but the reply text, plus an optional collapsed "🔧 used N tools" chip (`toggleAssistantToolsChip`) if Justin wants to peek at which tools ran — hidden by default. Tool scope is everything the connector exposes (Tasks/Leads/Notes/Outreach/Property Outreach/Availability/Pricing/Rate Bands/Renewals/Roster), no allow/deny list. See the 2026-07-10 (cont.) session below for the full design and deploy checklist.

**n8n import-status caveat (added 2026-07-21 doc audit):** the per-tab notes above flag every workflow whose "imported/active in n8n" status was never explicitly confirmed anywhere in this document's session log (18, 19, 22, 25) — CLAUDE.md can only track what was typed into it, not Justin's actual n8n instance, and a few of these went several sessions without a confirmation line ever getting added. If any of these are in fact live, update the corresponding tab note and the "Workflow import/active status" mentions below.

Deployed at: https://justinchore.github.io/do-or-wait/
Source: `C:\Users\jcho\Documents\Claude\Projects\do_or_wait\index.html` (single HTML file, push to GitHub to deploy)

---

## Tech stack
- **Frontend**: Single `index.html` — vanilla JS, no framework, no build step
- **Database**: Firebase Firestore (project: `do-or-wait`). **Rules locked down 2026-06-24** — read/write only for a signed-in user on the 3-email allowlist (see Auth section). Was previously open/no-auth.
- **App auth**: Firebase **email/password** sign-in, restricted to `ALLOWED_EMAILS` (jchoustin91@gmail.com, justin.cho@cubework.com, cubeworkautomation@gmail.com). Switched from Google sign-in (the cross-domain redirect fails on mobile). See Auth section.
- **Storage**: Firebase Storage (voice notes, file attachments)
- **Automation**: n8n at `https://ailinker.item.com` (self-hosted, behind nginx)
- **CORS proxy**: Cloudflare Worker at `https://plain-credit-5962.jchoustin91.workers.dev` (proxies app → n8n with correct CORS headers)
- **Microsoft OAuth2**: Azure app `n8n-cubework`, n8n credential named `Microsoft account 3` (oAuth2Api: `n8n-cubework`). Scope must include `offline_access`.

---

## Architecture decision — stay vanilla single-file (revisit at triggers)
**Decided 2026-06-10.** Keep `index.html` as one vanilla-JS file with **no framework and no build step**. The push-to-GitHub-Pages deploy simplicity and the ease of editing a single file (incl. by Claude) outweigh the developer-comfort wins a framework would bring. Do **not** migrate to React/Vite or introduce a bundler on a whim.

**When adding any new feature, keep this in mind and weigh whether we've hit a trigger to reconsider:**
- The file is getting hard to navigate / tooling strains on its size (watch line count — it's the real signal, not the framework itself). First move if so: split the JS into a few **native ES modules** (still no build), not a framework.
- Another developer joins the project.
- The `window.*`-exposure footgun (see the ES-module gotcha below) or the manual `esc()` string-templating start causing **repeat** bugs.

**If we ever do migrate**, the chosen path is **Preact + htm loaded from a CDN** — components + reactivity while preserving the zero-build, push-to-deploy workflow. Not full React-with-a-bundler.

**Deferred feature ideas (revisit at the Preact migration):**
- Side-by-side card comparison (open two cards at once). Cards are currently **accordion** (single-open) via `toggleCard` / `toggleLeadCard` — opening one collapses the rest. Multi-open + a comparison layout is cleaner to build with components than with the current string-templating.

---

## Firestore collections

### `topics/{id}`
Task threads. Fields: `title`, `archived`, `entries[]`, `createdAt`, `email_links[]` (see Linked email threads below).

### `leads/{id}`
Sales leads. Thread structure + lead-specific fields: `company`, `first_name`, `email`, `contact`, `phone`, `segment`, `stage`, `location`, `sqft`, `leaseLength`, `moveIn`, `unit`, `unit_sf`, `dock`, `rate`, `included_items`, `alt_option`, `is_importer`, `current_step`, `last_touch_date`, `next_due_date`, `seq_status`, `pending_email` (written by n8n when email ready to approve), `followup_draft` (`{subject, body, generated_at, source}` — written by the manual **✨ Generate follow-up** button, n8n workflow 17; `source` is `thread` or `notes` depending on what it was drafted from; see the 2026-07-06 session below), `email_links[]` (see Linked email threads below).

**Linked email threads — `email_links[]` (both topics AND leads).** Each item holds a list of labeled Outlook threads. Per entry: `{ id, label, subject, contact, url, conversationId, resolving }`. The app surface is `renderEmailLinks(item, coll)` (rendered on every lead card after details, and inside every task thread) + a `🔗 Email threads` block. **Why it's built this way:** Outlook has NO working search-by-URL (`deeplink/search` is unsupported — it opens a blank Outlook PWA) and NO desktop deep link. The ONLY supported "open this thread" link is a message's Graph **`webLink`**. So adding a thread is a resolve step: the inline add-form (`openAddEmailLink`/`submitAddEmailLink`, state `addingLink` Set, contact field is a pick-or-type `<datalist>` of `knownContacts()` — every contact linked anywhere + lead emails) collects **subject + contact**, then `resolveThreadLink(id,coll,linkId)` POSTs `{subject,contact}` to `RESOLVE_THREAD_WEBHOOK` (n8n workflow 11), which searches Graph and returns the newest hit's `webLink`; that's stored as `url` and the link opens the real thread on click. Link render states: `url` set = resolved/working (📧, opens OWA, plus a ↻ **Refresh** button — added 2026-07-02 — that re-runs `resolveThreadLink(id,coll,linkId)` for just that one link so a new reply/latest message can be pulled in with a single click, no dialogs; shown whenever `subject` is present) · `resolving` = lookup in flight (⏳) · neither = Graph found no match / webhook unreachable (⚠ + ↻ retry). Refreshing is intentionally per-link, not a "refresh all" sweep — each click is exactly one Graph search (the existing per-link cost), so there's no way to accidentally fan out Graph requests across every thread on every lead/task. **Manual-only "new mail" flag (added 2026-07-02, no polling).** Each ↻ click compares the newest message's `date` (already returned by workflow 11's "Pick newest hit") against a stored `link.lastMsgDate`; if it's strictly newer, `link.hasNew = true` and a 🆕 badge shows on the link — in both the collapsed-card chip (`emailLinkChips`, now takes `(item, coll)`) and the expanded row. Never flagged on a link's first-ever resolve (nothing to compare against). The badge clears via `markThreadSeen(id,coll,linkId)`, fired onclick when you actually open the thread. Deliberately **not** a background job — Justin's call was refresh-only, no scheduled n8n check, to avoid any automatic Graph cost. **Also auto-fires on card expand (added 2026-07-02).** `refreshCardEmailLinks(id,coll)` re-checks a card's already-linked, search-based threads (has `url`+`subject`) the moment you open it — wired into `toggleCard`/`toggleLeadCard` on the closed→open transition only (not on collapse, not on re-renders while already open). Still bounded to that one card's threads (usually 0-2), same `resolveThreadLink` call the ↻ button uses — no new webhook, no sweep across other leads/tasks. **Leads-tab auto-refresh (added 2026-07-02).** `refreshAllLeadEmailLinks()` loops every lead's linked, search-based threads and re-checks each via `resolveThreadLink`, fired from `switchTab('leads')` — so opening the Leads tab refreshes the whole list's 🆕 status without a manual click per card. A 60s cooldown (`_lastLeadsAutoRefresh`) stops repeat tab-clicks from re-firing the same batch. Deliberately client-triggered, not a scheduled n8n job — chosen over a cron poll or a real-time Graph subscription as the cheapest option to start with; those remain options if this proves too infrequent (Justin only sees fresh data when he actually opens the tab, not while the app sits idle in the background). Batch calls are staggered 250ms apart (not fired all at once) to avoid a burst of simultaneous Graph searches. **Extended to Tasks (added 2026-07-02, same day).** `refreshAllTaskEmailLinks()` is the identical pattern over `topics`, fired from `switchTab('tasks')` with its own 60s cooldown (`_lastTasksAutoRefresh`). Originally left Tasks-only-on-card-expand (not tab-open) because Tasks was the **default tab** — wiring a batch refresh there would've fired on every single page load, a bigger automatic footprint than intended. Once the default tab moved to Avail (2026-07-02 — Justin's actual usage is Avail and Leads most, Tasks only ~2x/day, so Avail now loads on open instead of Tasks; see the tab-bar `active` class + `tasks-view`/`avail-view` initial `display:none`), opening Tasks became a deliberate click like Leads, so the same tab-open batch was added for parity.

**Bug fixed 2026-07-02 — refresh could wipe a working link.** `resolveThreadLink` used to always write whatever the search returned, including a "no match"/error result, even over an already-resolved `url`. That was fine while refreshing was manual-only (you'd only retry an already-broken link), but the auto re-checks above made it visible: a transient miss (Graph's search index lagging a few seconds behind a message that just landed, or a throttled request from the batch) would blank out a perfectly good, working link back to the ⚠ "no match" state — until a later, uncontested manual ↻ click succeeded and looked like the fix. Symptom reported: intermittent "no match" on Wait-column leads (the ones being actively re-checked because a reply is expected) that a manual refresh always cleared. **Fix:** `resolveThreadLink` now captures `hadUrl` at the start and only writes a failure/no-match result when the link had no `url` yet (first-ever resolve or an explicit retry from the error state) — a refresh of an already-working link can only improve it (find a newer message) or no-op, never regress it.

**Whole-card "new mail" highlight (added 2026-07-02).** A small badge on the link wasn't visible enough, so any card with `hasNewMail(item)` true (any linked thread's `hasNew` flag set) gets a `has-new-mail` class → a light-yellow background + inset border (`--new-mail-bg`/`--new-mail-border` CSS vars), layered under the existing do/wait left-border color and shadow — applies to both `renderCard` (topics) and `renderLeadCard` (leads). Purely a render-time boolean check over data already in memory (same cost class as the existing open/archived/do/wait card classes); no new Graph calls or added processing — the actual cost in this feature was always the network round-trip to Graph, not the CSS. **Tab-bar badge (added 2026-07-02).** So a yellow card doesn't require actually being on that tab to notice: `render()` counts `topics` with `hasNewMail(t)` and writes a `.tab-badge` count onto the Tasks tab button (reusing the same badge pattern/CSS the Leads tab already used for pending-approval counts); `renderLeadsSection()` folds `hasNewMail(l)` into that existing Leads badge count alongside `pending_email`, counted once per lead so the number reflects cards-needing-a-look, not reasons. Both recompute on every render, so they update live off `resolveThreadLink`/`markThreadSeen` the same as the card highlight — no extra wiring. `editEmailLink` re-resolves when subject/contact change (or even unchanged, since it force-clears `url` first) via 3 prompts — the ↻ button is the same underlying call without the dialogs; legacy entries with a hand-pasted `url` and no `subject` keep editing the url directly (no ↻, since there's no subject/contact to search on). **Isolated from the Follow-up Scanner** — its own webhook workflow, reusing the same Graph cred. Caveats: opens in Outlook on the web / PWA (not classic desktop — Microsoft limitation), and a `webLink` can rot if that message is later moved between folders (re-resolve via ✏️ Edit or ↻). All inline handlers exposed on `window.*`.

**New-Outlook deeplink fix (2026-06-25).** Email-thread links broke en masse with *"This message might have been moved or deleted"* for **valid, current** messages (regardless of signed-in account). Root cause is Microsoft-side (see office-js #1095): the new Outlook (`outlook.cloud.microsoft`) deeplink reader fails to open `webLink`s. Graph returns the webLink as **either** the classic `outlook.office365.com/owa/?ItemID=…&viewmodel=ReadMessageItem` form **or** the new `outlook.cloud.microsoft/mail/deeplink/read/…` form — and **both fail**, because the `/owa/` form *redirects* into the same broken new-Outlook reader. The **only** form that opens (verified) is:
`https://outlook.office365.com/mail/deeplink/read/<pathId>?ItemID=<ItemID>&exvsurl=1`, where **`<pathId>` = the ItemID with base64 `/` and `+` swapped to `-`** (the path segment and ItemID query are the same id in two encodings). Fix: `fixThreadUrl(u)` in `index.html` extracts the `ItemID` from whatever Graph gave and **rebuilds that working URL at render time** (applied to both `renderEmailLinks` rows and `emailLinkChips` hrefs) — repairs every already-stored link (owa OR cloud form) with no re-resolve. Workflow 11's "Pick newest hit" node applies the **same** `fixThreadUrl` before storing, so new links save in the working form. NOT a folder-move/stale-ID issue and NOT a simple host swap (the earlier `cloud→office365` host-only rewrite did nothing, since the stored link was the `/owa/` form, not `cloud`). Re-resolving alone doesn't help — the URL rebuild is the fix.

**Follow-up fields** (written by the n8n Follow-up Scanner — workflow 6 — from Outlook/Graph): `last_contact_date` (YYYY-MM-DD of the most recent email to/from the lead), `last_contact_dir` (`out`/`in` — direction of that newest email: `in` = received from the lead, `out` = we sent it), `days_since_contact` (int), `followup_contacted` (bool — false if no email history was found and the date fell back to lead creation), `followup_due` (bool — true at ≥3 **business** days **and** `last_contact_dir==='out'`), `followup_checked_at` (ISO of last scan), `followup_snooze_until` (YYYY-MM-DD — set by the app's "Snooze" button, suppresses the lead until that date).

**Update categories (decided 2026-06-10).** Each lead thread entry has a `kind`: `note` (internal — "talked to Kevin", "looked into locations"; **NOT** counted as contact), or a contact brief `email`/`call`/`text`/`met`/`inbound` (counts). The ＋ Update modal shows a category picker for leads (`#m-kind-row`, `pickKind`/`syncKindUI`, state `mSelKind`, default `note`); quick-log buttons set the kind directly. `contactInfo` only counts brief entries (kind ∈ email/call/text/met/inbound) **plus legacy entries with no `kind`** (grandfathered as contact so existing leads' clocks don't recalculate); `kind==='note'` is skipped. So the follow-up clock = days since your last real lead contact, ignoring internal notes. (`✉️ Email` briefs use your typed text for now; reading the actual email is a later upgrade, gated on the scanner being live.)

**Follow-ups are OUTBOUND-COLD only (decided 2026-06-10).** A lead is a follow-up only when its *most-recent* contact was **outbound** (you reached out) and it's been ≥3 days. If the latest contact was **inbound** (an email received from them, or a 📥 Inbound log), the ball's in your court — that's a separate "needs-reply" job (not yet built; those leads sit in the **Do** column via the inbound log's `type:'do'`). `contactInfo(lead)` computes the most-recent contact across `last_contact_date`(+`last_contact_dir`) / `last_touch_date`(out) / thread entries — counting only contact briefs (`kind==='note'` skipped; legacy no-`kind` entries grandfathered as contact), each carrying `dir` (`out` for 📞/💬/✉️, `in` for 📥). It returns `{days, bizDays, date, contacted, dir}`; `isFollowupDue` requires `dir==='out'` **and** `bizDays >= FOLLOWUP_DAYS` (business days — see weekends note below). The app **does not depend on the scanner having run** — it works off your updates immediately; the scanner just folds in live Outlook email (with direction) once running.

**Waitlist fields** (a lead can be put on the waitlist to watch for matching space): `wl_on` (bool), `wl_locations` (array of propIds to watch), `wl_type` (`any`/`WH`/`OFFICE`/`DOCK`/`TRAILER`), `wl_sf_min`, `wl_sf_max`. The "⏳ Waitlist" lead filter shows only `wl_on` leads. The modal sets `wl_on`/type/size; **locations are chosen on the lead card** via `renderWaitlistBlock` — a collapsible block (`wlCollapsed` Set, `wlToggleCollapse`) with a "＋ Add location…" dropdown of current `availMap` locations and removable chips (`wlAddLocation`/`wlRemoveLocation`, which persist + re-render). Matches compute inline every render via `wlMatches(lead)` (available-or-hold units in the chosen locations matching type + size) — no button, the list updates live as you add/remove locations. All client-side against synced availability; no n8n.

**Space Match field** — `dm_snooze_until` (YYYY-MM-DD, set by the "Snooze 14 days" button on a Space Match card, suppresses that lead from the Space Matches banner/list until that date). See the **Space Matches** feature in the 2026-07-03 → 2026-07-06 session below for the full matching logic (this is a broader, proactive version of the Waitlist block above — it also watches archived and Lost leads, not just `wl_on` ones).

### `notes/{id}`
Ideas/issues notebook (💡 Notes tab). Fields: `trigger` (text — "What triggered this?", added 2026-07-02), `why` (text — "Why does this issue matter?"), `fix` (text — "What's the fix?"), `createdAt`, `updatedAt`, `_deleted` (soft-delete flag). `trigger` renders first (before `why`/`fix`) in both view and edit mode; older notes with no `trigger` just show "—" and edit in as blank — no migration needed. Notes have a **view mode** (read-only, shows Created + Edited dates, with Edit/Delete buttons) and an **edit mode** (textareas + Save/Cancel). `editingNotes` Set tracks which note ids are open for editing; `editNote`/`cancelNote`/`saveIdeaNote` toggle it. New notes open straight into edit mode (focus starts on `trigger`). The tab has a Newest/Oldest sort and a "Since" date filter on `createdAt`.

### `availability/{propId}`
Written by n8n after syncing each property's SharePoint file. Fields: `property`, `address`, `yardi_url`, `sharepoint_url`, `sheet_last_modified`, `synced_at`, `wh{}`, `office{}`, `dock{}`, `parking{}`, `units[]`, `pa{name,phone,email}`, `ownership` (`own`/`lease`/`''`), `lease_expiration` (YYYY-MM-DD).

**App-edited fields survive sync**: `pa`, `ownership`, `lease_expiration`, and (added 2026-07-09) `future_plan_url`/`future_plan_storage_path`/`future_plan_note`/`future_plan_updated_at` are written directly from the app via Firestore REST PATCH and are NOT in the sync's `updateMask`, so re-syncs don't clobber them. `ownership`/`lease_expiration` are set via the Ownership modal (`openEditOwnership`/`saveOwnership`); the collapsed location card shows an ownership badge (🏢 Owned / 📄 Leased · exp date, red if expired) that opens the modal on click. See the **Future Plan** feature (2026-07-09 session below) for the new fields.

Each entry in `units[]` (parsed from the BOT tab): `unit`, `type` (WH/OFFICE/DOCK/TRAILER), `sf`, `status`, `tenant`, `owner`, `phone`, `email`, `poc`, `notes`, `available` (bool), `hold` (bool). owner/phone/email/poc/notes come from BOT-tab cols 6-10 and are null when blank.

### `pricing/current`
One document — the national rate card. Written by the **n8n Pricing Sync (workflow 7)** from the SharePoint master price sheet (`New Master List_ Price.xlsx`, tab `New Price Sheet 2026`, on the **Sales_US** site). Fields: `rows` (array of per-location maps), `updatedAt`, `source`, `count`. Each row map: `state, city, address, sub2k, r2_5k, r5k, r5k_12mo, office_mo, office, dock, desk, virtual, truck, small_veh, wifi, annual, deposit, dp_office, dp_wh, dp_xdock, studio, conf, event, amenities, special` (blank fields omitted; per-SF tiers numeric, ranges/"n/a" as strings). The **💲 Pricing tab** reads this; if absent it falls back to the bundled snapshot in `pricing-seed.js`. (Decided to use n8n because the sheet changes often — same rationale as availability.) The sheet's **Special Pricing** column (negotiation notes, e.g. "$1.45 for 6mo, 1st month free for 12mo") and **Amenities** are captured as `special`/`amenities` and render full-width (line breaks preserved) in a row's expanded detail; rows with `special` show a 💬 badge. NOT captured: Excel cell *comments* (price-change history / stray notes) — they aren't part of the cell values, so neither the snapshot parse nor the Graph sync reads them.

### `pricing_bd/current`
One document — the internal Sales/BD rate-bands negotiating tool. Written by the **n8n Pricing BD Sync (workflow 18)** from a separate SharePoint file (`2026_cubework_pricing_by_state__Sales__BD_Copy.xlsx`, "Pricing Hub Files BD  Sales Team" folder on **Sales_US** — a different document library than `pricing/current`'s source sheet). Added 2026-07-07, deliberately kept **fully separate** from `pricing/current`/workflow 7 — the two sheets carry different, non-overlapping info (this one has no amenities/special-pricing notes/annual pricing; the public sheet has no bands/occupancy/break-even/promos), so neither replaces the other and neither sync touches the other's doc. Fields: `rows` (array of per-building maps), `updatedAt`, `source`, `count`. Each row map: `_id, state, unis` (bool — true for entries from the UNIS tab), `index` (building's card number within its sheet tab), `name, address, tag` (null or `Inactive`/`Pending`/`UNIS Ref`), `building_sf, lease` (date string or "Own"), `occupancy_pct`, `bands` (array of `{sf_range, tier, current, recommended, floor_ceiling, action}` — one per size tier, RAISE/HOLD/LOWER), `addons` (map, keys like `office`/`dock`/`cubicle_desk`/`virtual_office`/`truck_container_pkg`/`sm_vehicle_pkg`/`whse_wifi`/`deposit`/`day_office`/`day_whse`/`day_dock`/`event_space` — plus a slugified fallback key for any label not in that list, so an unrecognized add-on still gets captured), `conf_room`, `notes` (array of free-text strings), `below_break_even`, `promo`. The **📊 Rate Bands tab** reads this; no snapshot fallback (internal-only, no bundled seed — if the doc is empty the tab just says "Not synced yet").

**Sheet layout (tab-per-state, verified 2026-07-07 against a real `usedRange()` pull of the AZ tab — fixture kept at `n8n/pricing_bd_raw_grid.json`):** one worksheet per state (named by 2-letter code, e.g. "AZ") plus a "UNIS" tab and an "Overview & Instructions" cover tab (skipped by the sync). Within a tab, each building is a numbered card: a `[indexNum, "Name — Address [optional tag]"]` row starts the card, then `Building SF / Lease / Occupancy`, then optional single-cell free-text rows (BELOW BREAK-EVEN / PROMO / other notes — order varies, so the parser classifies by content, not position), then a `Band/Tier/Current/Recommended/Floor–Ceiling/Action` header followed by 1-6 tier rows (variable length — stops naturally at the next marker, not hardcoded to 6), then an `ADD-ONS / ANCILLARY PRICING` marker followed by label/value pairs (3 per row across the row's 6 remaining cells), then Conf Room policy and approval notes, then a blank spacer before the next numbered card. The UNIS tab additionally has bare `— CA —` style state-divider rows between groups (its cards don't otherwise carry a state). Parser: **`n8n/pricing_bd_sync.nodes.js`** (paste-ready copy of the workflow's Code nodes) — built as a tolerant label/content scan (not fixed cell coordinates) so it survives a card having fewer tiers, missing add-on rows, or an unexpected extra note line, the same design philosophy as the availability/renewals parsers.

### `roster/current`
One document — the staff contact directory backing the 🗂️ Roster tab. Written by the **n8n Roster Sync (workflow 19)**, manual-webhook only (no cron — the source sheet changes rarely), from `Cubework_Locations_Master_Roster_2026.xlsx` (Sales_US site, "Pricing Hub Files BD  Sales Team" folder — same document library as the Rate Bands source, different file). Fields: `rows` (array of per-location maps), `updatedAt`, `source`, `count`. Each row map: `_id, code` (3-digit internal ID, some cells store it as a number in the sheet — normalized to a string), `state` (full name, e.g. "Arizona"), `location` (short name, e.g. "AZ Tolleson"), `status` (blank = active; else "For Sale"/"Moved Out"/"UNIS building"/"UNIS — lead tracking only"/"Occupied by UNIS"/"All UNIS"/"No CW units built yet", free text), `sales` (team code, e.g. "A" or "A / H" — captured but not surfaced in the tab per Justin's ask, kept in case it's wanted later), `local_staff`, `facility_manager` (both "—" when unassigned). Unlike Rate Bands, this sheet is a **plain flat table** (title row, subtitle row, header row, then one row per location) — no card-dashboard parsing needed; the parser finds the header by matching `Code`/`Location` column content (not a fixed row index) so an extra title line later doesn't break it. Verified 2026-07-07 against a real `usedRange()` pull of the "Locations" tab (62 rows; the sheet's own subtitle still says "61 locations" — stale count in the source, not a parsing bug) — fixture kept at `n8n/roster_raw_grid.json`. The workbook also has an "Overview" tab (Sales-code A–F and status-key legend) that is deliberately **not** read — Justin's call was PA/manager-per-location only. The 🗂️ Roster tab reads this live; no snapshot fallback.

**Opened up to the Avail-only restricted role (Lucy), unlike Pricing/Rate Bands** — see the 2026-07-07 session below for the `firestore.rules` / `lockToAvailReadOnly` changes this required.

### `property_outreach/{id}` (added 2026-07-09)
Property-tied call-sheet backlogs backing the 🏢 Prop Outreach tab — deliberately separate from `outreach/{id}` (unattached Apollo/industry prospecting). `location` is a **hard** property id matching `list_locations`/`availMap` keys (not a loose tag). No bundled seed — live Firestore only, written directly from the browser (setDoc/onSnapshot) and, for bulk imports, via n8n workflow 20 (the generic Firestore writer). Falls under `firestore.rules`' existing catch-all full-access rule — no rules change was needed for this collection. Fields: `company` (from Yardi's Tenant/Company column — **often a person's name, not a business name**, see the wave/draft prompts' handling of this), `contact`/`phone`/`email` (**not** in the Yardi export for Ontario; blank until Justin looks them up and types them in via the modal — `savePOContact`; Pellissier's export DID already have these filled in, see below), `location`, `source` (e.g. `yardi`), `wave` (int, call-priority tier — fixed metadata baked in when the sheet was built, see `PO_WAVE_INFO`; NOT computed live), `sqft`/`sqft_est` (bool — SF estimated from deal notes text when Yardi's own field was blank), `source_notes` (deal type/stage/industry/notes/agent/call-outcome context assembled from the Yardi export), `status` (not_contacted/attempted/reached/interested/not_interested/bad_number/do_not_call/promoted), `attempts[]` (`{id,at,outcome,note}`, appended by `logPOAttempt`), `last_contacted_at`, `promoted_lead_id`, `archived` (soft-hide; set manually via `setPOArchived` or automatically by `logPOAttempt` on a 2nd "Not interested" — see 2026-07-09 cont. session below), `cold_draft`/`draft_context` (see below). First dataset: the Ontario Airport (`3950-airport-ontario`) 289-record Yardi call sheet — see the 2026-07-09 migration session below for how it got there (and how the earlier, wrongly-placed `outreach` copies were cleaned up). Second dataset: Pellissier (`pellissier-2720`) 75-record Yardi export, added same day — see the 2026-07-09 (cont.) session below.

**Reply-detection fields (added 2026-07-10, written by n8n workflow 23 — Outreach Reply Scanner, which watches this collection alongside the plain `outreach/{id}` collection in the same scan):** `reply_detected`(bool)`, reply_detected_at`(ISO — when the scanner found it)`, reply_received_at`(ISO — the actual message's `receivedDateTime`)`, reply_subject, reply_snippet`(bodyPreview, truncated ~220 chars)`, reply_web_link`(raw Graph webLink; the app applies `fixThreadUrl()` at render time)`, reply_seen`(bool — cleared to `true` via the modal's "Mark reviewed" button / `markPOReplySeen`, same pattern as Outreach's `markOutreachReplySeen`)`. See the 2026-07-10 session below for the full design.

### `property_outreach_context/{locationId}` (added 2026-07-09)
One doc per property — "what's true right now" for cold-drafting purposes: `available_space`, `discount`, `other`, `cubework_url` (added same day, see below — all free text, all optional), `updatedAt`. Edited via the talking-points panel shown once per property in the Prop Outreach tab (`savePOContextField`) — deliberately **not** auto-populated from Avail/Pricing/anything else; Justin sets it by hand so a draft never states something he hasn't actually chosen to say. Pre-fills the per-lead draft inputs in `openPOModal` (which can still be edited inline just for that one draft without changing this shared default) and is what n8n workflow 22 reads (well, the app passes it through in the webhook body — workflow 22 itself doesn't read this collection directly) when generating a cold draft.

**`cubework_url` (added 2026-07-09, before first import).** Justin wants every cold draft to include a link to that property's page on cubework.com. Unlike `available_space`/`discount`/`other`, this is property-level only — no per-lead override input in the modal, since the link never changes per lead — so `generatePODraft` reads it straight from `poContextMap[r.location]` rather than from a `po-draft-*` element. Passed to workflow 22 as `cubework_url` in the webhook body; the `Build Claude request` prompt was extended with a 3rd numbered instruction (renumbering the still-looking question to 4 and the sign-off to 5): if a link is given, work the exact URL into the body once, naturally, never shortened/altered/invented; if none is given, do not mention or invent one. The body word cap explicitly excludes the link and sign-off from the 90-word count so a long URL cannot silently crowd out the actual pitch. `Build Firestore patch` now also stores `cubework_url` in `draft_context` alongside the other three fields, for the same "what was actually stated in this draft" record-keeping.

### `site_outreach/{id}` (added 2026-07-20)
Backs the 📍 Nearby Prospects tab — per-site discovery of NEW nearby businesses to cold-outreach (importers/distributors/3PLs/fulfillment/manufacturers/etc), via **CoreClaw**'s Google Maps Scraper Worker. Deliberately separate from `outreach/{id}` (dead Apollo-based industry prospecting — Apollo sub cancelled) and `property_outreach/{id}` (Yardi call-sheet backlogs of EXISTING tenant leads, not new-business discovery). Doc id `{id}` = the real Google **`place_id`** (falling back to `data_id` if somehow absent — **there is no `cid` field on this Worker's output**, confirmed 2026-07-21 against a real result; the original design assumed one) — re-running discovery for a site naturally dedupes on this key. No bundled seed — live Firestore only, written by n8n workflow 25 (via workflow 20's generic `firestore-write` webhook, `bulk_update` action — see the design note in the 2026-07-20 (cont. #5) session below for why `bulk_update`, never `bulk_create`, is required here) and status/notes/call-log fields written directly from the browser (setDoc), same split as Property Outreach. Falls under `firestore.rules`' existing catch-all full-access rule — no rules change was needed. Fields: `location` (propId, matching `list_locations`/`availMap` keys), `place_cid` (holds the real `place_id`/`data_id` value, kept as a field too for convenience — field name unchanged from the original design to avoid an app-side rename, only its source changed), `name`, `address`, `city`, `state`, `website`, `phone`, `search_category` (which of the discovery run's keyword categories surfaced it — `importer`/`distribution company`/`wholesale distributor`/`3PL logistics company`/`freight forwarder`/`ecommerce fulfillment center`/`trucking company`/`manufacturer`/`moving and storage company` in v1), `google_category` (Google Maps' own `primary_category` for the business), `all_categories` (real array of strings, confirmed 2026-07-21), `latitude`/`longitude` (sourced from the Worker's nested `location:{lat,lng}` object, confirmed 2026-07-21 — not top-level fields as originally assumed), `rating` (sourced from the Worker's `review_rating` field), `review_count`, `contact_name`/`contact_title`/`contact_email`/`contact_linkedin` (picked from CoreClaw's `leads_enrichment[]` array — an array of contact objects with `full_name`/`job_title`/`email`/`linkedin_profile`/`mobile_number` plus company_* fields, confirmed 2026-07-21; the picker prefers a contact with a non-null email, else falls back to the first available contact — CoreClaw's LinkedIn-sourced contacts frequently have a null `email` even when a real person was found), `contact_phone` (new field, added 2026-07-21 — the contact's `mobile_number`; often the only reachable info when `email` is null, so it's surfaced in the app as a direct-dial fallback), all blank/null if enrichment didn't return them for that business, never fabricated, `discovered_at` (first-seen — see note below), `last_seen_at` (updated every re-run discovery finds the business again), `status` (not_contacted/attempted/reached/interested/not_interested/bad_number/do_not_call/promoted), `attempts[]` (`{id,at,outcome,note}`, appended by `logNPAttempt`), `last_contacted_at`, `notes` (free text, `saveNPNotes`), `archived` (soft-hide; set manually via `setNPArchived` or automatically by `logNPAttempt` on a 2nd "Not interested" — same convention as Property Outreach), `promoted_lead_id`.

**`discovered_at` is intentionally NOT written by every re-run** — workflow 25's masked `bulk_update` write only ever touches the CoreClaw-sourced fields listed above (never `status`/`notes`/`attempts`/`archived`/`promoted_lead_id`, so a re-discovery run can never clobber call history), and `discovered_at` was left out of that same masked write on purpose so it can't be pushed forward on every re-run either. The app defaults a **missing** `discovered_at` to `last_seen_at` at read time (same "default missing fields at read time" convention already used elsewhere in this app for `status`/`archived`/`attempts`), so first-seen is still knowable without a second, more complex conditional-write pass in n8n.

**CoreClaw's exact Worker input/output field names are UNVERIFIED** — built from CoreClaw's prose documentation (fetched live, no account existed at build time), not a real `/api/scraper` schema response or a real result record. The `Build custom params`/`Extract lead fields` Code nodes in workflow 25 do live schema-based substring matching specifically to hedge this (see the golden rule in that workflow's own comments — never hardcode a Worker's parameter names), but the field names for the **leads-enrichment** contact data (`contact_name`/`contact_title`/`contact_email`/`contact_linkedin`) are an outright best guess and must be checked against a real enriched result record the first time this workflow actually runs. See the 2026-07-20 (cont. #5) session below for the full caveat and deploy checklist.

**Research/draft fields (added 2026-07-22, written by n8n workflow 26 — Nearby Prospect Research & Draft):** `research_brief` (newline-joined string, 5-8 short facts, strongest recent trigger first — same shape as Outreach's), `email_subject`/`email_body` (the currently-active draft — mirrors whichever variant was picked), `email_variants` (array of `{style,subject,body}`, always exactly 4: mismatch/forced-bet/freedom/commitment — same 4-hook shape as `outreach/{id}`), `prospect_group`/`prospect_fit` (derived from `search_category`, not a full industry taxonomy — see workflow 26 below), `draft_confidence`/`confidence_reason`, `body_words`/`body_flesch`, `researched_at`. These are deliberately the **same field names** `outreach/{id}` uses for the identical purpose, so the app's Nearby Prospects modal reuses the exact same variant-tab/confidence-chip rendering code as the Outreach modal (see `openNPModal` in `index.html`) rather than building a second parallel UI.

### `assistant_threads/main` (added 2026-07-10)
One document — the persisted conversation behind the 🤖 Assistant tab. Fields: `messages` (array of `{role:'user'|'assistant', text, at, toolsUsed?}` — `toolsUsed` only present on assistant turns that called at least one MCP tool), `updatedAt`. Falls under `firestore.rules`' existing catch-all full-access rule — no rules change was needed (same as `property_outreach`). Only ONE thread doc exists (`main`) — `newAssistantThread()` just clears its `messages` array rather than creating a new doc id; multiple parallel threads were not asked for and would add UI (a thread picker) with no clear use case yet. See the 2026-07-10 (cont.) session below for the full design.

### `renewals/current`
One document — the per-tenant lease-renewal / expiration tracker. Written by the **n8n Renewals Sync (workflow 10)** from the Google Sheet renewal tracker (`gid 844109688`, link-viewable; read via its CSV `export?format=csv` URL — no Graph/creds for the read). Fields: `rows` (array of per-unit maps), `updatedAt`, `source`, `count`. Each row map: `property` (sheet's name, e.g. "CA Walnut" / "COI Pellissier 2720 (128)"), `unit`, `tenant`, `lease_type`, `amendment` (Renewal/Original Lease/Expansion/HoldOver), `end_date` (YYYY-MM-DD), `decision` (the "Increase Reason" outcome: Same Rate / Market Price / $0.10 Increase Only / 4% + $0.10 Increase Only / DO NOT RENEW / Pause Renewal / Renewed / New Office Rate — may be blank for not-yet-processed rows), `stage` (email lifecycle: Sent PI Email / Sent No Increase Email / DO NOT RENEW), `remarks` (joined Remarks/Additional/Retention notes), `monthly_rent`. Multi-unit sheet rows ("C09, C10") are **exploded into one record per unit** by the parser.

**Parsing nuance:** the sheet's right-hand columns drift (merged header cells), so the parser reads the stable left block by header NAME and resolves `decision`/`stage` by scanning each row against a known vocabulary (position-independent). The sheet stacks **two blocks**: the active cohort (decision + stage populated, real July end-dates) and a coded roster (`(818)` property / `(t000###)` tenant codes, uniform 7/31 placeholder-ish end-date, blank decision = not yet processed). Both are kept; they cover **disjoint units** per building (verified — no overlap), and blank `decision` renders as "—" so unprocessed rows read as preliminary. Node parser: **`n8n/parse_renewals.node.js`** (paste-ready copy of the Code-node JS).

**App surface (Avail tab, occupied unit rows):** `renewalsForLoc(propId, d)` builds a Map of unit-id→record for a location by matching the sheet `property`'s **place token** (`renewalPlace()` strips the `(code)`, a leading region prefix like `CA`/`COI`/`TX`, and trailing numbers → "pellissier"/"banana"/"walnut"…) against the location's `property + address` (substring, token ≥4 chars). Records WITH a decision win over blank ones. Each occupied unit row shows a color-coded `renewalBadge` (📅 exp date; amber ≤90 days, red if expired or DO NOT RENEW); the expand panel adds Lease ends / Renewal status / stage / remarks. Memoized via `renewalVer`/`_renewalLocCache`; the `renewals/current` listener bumps the version and re-renders. **Matching is heuristic** — only synced CA/COI locations match today; nationwide rows for un-onboarded sites simply don't display, and a new location matches automatically once its place name aligns. No snapshot fallback (unlike pricing).

### `config/properties`
One document. Contains a `properties` array — each entry:
```
id, driveId, itemId, property, address, yardi_url, sharepoint_url
```
(Some older entries also carry `available_statuses`/`hold_statuses` strings — these are **obsolete and ignored**; the parser normalizes statuses itself. The Add Location form still shows those fields but they have no effect.)

---

## n8n — ACTUAL current workflow state (both workflows combined in one file)

### Availability Sync chain
Triggered by: **Webhook** (path: `availability-sync`, receives `{ propId }`)
```
Webhook → Get property config → Find property → Get file metadata1 
→ Merge metadata → List worksheets1 → Pick data sheet 
→ Read sheet data1 → Parse + build patch → Write to Firestore1 [dead end]
```

### Add Location chain  
Triggered by: **Webhook1** (path: `add-location`, receives full location payload)
```
Webhook1 → Parse URL + generate ID → Resolve SharePoint file 
→ Extract file IDs → Read current config → Build updated config 
→ Save config → Save PA to availability → Get property config (→ continues into the sync chain)
```
Adding a location now also immediately syncs its availability — `Save PA to availability` writes the PA name/phone/email onto the availability doc and then feeds into the sync chain's `Get property config`.

---

## n8n changes (all applied 2026-06-08 — authoritative export: `n8n/5_add_location.json`)

### 1. Connect Add Location chain to sync — ✅ DONE
`Save config → Save PA to availability → Get property config` is wired, so adding a location immediately syncs it.

### 2. Parse + build patch — ✅ DONE (rewritten 2026-06-08)
The sync reads the licensee tab. **Tab names vary** ("BOT - NEW ACTIVE LICENSEE" at Pellissier, "BOT - Active Licensee Lists" at Reyes, etc.), so "Pick data sheet" matches by PATTERN — first worksheet whose name contains "LICENSEE", then "ALL UNITS", then starts-with "BOT", else sheet[0]. (Exact-name matching was the bug that left Reyes with zero units — it fell back to the wrong sheet.) The parser was rewritten to handle the real variations found in the live CA sheets:

- **Type normalization** — buckets units whether the sheet uses codes (`WH`/`OFFICE`/`DOCK`/`TRAILER`, e.g. Pellissier) or words (`Warehouse`/`Office`/`Dedicated`/`Shared`/`Trailer/Truck`/`Small Vehicle`, e.g. Reyes/Walnut). The OLD code only matched `WH`, so spelled-out sheets summed to **zero** units per category — that was the core bug.
- **Broad unit pattern** `/^[A-Z]{1,5}\d{1,4}(?:[A-Z]|-[A-Z0-9]{1,3})?$/i` — matches `A1`, `A01`, `A12B`, `A31-A`, `B01-A`, `C01-A`, `R101`, `DOCK01`, `DOCK 14`, `DD01`, `P01`.
- **A unit on hold counts as available**: `isHold = status/tenant contains "hold"`; `available = (status === 'vacant') || isHold`. Hold is kept as a separate boolean only for display (the Hold pill + notes), but holds are bucketed as available everywhere (summaries + filters).

Full node code: **`n8n/parse_build_patch.node.js`** (paste into the node's JS Code field). Tested against real Pellissier / Reyes / Walnut rows — every category returns correct non-zero counts and all edge-case unit IDs are captured. The reference copy `4_availability_sync.json` (node `av-009`) is already updated to match.

### 3. ~~Update "Build updated config" code~~ — NO LONGER NEEDED
The parser self-normalizes statuses, so per-location `available_statuses`/`hold_statuses` config is obsolete. Skip.

### 4. Fix "Find property" code — ✅ DONE
The dead `$('Prep PA patch')` reference is gone. Because "Find property" now runs from BOTH the availability-sync webhook AND the add-location tail (only one of those nodes executes per run), the propId lookup reads each inside try/catch so the unexecuted node doesn't throw:
```javascript
let propId = '';
try { propId = $('Webhook').first().json.body?.propId || ''; } catch (e) {}
if (!propId) { try { propId = $('Build updated config').first().json?.propId || ''; } catch (e) {} }
if (!propId) throw new Error('No propId found');
```
Also fixed: "Save PA to availability" was writing a literal `"="` into the PA fields — its jsonBody was rebuilt as a single `JSON.stringify({...})` expression.

### 5. ~~Add `available_statuses`/`hold_statuses` to Firestore entries~~ — NO LONGER NEEDED
Obsolete: the rewritten parser normalizes statuses itself, so no per-location status fields are required.

### Source-sheet finding (2026-06-08)
All CA PA Weekly Report workbooks share a consistent **`BOT - NEW ACTIVE LICENSEE`** tab (Layout A: `Unit # | Unit Type | Unit SF | Unit status | Company Name | ...`, header repeats per Warehouse/Office/Dock/Parking section). This is the canonical data source — NOT the Site Plan / parking-summary tabs (those vary per location and are floor-plan art). The earlier idea of building per-workbook "AppData" formula tabs is unnecessary: the BOT tab already IS the standard. Note: Yardi rent-roll exports (`_validated_` files) are clean too but Yardi is being discontinued, so do not depend on them.

---

## Current locations
| propId | Property | Notes |
|--------|----------|-------|
| `pellissier-2720` | 2720 Pellissier, City of Industry | Tab uses short type codes (WH/OFFICE/DOCK/TRAILER). R-units = OFFICE |
| `11179-banana-fontana` | 11179 Banana, Fontana | DD-units = DOCK. Parser handles DD01-DD14 |
| `reyes-compton` | Reyes, Rancho Dominguez | Licensee tab is named "BOT - Active Licensee Lists" — pattern match handles it; types spelled out (Warehouse/Office/Dedicated/Trailer) |
| `218-machlin-walnut` | 218 Machlin Ct, Walnut | single-digit (A1) + 3-digit office (R101) unit IDs |

More CA locations (Terminal West Sac, Fresno, Airport Ontario, La Mirada) are being onboarded via the Add Location flow; quick-fill entries live in `LOCATION_PRESETS`. SharePoint links for the CA set are in `sharepoint_location_links.txt`.

### Banana Fontana unit structure (from SharePoint)
- WH: A01-A09, B01-B05, C01-C03, D01-D04, E01-E07
- OFFICE: R01-R08
- DOCK: DD01-DD14
- PARKING: P01+

---

## App structure (index.html key functions)
- `renderAvailability()` — renders a search box + **sort toggle** + location cards **grouped by state (region)**. `stateOf(d)` parses the 2-letter state from the address (`, ST 99999`); groups are labeled via `STATE_NAMES`, "Other" last.
- **Sort by availability (added 2026-06-17).** `availSort` (`'sf'` default | `'name'`), toggled by `setAvailSort(key)` (re-renders). `availSfTotal(d)` = **total available square footage = warehouse `wh.avail_sf` + office `office.avail_sf`** (counted separately, summed; dock/parking excluded). When `availSort==='sf'`: regions are ordered by their total available sq ft (desc, "Other" last) and locations within each region by `availSfTotal` (desc, name tiebreak) — so the site with the most availability sits at the very top. `'name'` = the old A–Z order. Each region header shows a green `N sf avail` total. This is the "generate the site with the most availability" feature — it's a live sort of the Avail tab, not a separate report.
- `onAvailSearch(v)` / `applyAvailSearch()` — Avail search box; live-filters cards by name/address/id and hides empty state groups (toggles `display`, no re-render, so it stays snappy)
- `removeLocation(propId, ev)` — Remove button at the bottom of an expanded card; confirms, strips the entry from `config/properties`, deletes the `availability/{propId}` doc (card vanishes via onSnapshot). Direct Firestore, no n8n; does NOT touch the SharePoint sheet
- `renderLocCard(propId, d)` — collapsible card. Collapsed shows: availability chips, PA name+phone, Yardi/SP links, ⟳ refresh
- `renderLocBody(propId, d)` — expanded: stats grid, filter chips (Available/Occupied/All, default Available via `availFilter[propId]`; no Hold filter — holds are available), per-section unit row lists (WH/Office/Dock; Parking is count-only in the stats grid), floor plan. Rows are compact; clicking a row expands tenant/owner/phone/email/poc/notes. `isAvail(u) = u.available || u.hold`. Occupied rows also show a lease-renewal badge from `renewals/current` (see that collection above) — `renewalsForLoc`/`renewalBadge`; the expand adds Lease ends / Renewal status / stage / remarks.
- `setAvailFilter(propId, f, ev)` — sets the unit filter and re-renders the body
- `toggleUnitDetail(key, ev)` — expands/collapses a unit's detail panel
- `renderFloorplans(propId)` — **data-driven** SVG floorplan. Reads layout via `fpLayout(propId)` (Firestore `floorplans/{propId}`, else `FLOORPLAN_SEED` from `floorplan-seed.js`); `floorplanSVG()` renders items (`k:'u'` unit→live status colors via `fpRect`, `k:'l'` label, `k:'n'` note) over an optional faded `bg` image. Floor tabs auto-shown per floor present; properties with no layout show "No floor plan yet". Editor (Layer 2) is how others get added/updated.
- `triggerAvailSync(propId)` — POSTs `{ propId }` to Cloudflare Worker
- `submitAddLocation()` — POSTs full location payload to Cloudflare Worker
- `LOCATION_PRESETS` — quick-fill buttons in Add Location modal. Add entries here for each new location.
- unit rows (inside `renderLocBody`) — show a status pill (Available/Occupied/Hold) driven by the `hold`/`available` booleans, not by status text
- **Leads** have drag-to-reorder like Tasks (`leadDragStart/Over/Drop`, `order` field, `byOrder` sort), plus `toggleHideWaitlist()` — the "Hide ⏳" toggle in the Leads filter row that drops `wl_on` leads from the Do/Wait board (except when the ⏳ Waitlist filter is active).

### Gotcha — function exposure (ES module scope)
The `<script type="module">` means functions are NOT global. Any function referenced from an inline `onclick=`/`ondrag*=`/`oninput=` attribute MUST be added to the `window.* = ...` block near the bottom of the module, or it throws "X is not defined" at click time. (This caused the original drag bug — the handlers existed but weren't exposed.) When adding a new inline handler, always add the matching `window.` line.

## Floorplans — DATA-DRIVEN (decided 2026-06)
Layout lives in **data, not code**: Firestore `floorplans/{propId}` (editor-saved) overrides the bundled `floorplan-seed.js` (`window.FLOORPLAN_SEED`). Shape per property: `{ warehouse:{viewBox,bg,items[]}, office:{...} }`; each item `{k,id?,t?,x,y,w,h,bg?,tc?,anchor?}` where `k`: `u`=unit (live status by `id`), `l`=label (room/area box), `n`=note (free text). `bg` is an optional faded underlay image URL. Pellissier seeded from its old hardcoded SVG (renders identically — `pellissier-2720` warehouse D01-D05/B01/C01-C05/A01-A02, office R01-R09). **Editor (in `index.html`, `fpRenderEditor`/`fpStartEdit`/`fpSaveEdit` etc.):** "✏️ Edit" on the floor-plan section. Done so far — drag to move (`fpDragStart`/`fpDragMove`, SVG `getScreenCTM` transform, direct attribute updates mid-drag), corner-handle resize, Warehouse/Office switch, Save→`floorplans/{propId}` / Cancel (draft is a deep clone so edits can't corrupt the seed). **Sq-ft editing:** tap a unit → `fpSel`/`fpSetSf` → pending in `fpSfEdits` → on Save, writes the new `sf` into `availability/{propId}.units` (setDoc merge) so the location list / floorplan label / matching all update. The SharePoint availability sync **will overwrite** these on its next run — accepted tradeoff. **Reallocate (`fpPair`, `fpStartRealloc`/`fpReallocFromA`/`fpReallocFromB`):** from a selected unit, "reallocate with" another → their combined sq ft is locked and a slider + two linked inputs move footage between them live (A+B always = total); both write to availability on Save. A live "building total (sum of units)" readout is shown. Conserves the *pair* total (not gross building SF, which includes common area). **Split (`fpSplitUnit`, `fpNewUnits`):** "✂️ Split into two" on a selected unit creates a NEW unit (auto-suggested id `A01-B`/`-C`…, dedup-checked; box halved off the original), divides the sq ft 50/50, and opens the reallocation slider to fine-tune. On Save the new unit is **appended to `availability/{propId}.units`** (available/vacant defaults, type copied) so it shows in the location list. Caveat: a split-created unit (and sq-ft edits) are wiped by the next SharePoint sync — and after a sync the layout box for a removed unit shows gray "no data" until the BOT tab catches up. **Create-new flow (`fpSeedIfEmpty`/`fpAddUnitsAsGrid`/`fpUnplaced`/`fpAddUnplaced`/`fpDeleteBox`/`fpUploadBg`):** "✏️ Create floor plan" on a property without a layout auto-seeds a box for every unit on that floor (office floor = `OFFICE` type, warehouse floor = the rest) in a grid; "✚ Add N unplaced units" backfills any missing; "🗑 Remove box" deletes a box (layout only — the unit stays in availability; a not-yet-saved split unit is also unstaged); "📐 Upload blueprint" sends an image to Firebase Storage and sets the floor's `bg` as a faded tracing underlay. Still to build: dock markers; unit-ID rename/dropdown on a box.

---

## Follow-up model — DECIDED 2026-06-10: scanner only, sequencer OFF
Justin's rule is simple: **"no correspondence in 3 days → follow up,"** where correspondence = Outlook email (either direction) **OR** a logged contact brief (email/call/text/inbound) — internal `note` updates don't count, and only **outbound-cold** leads surface (inbound = needs-reply; see the leads section above). That's the **Follow-up Scanner (workflow 6)** + the 🔔 Follow-ups tab, and it's the ONLY follow-up mechanism we run. The 5-touch sequencer (workflows 1–3) is intentionally **left off** — it fires on a fixed cadence regardless of whether you've been in contact, which produces exactly the "false" notifications Justin wants to avoid. **In n8n, run only workflow 6** (plus the unrelated availability workflow 5). Do **not** activate 1/2/3. The sequencer docs below are kept only in case that decision is ever revisited.

**Updated 2026-06-12 — weekends.** The 3-day clock counts **business days**, not calendar days — weekends don't count as silence, so a Friday touch isn't "3 days cold" by Monday (it's 1). Implemented in both places via a `bizDaysBetween(fromMs, toMs)` helper: `index.html` (`contactInfo` returns `bizDays`; `isFollowupDue` triggers on `bizDays >= FOLLOWUP_DAYS`) and the scanner (workflow 6 `Compute days since contact` sets `followup_due` off `bizDays`). The displayed "Nd cold" / `days_since_contact` stay **calendar** days (truthful wall-clock); only the *due trigger* uses business days. Snooze still adds calendar days. (Holidays are not yet accounted for — weekends only.)

## Email sequencer (n8n workflows 1-3) — MANUAL-SEND model (currently OFF — see decision above)
Separate from availability. A 5-touch follow-up cadence. **Sending is manual** (Justin sends from his own Outlook) to avoid needing the `Mail.Send` Graph scope:
- **Activate workflows 1 + 3 only; workflow 2 stays OFF.**
- **Workflow 1** (`1_queue_checker.json`, 8am daily) drafts the next due touch and writes `pending_email` to the lead — Firestore only, no Graph. Fires for `seq_status === 'active'` leads (new leads default to `active`, so they auto-enroll) with an email and `next_due_date <= today`, up to 5 touches.
- **App** renders the draft (`renderEmailPreview`) with **✉️ Open in email** (`openSequencerEmail` → `mailto:`) and **✓ Mark sent** (`markTouchSent` → advances the cadence client-side: sets `current_step`, `last_touch_date`, `next_due_date` per `SEQ_CADENCE = {1:4,2:6,3:7,4:9}`, `seq_status` active/closed, clears `pending_email`). Plus Snooze/Skip. This replaced the old "Approve & Send" button (which wrote `seq_status='send_approved'` for workflow 2). All handlers exposed on `window.*`.
- **Workflow 3** (`3_reply_detector.json`, every 30 min, read-only `Mail.Read`) flips a replying lead to `seq_status='replied'`, removing it from the queue.
- **Workflow 2** (`2_send_trigger.json`) = the auto-sender; **unused** unless `Mail.Send` is later granted. To enable full auto-send: grant `Mail.Send` + admin consent, activate workflow 2, and swap the app's two send buttons back to one "Approve & Send". See `n8n/SETUP.md`.

## Follow-up Scanner (n8n workflow 6)
The follow-up system Justin actually asked for: surface "leads that haven't been contacted in 3 days." Separate from (and simpler than) the 5-touch sequencer — it never sends anything, it only **measures last contact from Outlook and flags cold leads**.

- **`n8n/6_followup_scanner.json`** — runs daily at 7am (cron `0 7 * * *`). Reads all `leads` from Firestore, filters in code to open/contactable leads (has email; not archived; stage not won/lost; `seq_status` ≠ `replied`; not snoozed), then for each lead queries Microsoft Graph `GET /users/{OUTLOOK_USER_ID}/messages?$search="participants:<email>"` and takes the newest message across the hits, recording its **direction** (`from`==lead ⇒ inbound, else outbound). Writes `last_contact_date`, `last_contact_dir`, `days_since_contact`, `followup_due` (outbound-cold, business-days), `followup_contacted`, `followup_checked_at` back to the lead. Reuses the **same two credentials** as the other workflows: `Microsoft OAuth2 — Cubework Outlook` and `Google Service Account — do-or-wait`, plus the `OUTLOOK_USER_ID` env var. No new credentials.
  - Graph note: `$search` can't be combined with `$orderby`, so it fetches the top 25 relevance-ranked hits and takes the max date in code — fine for a single correspondent. If a lead has zero email history, the date falls back to `createdAt` and `followup_contacted` is set false.
  - **splitInBatches v3 wiring**: the loop body hangs off output **index 1** ("loop"); output 0 ("done") is intentionally empty. (Workflow 3's loop wires the body to index 0 — that ordering is the older convention; follow the v3 ordering used here.)

### In-app surface (index.html, Leads tab)
- **`🔔 Follow-ups` filter button** (`#followup-filter-btn`, `setLeadFilter('followup')`) with a count badge of due leads.
- **Reminder banner** (`#followup-banner`) above the board: shows "N leads have gone quiet for 3+ days" with a "Review follow-ups →" button. Hidden when the Follow-ups filter is already active or nothing is due.
- **`🔔 Nd cold` chip** on each due lead card (or "no contact yet").
- **Suggested-message block** (`renderFollowupBlock`) inside a due lead's thread (only when there's no `pending_email`): **stage-aware, VALUE-LED** copy via `FOLLOWUP_BY_STAGE` (cold=First touch / contacted=Check-in / toured=Post-tour / proposal=Proposal nudge) plus a universal `FOLLOWUP_VALUE` and `FOLLOWUP_BREAKUP` in the **↻ cycle** (`cycleFollowupTpl` → `followupMsgFor`/`followupMsgsFor`). Each message leads with value, not "circling back": **playbook-grounded** value blocks (`FU_INCLUSIVE` = all-inclusive/no-CAM-NNN, `FU_FLEX` = month-to-month/30-day notice, `FU_FAST` = 24–48hr move-in, `FU_TCO` = ~20–30% lower all-in) **tailored by `lead.segment`** via `fuHook` (importer→ports/Cubeship, ecommerce→fulfillment/last-mile, 3pl→scale/multi-site, tenant→overflow/2nd site), **plus a live availability line** (`fuAvail` → `followupUnitStillOpen`/`followupAvailMatches`) that injects a REAL open unit from `availMap` when there's a match — the lead's specific unit if still open, else a size-matched unit (0.5×–2× of `unit_sf`/`sqft`/`wl_sf`, in watched-or-all-synced locations). Asserts only what's actually available right now (never a guess; empty string if no honest match), so no false claims. **No special-pricing injection yet** (deferred). Greets by first name only (no company-as-name); `fuSpace` keeps the space reference generic; surfaces the lead's most recent thread entry as a **↪ Latest update** context line so the real specifics live there, not in the draft. Buttons: **✉️ Open in email** (`openFollowupEmail` → `mailto:`), **📋 Copy** (`copyFollowupMsg`), **Snooze 3 days** (`snoozeFollowup`).
- Core logic: `FOLLOWUP_DAYS` (=3), `isFollowupDue(lead)` (outbound-cold; triggers on `bizDays`), `contactInfo(lead)` (returns `{days, bizDays, date, contacted, dir}` — `bizDays`/business days drives the due trigger, display uses calendar `days`). The block header shows "quiet N days" (the earlier "(Nd ago)" duplication is fixed). All inline handlers are exposed in the `window.*` block (per the ES-module gotcha).

### Quick-log call / text / inbound (launch + log in one tap)
Each active lead card shows a compact button row (`renderLeadCard` → `quickLog`): **📞 Call** / **💬 Text** when `lead.phone` is set (else a **📞 Add number** button that opens edit), plus **📥 Inbound** (logged when a lead calls/texts *you* — manual, since a personal cell's calls/iMessages aren't readable by any API). `logTouch(leadId, kind, e)` appends a dated thread entry (`📞 Called` / `💬 Texted` / `📥 They reached out`; `met` is still supported by the handler but has no button). Outbound kinds (call/text) log as type `wait` (you reached out, awaiting reply); **`inbound` logs as type `do`** (they reached out → ball's in your court, surfaces in the Do column). It then opens the card (accordion) so the outcome note is one tap away via 📝 Note, persists, then for call/text hands off to `tel:`/`sms:` (digits sanitized from `lead.phone`). This launches the dialer / Messages (iMessage on Apple) AND logs the touch in the same tap — which also resets that lead's 3-day follow-up clock (calls/texts are invisible to the Outlook scanner). New `phone` field added to the lead modal (`lf-phone`). `logTouch` is exposed on `window.*`. Note: `tel:`/`sms:` only do something on a device with a phone/Messages handler (iPhone, or Mac via Continuity) — the log still records on any platform.

---

## File locations
```
do_or_wait/
  index.html            ← entire app (push to GitHub to deploy)
  pricing-seed.js       ← auto-generated snapshot of the price sheet, loaded by index.html as the Pricing-tab fallback before/without the n8n sync. Regenerate from the master xlsx; NOT hand-edited. (Loaded via <script src> just before the module.)
  outreach-seed.js      ← cumulative CA outreach-prospect seed (loaded via <script src> before the module, like pricing-seed.js); overlaid live by the `outreach` Firestore collection.
  Copy of New Master List_ Price.xlsx ← the master price sheet (source for pricing-seed.js). GITIGNORED (large, internal, repo is public). CLAUDE.md elsewhere calls it "New Master List_ Price.xlsx" — the on-disk file has the "Copy of " prefix.
  REPLY_ASSISTANT_SPEC.md ← design spec (2026-06-17, status: Proposed) for an in-app "Reply Assistant". NOT yet built into index.html.
  reply_assistant_flow.mermaid ← flow diagram companion to REPLY_ASSISTANT_SPEC.md (the spec is still Proposed/unbuilt).
  firestore.rules        ← Firestore security rules (locked to the 3-email allowlist; published in console 2026-06-24). Companion doc: FIRESTORE_SECURITY.md.
  FIRESTORE_SECURITY.md  ← notes on the rules lockdown + the public-vs-secret apiKey explanation.
  CLAUDE.md             ← this file
  .gitignore            ← keeps the master price xlsx, *.xlsx, node_modules/, and .writetest.tmp out of git
  sharepoint_location_links.txt ← SharePoint URLs for the CA locations (input for presets)
  firestore-mcp/        ← SOURCE for the "Do or Wait — Firestore" MCP connector. Lets Claude read/write the do-or-wait Firestore live (list/create/update tasks·leads·notes, get_availability/pricing/renewals, trigger/sync availability, query_collection escape hatch). Runs on Justin's machine (sandboxes can't reach Firestore). NOTE: the connector that's actually live is an installed **desktop-extension** build that signs in with an allowlisted **email/password** account (nothing fancy) — so it gets an ID token that passes the locked rules. This repo folder is its SOURCE and lags the deployed build: the repo `server.js` still does unauthenticated web-API-key REST calls (`?key=…`, no token), which is the older approach. README.md, server.js, install.mjs, selftest.js, manifest.json, package.json; node_modules/ is gitignored. ⚠ README.md's "Firestore rules are open" line is STALE (rules are locked per 2026-06-24).
  n8n/
    -- core syncs / app webhooks --
    5_add_location.json        ← AUTHORITATIVE combined live workflow (availability-sync + add-location chains + daily cron sync — see 2026-07-06 below). Import THIS. Edit by hand here (bash mount read-only) or in n8n UI, then re-export.
    parse_build_patch.node.js  ← paste-ready copy of the "Parse + build patch" node code
    4_availability_sync.json   ← older reference copy of just the sync chain (REDUNDANT — 5_add_location.json supersedes it; deletion candidate)
    6_followup_scanner.json    ← Follow-up Scanner: flags leads cold 3+ days (Outlook/Graph → Firestore, with direction). No sending.
    7_pricing_sync.json        ← Pricing Sync: Sales_US master price sheet (Graph workbook API) → pricing/current. Daily 6am.
    10_renewals_sync.json      ← Renewals Sync: Google renewal tracker (CSV export, no creds) → renewals/current. Daily 7:30am.
    parse_renewals.node.js     ← paste-ready copy of the "Parse renewals" node code (workflow 10)
    11_resolve_thread.json     ← Resolve Thread Link: webhook (path `resolve-thread`), {subject,contact} → newest message `webLink`. Powers 🔗 Email threads. Request-driven (activate, NOT cron).
    resolve_thread.nodes.js    ← paste-ready copy of workflow 11's two Code nodes
    -- outreach pipeline (Apollo) --
    12_apollo_ingestion.json   ← Apollo Ingestion: people-search → reveal email+phone → score+draft → outreach/{id}. (+ .README.md, apollo_ingestion.nodes.js)
    13_research_prospect.json  ← Research & Personalize: webhook `research-prospect`, live web research + personalized draft → outreach/{id}. Must be Active. (+ .README.md, research_prospect.nodes.js)
    14_apollo_org_discovery.json ← Apollo Org Discovery (reusable last-mile / fashion-district org search → paste-ready array for wf12). Reads only. (+ .README.md, .nodes.js, apollo_org_discovery.reusable.nodes.js, socal-last-mile.discovery-config.js)
    15_apollo_phone_callback.json ← Apollo Phone Callback: webhook `apollo-phone-callback`, Apollo pushes phones → outreach/{slug}. Keep ACTIVE. (LIVE phone path)
    16a_apollo_phone_reveal.json ← Apollo Phone Reveal: requests phone reveals + writes apollo_phone_map bridge. (LIVE phone path)
    phone_reveal.nodes.js      ← paste-ready Code-node copies for the phone-reveal flow
    8_prospect_finder.json     ← Prospect Finder: nightly (4am) import/score leads (Claude scoring) → Firestore. (+ .README.md, prospect_score.node.js)
    -- leads follow-up generator --
    17_lead_followup_generator.json ← Generate Lead Follow-up: webhook `generate-followup`, reads the lead's email thread (Graph, last ~5 messages by conversationId) or falls back to app-sent notes text, drafts a value-add follow-up via Claude, writes `followup_draft` → leads/{id}. Imported, re-imported 2026-07-06/2026-07-07/2026-07-15/2026-07-20 to keep prompt edits current — confirmed current as of 2026-07-20. (+ lead_followup_generator.nodes.js). See 2026-07-06 session below.
    -- pricing bands (internal Sales/BD) --
    18_pricing_bd_sync.json    ← Pricing BD Sync: internal rate-bands sheet (Graph workbook API, tab-per-state) → pricing_bd/current. Daily 6:30am cron + manual webhook `pricing-bd-sync`. NEW, not yet imported. (+ pricing_bd_sync.nodes.js)
    pricing_bd_raw_grid.json   ← real `usedRange()` fixture (AZ tab) the parser in workflow 18 was built and verified against. Dev/reference only, not read at runtime.
    -- staff roster (contact directory) --
    19_roster_sync.json        ← Roster Sync: Locations Master Roster sheet (Graph workbook API, single flat-table tab) → roster/current. Manual webhook `roster-sync` only, no cron. NEW, not yet imported.
    roster_raw_grid.json       ← real `usedRange()` fixture (Locations tab) the parser in workflow 19 was built and verified against (62 rows). Dev/reference only, not read at runtime.
    -- generic Firestore writer + one-off migrations (2026-07-09) --
    20_firestore_writer.json   ← generic Firestore writer: webhook `firestore-write`, {collection,action,items/fields} → one Firestore `batchWrite` call. Supports create/bulk_create/update/bulk_update against ANY collection. LIVE — used to migrate the Ontario Airport call sheet into property_outreach. Stays live for future bulk/structural Firestore work without a tools.js redeploy cycle.
    21_cleanup_ontario_outreach.json ← one-off: webhook `cleanup-ontario-outreach`, live-queries `outreach` for location==ontario-airport AND source==yardi and deletes only those matches (dry-run via `confirmDelete:false` first). LIVE, single-use — used once to clear the stray Ontario copies out of `outreach` after the property_outreach migration. Safe to deactivate/delete now that it's done.
    -- property outreach cold draft --
    22_property_outreach_draft.json ← Property Outreach Cold Draft: webhook `property-outreach-draft`, {id,company,location,wave,sqft,source_notes,available_space,discount,other} → Claude drafts ONE cold first-touch email (no web research, unlike wf13 — most `company` values are a person's name from Yardi, not a researchable business), writes `cold_draft`+`draft_context` → property_outreach/{id}. Manual only, never auto-triggered. NEW, not yet imported. See 2026-07-09 session below.
    -- outreach reply detection --
    23_outreach_reply_scanner.json ← Outreach Reply Scanner: cron every 2hrs, ONE windowed Graph inbox scan (not a per-prospect loop, not a full collection list) → extracts distinct new senders → targeted Firestore IN-filter query against BOTH outreach AND property_outreach → flags reply_detected/reply_seen on either via workflow 20's firestore-write webhook. LIVE — imported and active (confirmed 2026-07-14). See 2026-07-10 session below. (+ outreach_reply_scanner.nodes.js)
    -- in-app assistant --
    24_assistant_chat.json     ← Assistant Chat: webhook `assistant-chat`, {messages} → Claude via the native MCP connector (mcp_servers + tools:[mcp_toolset], beta header mcp-client-2025-11-20) against the do-or-wait Cloud Run remote MCP server → {ok,reply,toolsUsed}. Backs the in-app 🤖 Assistant tab. **Imported and confirmed live 2026-07-10** (see the 2026-07-10 (cont. #2) session below — post-import fixes: hardcoded MCP URL, prompt caching, both applied directly in n8n). (+ 24_assistant_chat.README.md)
    -- nearby-prospects discovery (CoreClaw) --
    25_site_outreach_discovery.json ← Site Outreach Discovery: manual webhook `site-outreach-discover`, {propId,address,categories?} → loops 9 categories through CoreClaw's Google Maps Scraper Worker (business-leads enrichment on) → dedupes by cid → writes site_outreach/{cid} via workflow 20's bulk_update. Backs the in-app 📍 Nearby Prospects tab. NEW, not yet imported. CoreClaw field names UNVERIFIED — see 2026-07-20 (cont. #5) session below. (+ site_outreach_discovery.nodes.js)
    26_nearby_prospect_research.json ← Nearby Prospect Research & Draft: webhook `nearby-prospect-research`, {id,name,website,search_category,google_category,address,city,state} → Claude web research + 4-hook draft (forked from workflow 13's engine), writes research_brief/email_variants/confidence → site_outreach/{id}. Backs the in-app 📍 Nearby Prospects modal's 🔎 Research & draft button. NEW, not yet imported. See 2026-07-22 session below. (+ nearby_prospect_research.nodes.js)
    -- email sequencer (currently OFF) --
    1_queue_checker.json · 2_send_trigger.json · 3_reply_detector.json · templates.json
    SETUP.md
  outreach/                    ← outreach-campaign working files (CSVs, plans, cadence docs). Source/working artifacts, not read at runtime by the app.
    christine-data-analysis.html ← analysis of Christine's importer workbook (prospect source)
    ca-core-fit-batch1.csv · ca-distributors-trading-batch2.csv · ca-korean-fashion-district-batch3.csv · socal-last-mile-batch4.csv ← prospect batches fed into wf12
    socal-last-mile-curated.js   ← curated last-mile list
    household-durables-queue.csv / .xlsx ← household-durables prospect queue
    outreach-hub.md · 30d-launch-outreach.md · korean-fashion-district-plan.md ← campaign plans/notes
    outreach-send-cadence-and-templates.md / .html / .pdf ← send cadence + templates (3 formats of one doc)
  location_blueprints_sp/      ← per-location Site Plan tab CSV + .xlsx exports (one subfolder per location). Floorplan-parser dev fixtures only; NOT read at runtime (and the floorplan feature is abandoned — see below).
  note_emls/                    ← drop .eml files here to have Claude extract trigger/why/fix notes from an email thread and create them in the 💡 Notes tab. Processed source emails move to note_emls/processed/ so they aren't re-processed. See the 2026-07-03 → 2026-07-06 session below for how this actually works today (via the "Do or Wait — Firestore" MCP connector's create_note tool, not a file export).
```
**Cleanup (Stage 2, done 2026-06-29):** deleted `.writetest.tmp` (0-byte temp), `least-occupied-warehouses-2026-06-22.html` (one-off report), `n8n/16b_apollo_phone_write.json` (dead poll path), and `n8n/14_15_phone_reveal.README.md` (stale — pointed at the never-created `14_apollo_phone_backfill.json`). Still on disk: `n8n/4_availability_sync.json` (redundant reference copy — kept as a backup; delete if you want). `n8n/14_apollo_org_discovery.README.md` is current — keep.

## Floor plans = LINK to the SharePoint Site Plan (FINAL — decided 2026-06-17)
**Whole floor-plan feature abandoned in-app.** After trying auto-reconstruction (Graph + `.xlsx` color-region), uploaded images, and a LibreOffice weekly auto-render, Justin's final call: the n8n host can't install LibreOffice, and manual screenshots don't scale to 80+. So the in-app "Floor Plan" section is now **just a link** — `renderFloorplans` reads `availMap[propId].sharepoint_url` (+ `yardi_url`) and renders "📄 Open Site Plan (SharePoint) ↗". No rendering, upload, editor, or n8n. **Cleanup done 2026-06-18:** the dead floorplan artifacts were deleted from the repo — `n8n/8_floorplan_sync.json`, `n8n/9_floorplan_image_render.json`, `n8n/render_siteplan.py`, and the root `floorplan-seed.js` (the in-app machinery `fpUploadPlanImage`/`fpRemovePlanImage`/editor `fpStartEdit…`/`floorplanImages` listener/`FLOORPLAN_SEED` was already gone from `index.html`). The two sections below are kept as the written record of what was tried and why it didn't work — the files they describe no longer exist.

## (HISTORICAL) Floor plans = UPLOADED Site Plan images
**Auto-reconstruction was abandoned.** Both the n8n Graph sync (workflow 8) and the in-app `.xlsx` color-region importer were built and tested, but neither reliably matched the hand-drawn SharePoint plans (scattered boxes, misread dock-label letters like "LOADING", missing unlabeled big blocks). Justin's call: stop reconstructing — **just show the real drawing as an image.**

How it works now: each location's floor plan is a **screenshot/export image of the SharePoint Site Plan** that Justin uploads in-app. Stored in **`floorplan_images/{propId}` = `{ warehouse:<url>, office:<url>, updatedAt }`** (Firebase Storage for the file, this doc for the URLs). `renderFloorplans` is **image-first**: if `floorplanImages[propId][floor]` exists it shows the `<img>` (click → open full-size) with floor tabs + **📷 Replace / 🗑 Remove**; else it falls back to the box-layout view (seed/editor) with a **📷 Use image** option. Empty state offers **📷 Upload Warehouse/Office plan** + **✏️ Draw manually**. Handlers: `fpUploadPlanImage(file,propId,floor)` (→ Storage → `floorplan_images/{propId}`), `fpRemovePlanImage(propId,floor)` (writes the floor to `null`), `fpUpLabel` builds the file-picker. Both exposed on `window.*`. Tradeoff: the image is a snapshot (status colors as of upload); live status still lives in the availability list. The manual box editor (drag/resize, live colors) remains for anyone who wants clickable units.

**Removed from `index.html`:** the `.xlsx` importer (`fpImportXlsx` + SheetJS reconstruction), the **⚙ Generate** button + `triggerFloorplanSync`/`FLOORPLAN_WEBHOOK`. **`n8n/8_floorplan_sync.json` is left in the repo but unused/deprecated** — do not import it. The two sections further below are kept as a record of what was tried and why it didn't work.

### Weekly auto-render (n8n workflow 9) — the real engine behind the images
Manual upload doesn't scale to 80+ locations, so the images are produced automatically. **Key insight: a real renderer (LibreOffice) photographs the Excel Site Plan tab faithfully — colors, merges, labels — and doesn't care that every location is drawn differently, so it scales.** Proven on La Mirada: a single-page, full-color render identical to the SharePoint plan.

- **`n8n/render_siteplan.py`** (runs on the n8n host): given a location's `.xlsx` + propId + out dir, it (1) isolates the Site Plan tab by hiding every other sheet in `xl/workbook.xml` (strip any `state=`, add `state="hidden"`, set `activeTab`), (2) forces fit-to-one-page (patch the sheet's `<pageSetup fitToWidth/Height=1>` + `<pageSetUpPr fitToPage="1"/>`, landscape), (3) `libreoffice --headless --convert-to pdf`, (4) `pdftoppm -png -singlefile`, (5) `convert -trim`. Renders the warehouse tab and, if a separate office tab exists (Terminal/Walnut/347 Stimson), an office tab too. Per-location tab overrides in `PROFILES` (banana=`WH Site Plan`, terminal office=`OFFICE SITEPLAN`); all others auto-pick by name. Prints JSON `{warehouse:<png>, office:<png>|null}`.
- **`n8n/9_floorplan_image_render.json`** (weekly Mon 5am + on-demand webhook `floorplan-render`): get `config/properties` → loop → Graph download the `.xlsx` to `/tmp/fp/` → **Execute Command** runs `render_siteplan.py` → read each PNG → **upload to Firebase Storage** (`POST firebasestorage.googleapis.com/v0/b/{bucket}/o`, relies on the same open Storage rules the app's manual upload uses — no extra creds) → PATCH `floorplan_images/{propId}` with the download URL. Feeds the **same image view already in the app**, so no app change; manual upload stays as an override.
- **Host prerequisites** (install once on the n8n box): `sudo apt-get install -y libreoffice-calc poppler-utils imagemagick`, and drop `render_siteplan.py` at `/opt/cubework/render_siteplan.py`. Bucket defaults to `do-or-wait.appspot.com` (verify; override via n8n var `FIREBASE_BUCKET`). Reuses the existing `Microsoft OAuth2 — Cubework Outlook` (Graph download) + `Google Service Account — do-or-wait` (Firestore) creds. **Not yet run against live n8n — test on one location via the webhook before enabling the weekly schedule.**

## (SUPERSEDED) Floorplan Sync (n8n workflow 8) — shared parser + per-location profiles
Auto-generates floor-plan layouts from each location's SharePoint **Site Plan tab(s)** so we don't hand-trace blueprints. Same SharePoint→Firestore pattern as availability/pricing. The Site Plan layout rarely changes, so it runs **weekly (Mon 5am)** plus an **on-demand "⚙ Generate" button** per location (`triggerFloorplanSync` → `FLOORPLAN_WEBHOOK` → n8n). It writes `floorplans/{propId}`, **overwriting** any manual editor layout (Justin's call 2026-06-17 — keep it simple; editor tweaks hold until the next sync).

**Why Graph, not CSV/the SharePoint connector:** Graph workbook `usedRange(valuesOnly=true)` returns a **2D `values` array that preserves cell row/col** — which is what encodes unit positions. The SharePoint MCP connector and plain text extraction *flatten* the grid (positions lost), so they can't drive a layout. (Confirmed against Fresno: connector gave every unit + SF + tenant but no positions.)

**Shared parser + profiles (decided 2026-06-17).** One parser; a small `FLOORPLAN_PROFILES` map keyed by propId tunes the few things that differ per location. The n8n "Pick tabs" node switches on propId → profile (tolerant/normalized match, like `fpLayout`), but the branch only selects *parameters*, not separate code. New locations use the **auto default** (`{}`); add a 3-line override only when one looks off. A profile may also set `custom: fn` as an escape hatch.
- Profile fields (all optional): `tabs:[names]`, `officeTabs:[names]` (else auto-pick by name pattern `/site plan|warehouse|wh|layout|floor/i`, office = `/office/i`), `minCol/maxCol/minRow/maxRow` (clip region), `colw/rowh` (fixed scale; else auto-fit to ~960×680 preserving aspect).
- Current overrides: `banana-fontana → { tabs:['WH Site Plan'], minCol:14 }` (its Site Plan tab has a left **data list** in cols 1–6 that would otherwise stack every unit into one column; the real drawing is cols ≥14). `terminal-west-sac → { tabs:['Site Plan'], officeTabs:['OFFICE SITEPLAN'] }` (two-tab WH+office). All other 10 locations parse on the auto default.

**Parser rules (carry all observed formats — Banana grid, La Mirada free-form art, Terminal two-tab+parking):** (1) unit-ID regex `/[A-Z]{1,3}\d{1,3}(?:-[A-Z0-9]{1,2})?/` + a `Dock #N` rule; (2) NOISE denylist (`M2/AC/CC/OHD`) + skip blank/nbsp/`Â` mojibake cells; (3) type by prefix (`R`→OFFICE, `P`→PARKING, `DOCK`→DOCK, else WH); (4) cell `(col,row)`→`(x,y)`, box width from any nearby `… SF`, dock/parking = small markers; (5) **floors split by unit TYPE** (OFFICE→office floor, rest→warehouse) so a single combined tab AND separate WH/office tabs both work. Units present in the BOT availability data but **not drawn** on the Site Plan (e.g. Banana's DD docks / P / R04–R08) get no box — backfill them via the editor's "add unplaced units". `parse_build_patch`-style logic lives in workflow 8's "Build floorplan patch" Code node.

## (SUPERSEDED) Import Site Plan (.xlsx) — in-app reconstruction
The Graph sync (workflow 8) only sees cell *text*, so its layout is a rough scaffold. The **accurate** path is the in-app **"📥 Import Site Plan (.xlsx)"** button (in the floor-plan section header, the empty state, and the editor toolbar). It reads the real workbook **client-side with SheetJS** (lazy-loaded from cdnjs `xlsx@0.18.5`; nothing is uploaded anywhere) and reconstructs the layout from what only the raw file has: **merged-cell ranges + cell fill colors**.
- Why it works: the Site Plan draws each unit as a **colored block of cells** (green=available, pink=occupied, yellow=Cubework/UNIS). Graph `values` and CSV both discard colors+merges; the `.xlsx` keeps them. Columns have **no custom widths** in these files, so uniform cell size is correct — no width data needed.
- Algorithm (`fpReconColor`): build the cell grid (propagate each merge's fill across its range), take every unit-ID label as a **seed**, then assign each colored cell to its nearest seed (**Voronoi by label**) — this separates touching same-status units that a plain color flood-fill would merge. Each seed's territory bbox → a box (uniform `CW=22,CH=17`, then `fpScaleFit` caps the longest side at 1400). Floors split by unit **type** (R→office, else warehouse); a separate office tab (e.g. Terminal `OFFICE SITEPLAN`, Walnut `Office Layout`) overrides the office floor.
- **Grid fallback** (`fpReconGrid`): if color reconstruction yields <4 units (drawing not color-filled), fall back to text-grid parsing + SF-based box sizing. (Banana's WH tab needs `minCol:14` either way to skip its left data list — and with that it color-reconstructs fine, ~27 units.)
- Per-location tuning: **`FP_XLSX_PROFILES`** in `index.html` (currently `banana-fontana → {tab:'WH Site Plan', minCol:14}`, `terminal-west-sac → {tab:'Site Plan', officeTab:'OFFICE SITEPLAN'}`; all others auto). Same tolerant propId matching as `fpLayout`.
- Flow: `fpImportXlsx(file, propId)` → reconstruct → load into `fpDraft` → drops you into the **editor** to nudge → **Save** writes `floorplans/{propId}` (same as any editor save). So import = a faithful *starting draft*, not a final commit. Verified on Banana/La Mirada/Terminal/Walnut/Warner (27/38/75/68/40 warehouse units).
- The **`location_blueprints_sp/<location>/`** folders now also hold each location's real **`.xlsx`** (not just CSVs) — these are the fixtures used to build/verify the color profiles.

---

## Quick reference — webhook URLs
- Availability sync: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/availability-sync`
- Add location: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/add-location`
- Resolve thread link: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/resolve-thread` (POST `{subject,contact}` → `{found,webLink,conversationId,…}`; n8n workflow 11)
- Research prospect: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/research-prospect` (POST `{id,company,domain,industry,location}` → web research + personalized draft PATCHed to `outreach/{id}`; n8n workflow 13)
- Generate lead follow-up: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/generate-followup` (POST `{leadId,company,first_name,contact,segment,stage,unit,unit_sf,conversationId,notesText}` → value-add draft PATCHed to `leads/{id}.followup_draft`; n8n workflow 17 — imported, re-imported 2026-07-06 and again 2026-07-20 to pick up prompt edits that had gone stale in between; confirmed current as of 2026-07-20)
- Pricing BD sync (manual): `https://plain-credit-5962.jchoustin91.workers.dev/webhook/pricing-bd-sync` (POST `{}` → re-syncs `pricing_bd/current` on demand; n8n workflow 18 — status not confirmed imported/active anywhere in this doc's session log; also runs on its own daily 6:30am cron once imported)
- Roster sync (manual only, no cron): `https://plain-credit-5962.jchoustin91.workers.dev/webhook/roster-sync` (POST `{}` → re-syncs `roster/current` on demand; n8n workflow 19 — status not confirmed imported/active anywhere in this doc's session log)
- Generic Firestore writer: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/firestore-write` (POST `{collection,action,items|fields,id?}`, action ∈ create/bulk_create/update/bulk_update → one Firestore `batchWrite`; n8n workflow 20 — LIVE, used repeatedly through 2026-07-20)
- Property outreach cold draft: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/property-outreach-draft` (POST `{id,company,location,wave,sqft,sqft_est,source_notes,available_space,discount,other,cubework_url}` → cold first-touch draft PATCHed to `property_outreach/{id}.cold_draft`; n8n workflow 22 — status not confirmed imported/active anywhere in this doc's session log, despite several sessions of prompt edits riding along with "the same first import")
- Outreach reply scan (manual "⟳ Check now"): `https://plain-credit-5962.jchoustin91.workers.dev/webhook/outreach-reply-scan` (POST `{}` → runs the next reply-detection scan immediately, resuming from the last cursor; n8n workflow 23 — LIVE, imported and active, confirmed 2026-07-14; also runs on its own cron every 2 hours)
- Assistant chat: `https://plain-credit-5962.jchoustin91.workers.dev/webhook/assistant-chat` (POST `{messages}` → Claude via the native MCP connector → `{ok,reply,toolsUsed}`; n8n workflow 24 — LIVE, imported and confirmed 2026-07-10)
- Site outreach discovery (manual, per-site): `https://plain-credit-5962.jchoustin91.workers.dev/webhook/site-outreach-discover` (POST `{propId,address,categories?}` → CoreClaw Google Maps scrape per category, writes `site_outreach/{cid}`; n8n workflow 25 — status not confirmed imported/active anywhere in this doc's session log; also CoreClaw's own field names are UNVERIFIED, see the 2026-07-20 (cont. #5) session)
- Nearby prospect research & draft (manual, per-business): `https://plain-credit-5962.jchoustin91.workers.dev/webhook/nearby-prospect-research` (POST `{id,name,website,search_category,google_category,address,city,state}` → Claude web research + 4-hook draft PATCHed to `site_outreach/{id}`; n8n workflow 26 — NEW, not yet imported, see the 2026-07-22 session)
- n8n base: `https://ailinker.item.com`
- **Note:** the earlier "Floorplan sync" webhook line was removed from this list 2026-07-21 — the floor-plan-reconstruction feature was fully abandoned 2026-06-17/18 (see that section below) and `FLOORPLAN_WEBHOOK`/`triggerFloorplanSync` no longer exist anywhere in `index.html` (confirmed via grep, 0 hits).

---

## 2026-06-24 session — Outreach pipeline, research agent, auth lockdown

### App auth — email/password gate (was open → Google → email/password)
- Firebase **email/password** sign-in. `ALLOWED_EMAILS` (jchoustin91@gmail.com, justin.cho@cubework.com, cubeworkautomation@gmail.com) gates the app; `onAuthStateChanged` shows `#auth-screen` until a valid user signs in, else hides `#app`. Handlers: `doEmailSignIn` / `doSignOut`. `firebaseConfig` gained `authDomain: do-or-wait.firebaseapp.com`.
- **Why not Google:** the OAuth redirect crosses github.io ↔ firebaseapp.com, which mobile (esp. iOS) blocks via storage partitioning → bounce back to login. Email/password has no cross-domain step, works everywhere. Passwords are set in Firebase console → Authentication → Users.
- **Listeners DEFERRED until auth:** `liveSnapshot()` wraps every `onSnapshot` and queues it; `startLiveData()` flushes the queue on first valid sign-in. So nothing reads Firestore before auth (would 403 under the locked rules). All 7 listeners use `liveSnapshot`.
- **App REST writes carry the ID token:** `savePA` / `saveOwnership` send `Authorization: Bearer <idToken>` via `authToken()` so they pass the rules.

### Firestore rules — LOCKED DOWN (`firestore.rules`, published in console)
- read/write only if `request.auth.token.email.lower()` is in the 3-email allowlist. **No `email_verified` check** — console-created email/password accounts are not verified, so requiring it locks everyone out. The allowlist is the gate. Companion: `FIRESTORE_SECURITY.md`. The `AIzaSy…` apiKey is a public Firebase web key (not a secret); the open rules were the real exposure, now closed.

### CONSEQUENCE for n8n — Firestore nodes MUST authenticate
- The lockdown 403s any n8n HTTP node that hit Firestore REST with `authentication: none`. **Fix:** every Firestore HTTP node uses `predefinedCredentialType` → `googleApi` → a Google **service-account** credential with the **datastore scope** and the "Set up for use in HTTP Request node" toggle ON. Service accounts use IAM and **bypass security rules** (so n8n is unaffected by the email allowlist).
- Workflows **4 and 5** (availability / add-location) were unauthenticated and were fixed to use the SA credential. The user created a fresh Firebase Admin SDK key credential named **`Firebase_SDK_do_or_wait`** (datastore scope). Other workflows already used a SA credential. The app's end-user auth (email/password) and the service-account auth are separate systems — changing one doesn't affect the other.

### `outreach/{id}` collection (NEW) — written by workflow 12, patched by 13
Doc id `ap-<domain-slug>`. The 📧 Outreach tab reads it live and overlays `outreach-seed.js` (matched by domain). Fields: `company, domain, first_name, contact`(title)`, seniority, department, headline, photo, email, email_status, linkedin, person_loc, phone`(empty until reveal-on-reply)`, industry, employees, founded, annual_revenue, total_funding, latest_funding, growth_6mo/12mo/24mo, num_jobs, description, org_phone, org_address, org_state, org_website, org_linkedin, teu, prospect_tier, prospect_score, why_now, email_subject, email_body, status`(user-edited)`, invalid`(user flag — hidden from normal views, restore via the "Invalid" filter)`, research_brief + researched_at`(from wf 13)`, prospect_source, createdAt`.

**Reply-detection fields (added 2026-07-10, written by n8n workflow 23 — Outreach Reply Scanner):** `reply_detected`(bool)`, reply_detected_at`(ISO — when the scanner found it)`, reply_received_at`(ISO — the actual message's `receivedDateTime`)`, reply_subject, reply_snippet`(bodyPreview, truncated ~220 chars)`, reply_web_link`(raw Graph webLink; the app applies `fixThreadUrl()` at render time, same convention as `email_links[]`)`, reply_seen`(bool — cleared to `true` via the modal's "Mark reviewed" button / `markOutreachReplySeen`, same "flag persists, seen-state clears the badge" pattern as `hasNew` on Leads' email links)`. See the 2026-07-10 session below for the full design.

### Claude in-app chat tools (index.html, `window.*`-exposed)
For driving updates from a chat assistant against the LIVE page (the sandbox can't reach Firestore; the browser can): `claudeFindLeads`, `claudeLogLeadUpdate` (append a categorized do/wait thread entry to a lead), `claudeFindTasks`, `claudeAddTask`, `claudeAddTaskUpdate`. They match by id/name/title, return candidates on ambiguity, and write via the app's own persist path.

### n8n workflow 12 — Apollo Ingestion (CA core-fit → Outreach) — `n8n/12_apollo_ingestion.json`
Manual trigger. Per company: **Apollo People Search** by domain (`mixed_people/api_search`, free, no email) → **Pick best contacts** (1–2 warehousing titles, ALWAYS returns an item) → **IF "Has contact?"** (no-contact companies route straight back to the loop so the batch never dies) → **Apollo reveal email** (`people/bulk_match`, `reveal_personal_emails:true`, phone OFF — phone is reveal-on-reply, 8 credits) → **OpenAI score + draft** (gpt-4o, industry-aware) → **Build prospect doc** (rich fields, dash-stripped, **NO score gate** — every qualified company is written; tier is just a label) → **Create prospect in Firestore**. `splitInBatches` batchSize 1; `retryOnFail` on the network nodes; **no Wait/sleep node** (it stalls manual runs and hogs the task runner — that caused "stops early"/300s runner timeouts). Creds: Apollo (`X-Api-Key` master key), OpenAI (`openAiApi`), Google SA (`Firebase_SDK_do_or_wait`).
- Prospect list embedded in the **"CA core-fit input"** Code node, built from the **importers tab** of Christine's workbook (`Copy of Industries_ Services_ CUBEWORK.xlsx`, source = S&P/Panjiva). Strategy: **California-first** (best fit for the LA/LB pitch + conserves Apollo credits), multiple industries. Batches run: CA core-fit (Household Durables/Specialty Retail/Apparel/Leisure, TEU 150–10k, ~139) and CA Distributors+Trading (~125). To add a batch, regenerate the embedded array. Apollo coverage thins for small importers (no contact → skipped). Note: some importer rows carry the *ultimate-parent* domain (a holding co/PE firm) → Apollo may return parent-co contacts; use the ✕ Invalid flag to prune.

### n8n workflow 13 — Research & Personalize (`n8n/13_research_prospect.json`) — the email agent
Webhook `research-prospect`, request-driven by the 🔎 button; **must be Active**. OpenAI **`gpt-4o-search-preview`** (`web_search_options:{}`) does live web research INSIDE one call, returns `research_brief` (array) + personalized email, PATCHed onto `outreach/{id}`; the app modal auto-refreshes (`outreachModalDomain` + the listener re-opens it).
- **The system prompt is the IP.** To tune it, paste the **"Build OpenAI request"** node (from `research_prospect.nodes.js`); only re-import on structural change. Current rules: write like a **logistics operator, not a marketer**; do NOT quote their website/products; open with the **strongest POSITIVE/neutral trigger event** (new DC, funding, hiring, expansion — rank strength) tied to a **specific** operational pain (e.g. East Coast hub → volatile West Coast balancing → overflow headache); **anti-fake-personalization** (if no real specific angle, open with one honest direct line, don't force it); **never reference negative/sensitive news**; pitch flexible month-to-month overflow space + **all-in pricing / total cost of occupancy / no NNN surprises**; **never the word "lease"** → say "no long-term commitments" (sets up the license-agreement convo on a call); no em/en dashes (also hard-stripped in code by `dedash` in "Build Firestore patch", and in wf 12's Build prospect doc); no bulleted lists; ~90–120 words; sign "Best, Justin"; two few-shot gold emails included.
- **Prompt tightened 2026-06-25** (after a Joneca draft opened "Saw your recent partnership…" about a **2023** deal, recapped products, and used banned phrases). Changes to the "Build OpenAI request" node: (1) **injects today's date** (`new Date()` → `TODAY'S DATE IS …`) so the model can judge recency — the model has no clock, which is why it called a 2-3yr-old event "recent"; (2) **hard RECENCY RULE** — a trigger only counts if dated within 12 months, else treat as NO TRIGGER and use the honest fallback opener, never imply an unverified date; (3) **NO PRODUCT RECAP** (hard) — never list/quote product names/brands/catalog; (4) explicit **email_subject rules** (3-6 words, sentence/lowercase, no title case/colon/company name/"Needs"/"Solutions"); (5) extended banned list (`such situations`, `designed for`, `ramp up production…`, `can become challenging`, `managing West Coast inventory`, `next stage of expansion`, …). The Joneca-style failures map 1:1 to these additions.
- Cost ~3–5¢/draft. **Current model: `gpt-4o-search-preview`** (live). Switching this node to Claude Sonnet is a **deferred idea** (Sonnet expected to follow the nuanced rules — recency, anti-fake-personalization, no product recap, dashes — better than the search-preview model), **not started**; revisit later. If the model ever errors, the app's 🔎 button now surfaces it (see `researchProspect` `res.ok` handling below).

### New files
- `outreach-seed.js` — cumulative CA prospect seed (loaded before the module, like `pricing-seed.js`).
- `firestore.rules`, `FIRESTORE_SECURITY.md`.
- `outreach/` — `christine-data-analysis.html` (workbook analysis), `ca-core-fit-batch1.csv`, `ca-distributors-trading-batch2.csv`.
- `n8n/12_apollo_ingestion.json` (+ `.README.md`, `apollo_ingestion.nodes.js`), `n8n/13_research_prospect.json` (+ `.README.md`, `research_prospect.nodes.js`).

## 2026-06-25 session — phone reveal (use-it-or-lose-it Apollo credits)

**Decision.** Apollo sub is being cancelled, and current usage is tiny (**578 of 9,030 credits** used this cycle, nearly all Enrichment from the n8n pipeline — credit usage is workspace-level, not per-seat). So credits are use-it-or-lose-it. Reversed the old "email → reply → reveal phone" rule: reveal-on-reply spent credits on the *easy* contacts (if they reply you can just ask for the number). The valuable numbers are the **non-repliers'** (for cold-calling, what the Playbook is built for). New plan: **phone-reveal the whole list now + pull phone alongside email on every future batch**, before the sub lapses. At ~260 prospects × 8 credits ≈ 2,080, we're nowhere near the cap, so no tier rationing.

## 2026-06-29 session — Claude draft engine (MTM dials, confidence, 4 hooks) + final phone-reveal state

### Phone reveal — FINAL LIVE PATH (supersedes any earlier 14/15/16 notes above)
**Live = workflow 16a → Apollo async push → workflow 15.** Polling is dead.
- **`n8n/16a_apollo_phone_reveal.json`** (manual, run in CHUNK batches): lists `outreach`, skips invalid / already-has-phone / already-requested, calls `people/bulk_match` with `reveal_phone_number=true` + `webhook_url` per contact, captures the Apollo **person id** (`matches[0].id`, a clean hex — NOT the int64 request_id), writes **`apollo_phone_map/{personId}={slug}`**, and marks `phone_reveal_requested` on the doc. `CHUNK` const (default 50) keeps each run under the 300s task-runner timeout; re-run until it processes 0. Every per-item Code node iterates `$input.all()` (see gotcha 2).
- **`n8n/15_apollo_phone_callback.json`** (webhook `apollo-phone-callback` → `https://ailinker.item.com/webhook/apollo-phone-callback`; **MUST stay published/Active** or Apollo's pushes are dropped): parses `people[].phone_numbers`, looks up `apollo_phone_map/{id}` → slug, PATCHes `outreach/{slug}` phone fields.
- **`apollo_phone_map/{apolloPersonId}` = `{ slug, created_at }`** — bridge collection between the sync reveal and the async push.
- **`n8n/16b_apollo_phone_write.json` is DEAD — delete it.** Apollo's `webhook_result` poll endpoint 404s for bulk reveals; polling never works. The push (15) is the only path.
- Result so far: ~120 of ~155 revealed landed (rest have no mobile, which is normal).
- **Two hard-won gotchas:** (1) Apollo's `request_id` is a 64-bit int that JS/n8n rounds the moment the response is JSON-parsed — we sidestep it entirely by matching on the **person id** in the push, never the request_id. (2) **n8n Code nodes default to "run once for all items,"** silently collapsing a 50-item batch to 1 — every per-item Code node must loop `$input.all()`.

### Strategy decision — lead with MTM
Cubework's #1 differentiator and the only thing guaranteed to every lead = **month-to-month / flexible space, no long lease.** We are NOT cheaper than a traditional lease (parity or slightly over on total cost of occupancy); sell **agility/freedom + zero upfront CapEx + speed**, never price. Email hooks anchor on the **universal tension** (their needs change, a lease can't) but are always **grounded in a REAL specific + a tension twist** — never bare-generic ("need space?" reads as spam) and never flat-specific ("overflow space for textiles" has no pull).

### wf13 — now Claude + MTM dials + confidence + 4 hooks (`13_research_prospect.json`)
- **Model swap:** Anthropic **`claude-sonnet-4-6`** via `POST https://api.anthropic.com/v1/messages` (was OpenAI `gpt-4o-search-preview`). Uses the **Anthropic API credential** (predefined type `anthropicApi`) + header `anthropic-version: 2023-06-01`, and Anthropic's **web search tool** (`web_search_20250305`, must be enabled in Anthropic Console → Settings → Privacy). Response is parsed from **`content[]` text blocks**, not `choices[0]`. Nodes renamed **"Build Claude request"** and **"Claude research + draft"**.
- **MTM dials (emphasis, not a send gate):** a `GROUP_MAP` in "Build Claude request" derives **group + fit tier** from the prospect's `industry` (11 groups; fit High/Medium/Mixed/Low) and injects a small **category module** into the master prompt. The master prompt (the IP) is left verbatim; all steering rides in the appended module. **Low fit = short honest "keep us in mind" email.** Stored `prospect_group`, `prospect_fit`.
- **Confidence (review signal, NOT a gate):** model returns `confidence` (0-100) + `confidence_reason` (high when a real specific personalization was found, low when only the universal angle). Stored `draft_confidence`, `confidence_reason`. Nothing is auto-send; this just prioritizes manual review.
- **4-hook draft model (final):** output = one **shared body** + `hooks[]` of EXACTLY 4 `{style, subject, opener}`. Styles: **mismatch, forced-bet, freedom, commitment.** Each **subject pairs a REAL specific (category or verified fact) with a tension twist**; the **opener is the first email line, aligned to that subject**; the shared body = a **gated single personalization line** (only if a verified specific fact exists, else skipped — no fake personalization), the MTM all-in value, one low-friction ask, and "Best, Justin". "Build Firestore patch" **assembles `email_variants` = 4 full drafts** (opener + shared body) and sets variant 0 active in `email_subject`/`email_body`.
- **Tightened for length 2026-06-29** (per the Overloop cold-email-length data: ~75 words / under-80 is the reply-rate sweet spot, subjects truncate at ~35 chars on mobile). Edits to the "Build Claude request" node in `13_research_prospect.json`: (1) **body target 90–120 → 60–80 words** (both the master `sys` and the MODULE); (2) **all-in pricing line compressed** to "one all-in monthly rate, no NNN, no long lease" (was the full space/utilities/CAM/taxes/security/maintenance enumeration) — frees the word budget so the cut doesn't eat personalization; (3) added a **readability rule** to the VOICE section (per the same Overloop data: 3rd–5th-grade copy gets ~+36% replies): write at a 5th-grade level, **most sentences under 12 words, one idea each, break long sentences in two, use the simplest word that works (use not utilize, etc.), cut filler**, no jargon, Flesch-Kincaid 60+. **Subject rule: reverted 2026-06-29** — the brief "under 5 words" experiment was too short to read sensibly, so subjects are back to the prior form: **3–6 words pairing a REAL specific (category or verified recent fact, e.g. "home textiles", "west coast importing") with a small tension twist**; lowercase, plain, no bare-generic lines like "need space"; the opener matches that subject angle. The REAL specific is now **REQUIRED on all 4 subjects** (verified fact preferred, category as the guaranteed floor since every prospect has an `industry`), so personalization is never missing from any subject. (The four per-style twists — mismatch/forced-bet/freedom/commitment — still live later in the MODULE.) `research_prospect.nodes.js` was **resynced to the live Claude version** (it had been the stale OpenAI copy). Re-import wf13 to apply.

### `outreach/{id}` new fields
`prospect_group`, `prospect_fit`, `draft_confidence`, `confidence_reason`, `email_variants` (array of `{style,subject,body}`), `body_words` + `body_flesch` (word count + Flesch Reading Ease of the active draft, computed in "Build Firestore patch" 2026-06-29 — a review signal, NOT a gate; aim Flesch ≥60 / ~60–80 words), plus the phone fields written by wf15 (`phone`, `phone_type`, `phone_status`, `phone_all`, `phone_revealed_at`).

### App (`index.html`) — Outreach modal draft variants + review chips
- `openOutreachModal` draft section now shows **variant tabs** (labels via `VARLABEL`: Mismatch / Forced bet / Freedom / Commitment) that swap the **subject + first line together**; **★ Use this version** writes the chosen variant to `email_subject`/`email_body` (and shows **✓ active version**); Open-in-email / Copy act on the shown variant. A **confidence + fit chip** (color by score, `confidence_reason` on hover) + group shows for review, plus a **length/readability chip** (`body_words` + `body_flesch`, added 2026-06-29; green when ~60–80 words AND Flesch ≥60, red when >100 words or Flesch <50, amber otherwise; targets on hover). Backward-compatible: docs with no `email_variants` render as the old single draft. State var `outreachVariant{}`; handlers `setOutreachVariant`/`useOutreachVariant` exposed on `window.*` (ES-module gotcha). `copyOutreachDraft` copies the shown variant.
- **Deploy:** index.html changes require a **git push** (GitHub Pages). n8n workflows go live by **import**, not push.

### Files (this session)
- **New:** `outreach/outreach-categories-mtm.xlsx` (51 Apollo industries → 11 MTM-fit-rated groups, each with its MTM angle/research/fit).
- **Updated:** `n8n/13_research_prospect.json` (Claude + dials + confidence + 4 hooks), `n8n/16a_apollo_phone_reveal.json`, `n8n/15_apollo_phone_callback.json`, `index.html` (outreach modal).
- **Delete:** `n8n/16b_apollo_phone_write.json` (dead poll path).

### Open / next session
- Re-import the updated **wf13**, re-pick the Anthropic credential on "Claude research + draft", redraft one prospect; **push `index.html`**; confirm the 4 hook tabs render in the Outreach modal.
- The n8n **Anthropic credential test can show a false-negative** "couldn't connect" — the real POST works, so save anyway / verify via an actual run (check for 401 = bad key, or a credit/billing message = $0 API balance).
- **Deferred ideas:** log which hook variant is sent vs. who replies (so the system learns which pull wins — the real compounding lever); optional Apify social-scrape enrichment for tiny companies (decided not worth it now).
- **TODO — upgrade wf12's draft to match wf13.** wf12 (`12_apollo_ingestion.json`) still drafts via the OLD **`Score + draft (OpenAI)` / gpt-4o** node — a basic scoring + single draft with NONE of wf13's engine (no Claude, no live web research, no recency/tone/no-product-recap rules, no MTM dials, no confidence, no 4 hooks, no length/subject/readability/`body_flesch` work from 2026-06-29). So the draft a prospect *lands with* at Apollo ingestion is the weak one; the good draft only appears after hitting 🔎 (wf13). Later: port wf13's "Build Claude request" + "Build Firestore patch" logic into wf12's draft step (or have wf12 just call the wf13 webhook per prospect) so every ingested prospect starts with the strong 4-hook draft. Tradeoff to weigh: Claude + web search per prospect at ingestion costs more than the current gpt-4o draft across a full batch.
- **Doc note:** the `firestore-mcp` connector still works after the 2026-06-24 rules lockdown (it authenticates with a service-account credential that bypasses Firestore rules); its README's "Firestore rules are open" line was **stale — fixed 2026-07-21**, the README now explicitly notes the 2026-06-24 lockdown and that the connector's email/password sign-in passes the locked rules.

**Apollo phone mechanics.** `people/bulk_match` reveals **email synchronously** but **phone asynchronously**: set `reveal_phone_number=true` + a mandatory `webhook_url` (query params), and Apollo POSTs the numbers to that webhook minutes later. Phone = **8 credits** each. Webhook payload: `{ people:[ { id, phone_numbers:[ { sanitized_number, raw_number, type_cd, status_cd, position } ] } ] }`.

### Phone reveal — LIVE path is 16a → (Apollo push) → 15 (renumbered 2026-06-26)
**Proven working — 120 numbers landed via workflow 15 on 2026-06-26.** Reveal is a request-now / receive-later pair bridged by the `apollo_phone_map` collection:

- **`n8n/16a_apollo_phone_reveal.json`** — "CW — 16a. Apollo Phone Reveal" (manual). Lists `outreach` (Firestore REST, field-masked) → keeps prospects needing a phone (skips invalid, already-has-phone, and no-email-AND-no-linkedin) → `splitInBatches` 1 → `bulk_match` with `reveal_phone_number=true`&`webhook_url` (8 cr each) → extracts the Apollo person id → writes the `apollo_phone_map/{personId}={slug}` bridge and marks the outreach doc requested. It does NOT receive numbers — Apollo pushes those to 15.
- **`n8n/15_apollo_phone_callback.json`** — "CW — 15. Apollo Phone Callback" — **must stay ACTIVE/published**. Webhook `apollo-phone-callback` (path → `https://ailinker.item.com/webhook/apollo-phone-callback`, **server-to-server from Apollo, NOT via the Cloudflare CORS worker**). Parses `people[].phone_numbers` (tolerant of wrapping), looks up `apollo_phone_map/{id}` → slug, PATCHes `outreach/{slug}` phone fields (updateMask, so nothing else is touched). Shared by 16a and 12. **Requires HTTPS + public reachability + active workflow** (Apollo's webhook rules). Idempotent (Apollo may retry).

**DEAD — `16b_apollo_phone_write.json` (poll-based) does not work and is being removed.** Polling Apollo's `webhook_result` endpoint returns **404 for bulk reveals** — there is no pull path; the async push to 15 is the only way numbers land. Do not document or revive 16b as an alternative.

**Two hard-won gotchas (do not repeat):**
1. **Apollo's `request_id` is a 64-bit int** — `JSON.parse` rounds it and you lose the correlation. We sidestep it entirely by matching on the **person id** in the push, never the request id.
2. **n8n Code nodes default to "run once for all items,"** which silently collapses a 50-item batch down to 1. Set them to run per-item where the batch must fan out.

**`apollo_phone_map/{apolloPersonId}` = `{ slug, email, mapped_at }`** — the bridge between the sync reveal and the async push: 16a saves id→slug at request time; 15 looks it up by the same person id when the push arrives.

**`outreach/{id}` phone fields** (written by 15): `phone` (best number, sanitized — prefers mobile, then valid, then position 0), `phone_type`, `phone_status`, `phone_all` (all numbers comma-joined), `phone_revealed_at`. The Outreach tab's existing `phone` column fills in automatically.

### Workflow 12 change — phone now ON for future batches
`Pick best contacts` enrichBody dropped the inline reveal flags (now `{details}` only); the reveal node sends `reveal_personal_emails=true`&`reveal_phone_number=true`&`webhook_url` as **query params**; a new **Save phone map** node (after Create prospect) writes `apollo_phone_map/{personId}={slug}` so workflow 15 attaches the async phone. New per-contact cost ≈ **9 credits** (1 email + 8 phone). The reveal node's display name still reads "(1 credit)" — kept to avoid breaking the `$('Apollo reveal email (1 credit)')` references in downstream Code nodes; it now costs ~9.

### n8n workflow 14 — Apollo Org Discovery (`n8n/14_apollo_org_discovery.json`)
Slot 14 is now **Org Discovery**, not phone backfill. "CW — 14. Apollo Org Discovery (reusable: last-mile / fashion-district)" — manual, reusable org-search head for verticals that can't be sourced from Christine's importer workbook (e.g. the LA Korean Fashion District's ~1,000 small private jobbers). Apollo `mixed_companies/search` by city/keyword/headcount → post-filters to district ZIPs in code (Apollo has no zip/radius param) → outputs a paste-ready company array to drop into workflow 12. **Reads orgs, writes nothing on its own.** Flags likely-Korean-owned orgs (`korean_likely`) by romanized-surname hints rather than dropping English-brand jobbers. Config node toggles `VERTICAL` (`lastmile` / `fashion_district`) and `pages`. Companions: `n8n/14_apollo_org_discovery.nodes.js`, `n8n/apollo_org_discovery.reusable.nodes.js`, `n8n/socal-last-mile.discovery-config.js`, seed `outreach/ca-korean-fashion-district-batch3.csv`. See `n8n/14_apollo_org_discovery.README.md`.

### New / changed files (2026-06-25 → 2026-06-26)
- `n8n/16a_apollo_phone_reveal.json` (live), `n8n/15_apollo_phone_callback.json` (live, keep active), `n8n/phone_reveal.nodes.js`.
- `n8n/14_apollo_org_discovery.json` (+ `.README.md`, `.nodes.js`), `n8n/apollo_org_discovery.reusable.nodes.js`, `n8n/socal-last-mile.discovery-config.js`.
- Updated: `n8n/12_apollo_ingestion.json` (phone reveal on).
- **To delete (stale/dead):** `n8n/16b_apollo_phone_write.json` (polling 404s), `n8n/14_15_phone_reveal.README.md` (points at the never-created `14_apollo_phone_backfill.json`).

---

## 2026-07-03 → 2026-07-06 session — Space Matches, note_emls → Notes pipeline, Firestore MCP connector confirmed live

### Space Matches (Leads tab) — proactively reconnect dead leads with new availability
**Pain point:** leads that go quiet get archived (or waitlisted, or marked Lost) and drop off Justin's radar — but plenty of them wanted a real space before they fizzled. The existing Waitlist block (`wlMatches`) only checks for a match when you happen to open that lead's card, and only if `wl_on` + `wl_locations` were explicitly set. Space Matches is the proactive version: **any lead in the "dead pool" is watched automatically, every render, with zero setup required.**

- **`isDeadPoolLead(lead)`** = `lead.archived || lead.wl_on || lead.stage === 'lost'`.
- **`spaceMatches(lead)`** — generalized matcher. If `wl_locations`/`wl_type`/`wl_sf_min`/`wl_sf_max` are explicitly set, behaves identically to `wlMatches`. Otherwise **falls back** to whatever's already on the lead (`unit_sf`/`sqft` via the existing `followupTargetSf` heuristic — a 0.5×–2× size window) searched across **all synced locations**, so an archived lead needs no extra configuration to be watched. Guards against matching everything when there's nothing concrete to match on (mirrors the existing `followupAvailMatches` safety check).
- **`spaceMatchMsgFor(lead)`** — basic re-engagement draft (per Justin: "a basic one," not the Outreach tab's 4-hook engine) referencing the actual matched property/unit/sf, plus the evergreen all-inclusive/month-to-month value props. **`renderSpaceMatchBlock(lead)`** renders it (email-preview style, green) with **✉️ Open in email** / **📋 Copy** / **Snooze 14 days** (`openSpaceMatchEmail`/`copySpaceMatchMsg`/`snoozeSpaceMatch`, writes `dm_snooze_until`). Shown on a lead's card whenever `isDeadPoolLead(lead)` is true and `wl_on` is false (a `wl_on` lead keeps showing the existing, more detailed `renderWaitlistBlock` instead — no duplication).
- **Surface:** `renderLeadsSection()` computes `deadPool`/`spaceMatchLeads` every render and drives three UI pieces — a green dashed banner (`#space-match-banner`, same pattern as the amber Follow-ups banner) showing "N past leads now have matching space available" with a **Review matches →** button, a toggle (`#space-match-toggle` / `toggleSpaceMatches` / `showSpaceMatches`) that expands `#space-match-list` (matched leads rendered via the normal `renderLeadCard`), and the per-card block above.
- **Known gap (flagged 2026-07-06, not yet fixed):** the Space Match block has no "↻ Check" recheck button (unlike `renderWaitlistBlock`) and doesn't show the matched location's last-synced timestamp — so a match could be checked against stale `availability/{propId}` data if that location hasn't synced recently. See "Open / next session" below — this is what motivates the daily-sync-all-locations item.
- All new handlers exposed on `window.*` (ES-module gotcha, same as every other inline `onclick`).
- **Deployed** — required a manual `git push` from Justin's machine (the Cowork sandbox has no GitHub credentials, can't push itself). Hit an unrelated GitHub-side hiccup along the way: the `pages-build-deployment` Actions run got stuck `Queued` and wouldn't cancel ("You can't perform that action at this time" — a known quirk with these auto-managed Pages runs, not a repo config issue; confirmed via a resolved GitHub Pages incident on 2026-07-02 on githubstatus.com, though the stuck run itself lingered a bit after the incident resolved). Fixed by pushing a new commit (`git commit --allow-empty` + push), which superseded the stuck run rather than fighting the cancel button. No config changes needed — if this recurs, that's the fix, not a Settings→Pages investigation.

### `note_emls/` — email → Notes tab extraction (established this session, no code needed)
Justin drops a `.eml` file (an exported/forwarded email thread, e.g. a customer complaint chain) into `note_emls/`. On request ("process the notes folder"), Claude parses it (Python `email` package, `BytesParser` with `policy.default`, pulling the plain-text body via `get_body(preferencelist=('plain','html'))`), reads the thread for context, and writes a note matching the 💡 Notes schema: `trigger` (what happened), `why` (why it matters), `fix` (the actual fix). The source `.eml` moves to `note_emls/processed/` so it isn't reprocessed.

**Delivery mechanism — settled on the live Firestore MCP connector, not a file export.** The first pass (before the connector was confirmed available in a Cowork session) wrote an `extracted_notes.json` review file instead. Once the **"Do or Wait — Firestore" MCP connector** (see below) was confirmed live, that became unnecessary — Claude now calls its `create_note` tool directly and the note appears in the app immediately, no manual copy-paste. **Known schema gap:** the connector's `create_note`/`update_note` tools only take `why`/`fix` params — they predate the `trigger` field (added to the app 2026-07-02) and have NOT been updated to expose it. Current workaround: `trigger` text is prefixed into the `why` field ("Triggered by: … \n\n Why it matters: …") so nothing is lost, but it won't populate the dedicated Trigger box in the UI. Justin's call (2026-07-06): **hold off fixing the connector for now** — revisit if this becomes annoying in practice.

### "Do or Wait — Firestore" MCP connector — confirmed live in Cowork, tool inventory
The connector described in the File locations section (`firestore-mcp/` source) showed up mid-session as live MCP tools in this Cowork chat (`mcp__Do_or_Wait___Firestore__*`) — worth noting since it isn't always attached/enabled and its availability isn't guaranteed session-to-session. Confirmed tools: `create_note`(why,fix) · `update_note`(id,why?,fix?) · `list_notes` · `create_task` · `update_task` · `get_task` · `list_tasks` · `add_task_update` · `create_lead` · `update_lead` · `get_lead` · `list_leads` · `add_lead_update` · `get_availability` · `get_pricing` · `get_renewals` · `list_locations` · `trigger_availability_sync` · `sync_all_locations` · `get_document`(collection,id — read-only escape hatch) · `query_collection`(collection,limit — read-only escape hatch, no write equivalent). Note `sync_all_locations` already exists as a connector tool — worth checking before building the daily-sync n8n workflow below, in case it already does most of what's needed (it wasn't invoked or inspected this session, just discovered in the tool list).

### 2026-07-06 (cont.) — Daily n8n Cron sync built, follow-up-generation button scoped

**Daily sync of ALL locations — built into `5_add_location.json`, NOT YET IMPORTED.** Inspected the Firestore MCP connector's `sync_all_locations` first: its `firestore-mcp/server.js` handler already loops `config/properties` and POSTs the `availability-sync` webhook per location inside a `try/catch` (per-location error isolation already existed there), but it's manual-trigger only — nothing calls it automatically. Justin chose the **n8n Cron** route over a Cowork scheduled task (the scheduled-task route would have depended on the Firestore MCP connector being attached at run time, which isn't guaranteed session-to-session). Added 5 new nodes to `5_add_location.json`, wired as a fully separate branch that does NOT touch the existing Webhook/Webhook1 chains (zero risk to current behavior — it re-triggers the same public `availability-sync` webhook per location rather than forking the internal "Find property" logic):
- **`Daily Sync - Cron`** (`scheduleTrigger`, `0 3 * * *` — 3am, ahead of Pricing's 6am / Renewals' 7:30am).
- **`Get all properties (cron)`** — same `config/properties` GET as the existing "Read current config" node.
- **`Build propId list`** — Code node, extracts every `id` from `properties[]` and emits one item per location.
- **`Batch locations (loop)`** — `splitInBatches`, batchSize 1 (same v3 loop-off-index-1 convention as workflow 6: "done"/index 0 left empty, "loop"/index 1 feeds the next node).
- **`Trigger location sync`** — HTTP POST to `https://ailinker.item.com/webhook/availability-sync` with `{propId}`, `onError: continueRegularOutput` so one bad SharePoint link logs an error on that item and the loop continues to the next location instead of halting the run, then loops back into `Batch locations (loop)` to advance.
- Also added a third fallback line to the existing `Find property` Code node (`$('Batch locations (loop)')`) — was pre-wired in case we ever route the loop through the internal chain directly instead of the public webhook; currently unused by the live design below but harmless.

**Deliberately calls the public webhook per location instead of wiring into the internal "Find property" chain directly.** Considered hooking the batch loop straight into the shared `Find property → Get file metadata1 → ... → Write to Firestore1` chain (Justin asked about this directly). Rejected because that chain is shared with the manual ⟳ sync and add-location flows — getting real per-location error isolation there would require adding `continueOnFail`/`onError` to those shared Graph/Firestore nodes, which would also silently swallow errors on every normal manual sync (today those fail loudly, which is correct). Calling the public webhook per location instead gives isolation for free: each location becomes its own independent n8n execution, so one failure can't cascade into the next, with zero change to how manual syncs behave.

**Live behavior confirmed 2026-07-06 — the webhook responds BEFORE the sync finishes.** n8n's webhook (no "Respond to Webhook" node configured) replies immediately with `{"message":"Workflow was started"}` rather than waiting for the triggered execution to complete. This caused real confusion during testing: looking at the "Daily Sync - Cron" execution's own trace, it appeared nothing happened past `Trigger location sync` — because the actual sync work (Find property → Graph calls → Parse + build patch → Write to Firestore1) runs in **22 separate, async "Webhook"-triggered executions** that don't show up inside the Cron execution at all. Confirmed working: after running the Cron node manually, all 22 locations' `synced_at` updated. **Known side effect:** because the webhook doesn't block, the batch loop doesn't actually wait for each location to finish before firing the next — all ~22 syncs fire in a tight burst rather than one at a time as originally intended (batchSize 1 paces the *triggering*, not the actual sync work). This produces a burst of concurrent Graph API calls once a day at 3am rather than a gentle sequential trickle. Not currently a known problem (Graph handled the test burst fine), but worth revisiting if Graph throttling ever shows up in the 3am runs — the fix would be adding a short `Wait` node between batch iterations, or switching `Trigger location sync`'s webhook to synchronous response mode.
- **Timezone — FIXED.** The Schedule Trigger had been inheriting n8n's default timezone (a manual test showed Asia/Shanghai, UTC+8 — `0 3 * * *` would've fired ~11am-noon Pacific, mid-business-day). Justin set this workflow's Settings (⋯ menu → Settings → Timezone) to `America/Los_Angeles` and saved, so the 3am trigger now actually means 3am Pacific.
- **Import created a duplicate workflow, not an in-place update.** Importing the JSON via the workflows list created a second "CW — 5. Add Location" entity rather than updating the original in place, and both tried to claim the same `availability-sync`/`add-location` webhook paths, blocking activation ("Conflicting Webhook Path"). Resolved by deactivating and deleting the old original and activating the freshly-imported one. **Takeaway for next time:** to update this workflow, either edit directly in the n8n UI, or open the existing workflow and use its own Import-from-File (which replaces canvas contents in place) rather than importing from the workflows list (which always creates a new entity).
- **Note:** building this makes the app's `sync_all_locations` MCP tool now somewhat redundant for the *daily* use case (it still holds value as an on-demand "sync everything right now" action) — no changes made to that tool.

**Last-synced timestamp on the Space Match card — DONE.** `spaceMatches()` now carries `propId`/`synced_at` on each match; `renderSpaceMatchBlock` shows it via the existing `availExpSyncLabel()` helper (same "Synced: Jul 6, 3:00 AM (2h ago)" format already used in the Avail-export modal). Needs a `git push` from Justin's machine to deploy.

**Second feature scoped: manual Follow-up-generation button.** Justin's ask: a manual **"Generate follow-up"** button (separate from the existing automatic 3-day-cold follow-up block described earlier in this doc, which he flagged as needing rework later — left untouched). Read the lead's linked email thread if present, else fall back to logged notes/updates; call Claude for a value-add draft, never a bare "just checking in." See the next session below for the build.

---

## 2026-07-06 (cont. #2) — Leads communication-tracking discussion (tabled), Follow-up Generator built

### Do/Wait rethink for Leads — discussed, put on the backburner
Justin raised two things: (1) the email-thread new-mail check (🆕 flag) is purely visual today — it doesn't feed `last_contact_date`/`last_contact_dir`, so it's disconnected from the actual follow-up clock; and (2) the Do/Wait split for Leads doesn't feel useful anymore — he relies on the thread check and notes instead. Talked through a fuller idea: replace Do/Wait with an explicit **next-action + date per lead** (every update carries a planned next step and a real follow-up date, not a generic clock), surfaced as a single list banded by urgency (Overdue/needs a next step, Due today, Upcoming, Later) rather than two columns. Landed on: since Justin never logs inbound manually (he replies, then logs the outbound), the automated thread-check would have to be what flags "needs a next step," not a manual inbound log — the two features are really one. **Justin's call: interesting, but not sure it's worth it — tabled for now, revisit later if it keeps coming up.** No code changed for this part.

### Follow-up Generator — built (manual "✨ Generate follow-up" button)
Scoped and built per Justin's answers: (1) thread context uses the last ~5 messages of real back-and-forth, not just the newest one; (2) if no linked thread exists, falls back to the lead's own logged notes/updates (last 5 entries); if **neither** exists, the button doesn't call the webhook at all — it alerts "nothing to base a follow-up on yet" (client-side gate, `leadNotesContext`/`leadThreadConversationId` in `index.html`); (3) the draft saves to the lead and renders as a card (same visual pattern as the other email-preview blocks), not a one-off popup. **Tone was called out as the most important part** — the prompt is written to sound like a real continuation of an actual conversation, not a template, reusing the same voice discipline as wf13 (short sentences, 5th-grade readability, no dash characters, a long banned-phrase list including "just checking in"/"circling back", no invented details, sign off "Best,\nJustin Cho\nBusiness Development, Cubework").

**App side (`index.html`):**
- `leadNotesContext(lead)` — last 5 dated thread entries (kind + text), joined as fallback context.
- `leadThreadConversationId(lead)` — first resolved `email_links[]` entry with a `conversationId`.
- `generateFollowup(leadId, e)` — client gate (alerts and stops if neither a thread nor notes exist), POSTs to `FOLLOWUP_GEN_WEBHOOK`, same `res.ok` error handling as `researchProspect` (404 = workflow not imported/active, other = Claude/model error — surfaced via `alert`, never a silently stuck button).
- `renderFollowupDraftBlock(lead)` — renders `lead.followup_draft` (purple, to visually distinguish from the amber automatic follow-up block and the blue sequencer preview) with Open in email / Copy / Dismiss / Regenerate. Shown in place of the automatic follow-up block whenever a generated draft exists (`lead.followup_draft ? renderFollowupDraftBlock(lead) : (due ? renderFollowupBlock(lead) : '')`).
- New **✨ Generate follow-up** / **✨ Regenerate follow-up** button in the card's thread-foot, next to ✏️ Details and ＋ Update.
- All new handlers exposed on `window.*` (ES-module gotcha).

**n8n (new, NOT YET IMPORTED): `n8n/17_lead_followup_generator.json`** (+ `lead_followup_generator.nodes.js`). Webhook `generate-followup`, `responseMode: responseNode` (synchronous, like wf11/wf13 — not the fire-and-forget default that tripped up the Cron work above). Flow: `Has thread?` (IF node on `conversationId` present) → true branch **`Get thread messages`** (Graph `GET /me/messages`, `$filter=conversationId eq '...'` + `$orderby` + `$top=5`, same MS creds as wf11; `continueOnFail` so a Graph hiccup falls through) → **`Format thread context`** (strips HTML, keeps last 5 messages oldest-to-newest, falls back to the app-sent `notesText` if Graph errored or returned nothing) — false branch **`Format notes context`** (just passes through `notesText`) — both converge into **`Build Claude request`** (the voice/tone prompt, `claude-sonnet-4-6`, no web search needed since this isn't cold-prospecting research) → **`Generate follow-up (Claude)`** (same `anthropicApi` credential pattern as wf13 — Justin will need to re-pick the credential on import, same as he did for wf13) → **`Build Firestore patch`** → **`Write to Firestore`** (PATCH `leads/{id}.followup_draft`) → **`Respond`** (`{ok:true,leadId}`).
- **New workflow, brand-new webhook path** — deliberately NOT touching any existing active workflow, so this import carries none of the "Conflicting Webhook Path" risk from the daily-sync saga above. Straightforward "Import from File" as a new workflow is fine here.
- **Assumption flagged, unverified:** the Cloudflare Worker (`plain-credit-5962.jchoustin91.workers.dev`) is assumed to proxy any `/webhook/<path>` generically through to the same-named n8n path (true for every existing route — availability-sync, add-location, resolve-thread, research-prospect all work through it with no per-route Worker config mentioned anywhere in this doc). If `generate-followup` 404s specifically at the Worker level (not at n8n), the Worker needs an explicit route added — check this first if the button gets a 404 that n8n's own execution log doesn't show at all.
- **To do next session:** import into n8n, pick the Anthropic + Microsoft + Google credentials on the three respective nodes (they reuse the exact same credential IDs as wf11/wf13/wf5 elsewhere in the instance, so this should just be a dropdown pick, not new setup), activate, then test end to end on one lead with a linked thread and one with notes only.
- **Deployment note:** `index.html` changes for this feature (the button, the draft block) only take effect after a `git push` from Justin's machine — confirmed 2026-07-06 the button wasn't showing on the live site until this is done. n8n workflows go live by import, `index.html` goes live by push; neither happens from the sandbox.

### Refinements (same session, before first import)
- **Context is now combined, not either/or.** Justin's ask: when a lead has BOTH a linked email thread AND logged notes/updates, feed Claude both, not just whichever one wins. `Format thread context` no longer falls back to `notesText` only when the thread is empty — it now concatenates: `EMAIL THREAD (...)` block + `ADDITIONAL LOGGED NOTES/CALLS (...)` block, when both exist. New `contextSource` value `thread+notes` added (`Build Claude request`'s context label and `Build Firestore patch`'s stored `source` field both handle the three-way now: `thread` / `notes` / `thread+notes`). No app change needed here — `generateFollowup` already sent both `conversationId` and `notesText` on every call; the workflow just wasn't combining them yet.
- **Button visibility narrowed.** The ✨ Generate follow-up button now only renders when `leadThreadConversationId(lead) || leadNotesContext(lead)` is truthy — i.e. only on leads that actually have a resolved thread or at least one logged update. A brand-new lead with zero history no longer shows a button that would just alert-fail on click. **Open question for Justin:** this only gates on "is there anything to draw from," not "is a follow-up actually due" — a lead touched yesterday will still show the button even though it's nowhere near due. Flag if you'd rather it also respect `isFollowupDue`-style timing, not just content availability.

### First import + first real bug (2026-07-06)
Justin imported `17_lead_followup_generator.json` and test-ran it on a real lead with a linked thread. `Get thread messages` (the Graph call) failed with **HTTP 400 `InefficientFilter`: "The restriction or sort order is too complex for this operation."** — a known Graph quirk: `/me/messages` rejects combining `$filter=conversationId eq '...'` with `$orderby=receivedDateTime desc` in the same request. **Fixed:** dropped the `$orderby` query parameter entirely — it was redundant anyway, since `Format thread context` already re-sorts the fetched messages by date itself before building the context block. Justin instead chose to re-import the updated file into the existing workflow (⋯ → Import from File **from within the workflow itself**, not the workflows list — same in-place-update pattern learned from the wf5 duplicate-workflow saga, so this carried no webhook-conflict risk).

**Notes-attribution rule added (same re-import).** Justin's correction: anything in a lead's logged notes/updates is HIS OWN reminder to himself, not something the lead said, unless the text explicitly frames it that way ("they said", "the lead asked"). Without this, Claude could misread a self-reminder like "Let's follow up on this on Friday" as a request FROM the lead. Added an "IMPORTANT ABOUT NOTES" paragraph to the `Build Claude request` system prompt (in both `17_lead_followup_generator.json` and `lead_followup_generator.nodes.js`) making this explicit — only the actual email thread (when present) is treated as the lead's own words; logged notes/calls are Justin's perspective only.

### Prompt refinements — availability honesty, softened stage guidance, no fabricated next-step (2026-07-07)
Three more edits to the same `Build Claude request` system prompt (applied to both `17_lead_followup_generator.json` and `lead_followup_generator.nodes.js`, inserted after "IMPORTANT ABOUT NOTES" and before "Write ONE follow-up email that:"), from reviewing the prompt together:

- **AVAILABILITY AND PRICING HONESTY.** Any unit/price/sf mentioned in the lead's details or notes reflects what was true when it was recorded, not necessarily now — the model must not state or imply current availability or a still-valid price unless the given context itself confirms it. If referencing a unit/price from history, frame it as something already discussed ("the unit we talked about," "the pricing we went over"), never as a current fact. Justin's reasoning: he doesn't want a follow-up implying an available unit is still open, or a quoted rate is still on the table, when it might not be. A future idea (not built) is a testimonials/case-study library the model could pull from instead — deferred.
- **STAGE GUIDANCE — deliberately a tie-breaker, not a driver.** First draft made `lead.stage` a primary input to tone (early stage = light, late stage = direct). Justin pushed back: he's manually setting every lead's stage while bootstrapping this system, and can't guarantee it's accurate 100% of the time — a prompt that leans hard on stage would produce wrong-toned emails whenever stage drifts from reality. Revised so stage is only a loose hint, and the REAL context (thread content, notes) always overrides it when they disagree — e.g. stage says "cold" but notes describe a tour, treat the lead as further along than the stage says. This is a broader principle Justin is applying while semi-automating: observed, real signals outrank manually-set metadata whenever they conflict.
- **No manufactured next step.** Old wording ("Adds ONE genuinely useful new thing... Never a bare check-in with nothing new to say") pressured the model into inventing a next step even with nothing real to add. Changed to explicitly allow skipping that step when there's genuinely nothing new — never invent a next step/update not in the context; if there's truly nothing new, keep the email a short, honest, specific check-in instead of a bare generic one.

**Not yet applied to n8n** — these are file-only edits so far. To deploy: open the existing **"CW — 17. Generate Lead Follow-up"** workflow in n8n (do NOT import from the workflows list — that creates a duplicate and risks the same "Conflicting Webhook Path" issue hit with workflow 5) and use its own **⋯ → Import from File**, pointing at the updated `17_lead_followup_generator.json`. This replaces the canvas in place, preserving the workflow ID and existing credential bindings (Anthropic/Microsoft/Google), so no credentials need to be re-picked.

**Still open:** whether the ✨ Generate follow-up button should also gate on `isFollowupDue`-style timing (not just "is there content to draw from") — flagged, not yet answered.

---

## 2026-07-07 (cont.) — Restricted role: Lucy, Avail-tab-only, read-only

Justin wants coworker Lucy (`lucy.wang@cubework.com`) to use the app but see only the 🏭 Avail tab, and not be able to edit anything there (confirmed: read-only view, including the renewal badges on occupied units).

**Two-layer implementation — UI gate in `index.html` + real enforcement in `firestore.rules`.** A UI-only restriction would be cosmetic: her signed-in token would still pass the old single-allowlist rule and could read/write `leads`/`topics`/`notes`/`outreach` directly via the browser console. So this needed both:

- **`index.html`**: new `AVAIL_ONLY_EMAILS` list (separate from `ALLOWED_EMAILS`) + `AVAIL_READONLY` flag, set in `onAuthStateChanged` when a matching email signs in. `lockToAvailReadOnly()` hides every `.tab-btn` except Avail and the "＋ Add Location" button, then forces `switchTab('avail')`. Every Avail write control (⬇ Export JSON, ⟳ Sync now / per-location ⟳, ✏️ Ownership badge click, PA edit/add, Remove location, ＋ Add Location) is conditionally omitted from its template string when `AVAIL_READONLY` is true, and every underlying write function (`openAvailExport`, `doAvailExport`, `triggerAvailSync`, `removeLocation`, `openAddLocation`, `submitAddLocation`, `openEditPA`, `savePA`, `openEditOwnership`, `saveOwnership`) has a defensive `if (AVAIL_READONLY) return;` guard as a second layer.
- **`firestore.rules`**: added `isAvailOnly()` (checks `AVAIL_ONLY_EMAILS`, kept in sync with the app-side list) and `isAnyAllowed()`. `availability/{propId}`, `config/properties`, and `renewals/current` now have explicit `match` blocks: read allowed for `isAnyAllowed()`, write only for `isAllowed()` (the original 3-email full-access list). Everything else falls through to the catch-all `{document=**}` block, which stays `isAllowed()`-only — so Lucy's token gets zero access (read or write) to `topics`, `leads`, `notes`, `outreach`, `pricing/current`, `apollo_phone_map`, etc., no matter what the UI shows.

**Manual steps still needed from Justin (not doable from the sandbox):**
1. Firebase console → Authentication → Users → add `lucy.wang@cubework.com` with a password, same as the existing 3 accounts.
2. Firebase console → Firestore Database → Rules → paste the updated `firestore.rules` → Publish.
3. `git push` the updated `index.html` (GitHub Pages deploy).

Both allowlists (`AVAIL_ONLY_EMAILS` in `index.html`, the array inside `isAvailOnly()` in `firestore.rules`) must be kept in sync by hand if her email ever changes — same pattern as the existing `ALLOWED_EMAILS` / `isAllowed()` pair.

---

## 2026-07-07 (cont. #2) — 📊 Rate Bands tab (internal Sales/BD pricing bands)

New internal-only tab reading a second pricing sheet Justin surfaced this session: `2026_cubework_pricing_by_state__Sales__BD_Copy.xlsx` (Sales_US site, "Pricing Hub Files BD  Sales Team" folder — a different document library than the public price sheet). Kept **fully separate** from `pricing/current`/workflow 7 per Justin's call ("keep the old one too — it also has some information that the other one doesn't have and vice versa"): the two sheets are complementary, not overlapping (old sheet has amenities/special-pricing notes/annual pricing that the new one lacks; new sheet has bands/occupancy/break-even/promos/UNIS directory that the old one lacks).

**Discovery process.** A first flattened text pull (via the SharePoint MCP connector's `read_resource`) scrambled which numbers belonged to which building — the same problem the old floorplan importer hit with plain text/CSV extraction, because the sheet is a visual per-building "card" dashboard, not flat rows. Rather than guess, got a real `usedRange(valuesOnly=true)` grid pull for one tab (AZ) directly from Justin via a throwaway n8n HTTP Request node, and built/verified the parser against that real fixture (saved at `n8n/pricing_bd_raw_grid.json`) before writing anything else. Also discovered along the way: the workbook is **tab-per-state** (confirmed by Justin after an initial wrong guess that it was one big sheet), plus a "UNIS" tab and an "Overview & Instructions" cover tab.

**Auth gotcha hit and fixed.** The first attempt at the throwaway HTTP node used `authentication: predefinedCredentialType` / `nodeCredentialType: microsoftOAuth2Api` (the pattern written into workflow 7, which was never actually verified live — it still has a `REPLACE_WITH_CREDENTIAL_ID` placeholder). That failed with **"Unable to sign without access token."** The pattern that's actually proven working in this n8n instance (used successfully by workflows 11 and 17) is `authentication: genericCredentialType` / `genericAuthType: oAuth2Api` / credential **"n8n-cubework"**. Workflow 18 (below) uses that proven pattern throughout, not workflow 7's untested one — worth revisiting workflow 7's auth if it's ever actually activated.

**n8n workflow 18 — `18_pricing_bd_sync.json`** (+ `pricing_bd_sync.nodes.js`, NEW, not yet imported). Two triggers — `Daily 6:30am` cron (between Pricing's 6am and Renewals' 7:30am; **set the workflow's Settings → Timezone → America/Los_Angeles on import**, same lesson learned from the daily-sync work above) and a manual webhook `pricing-bd-sync` (Justin's pick: cron baseline **plus** an on-demand button, not cron-only like Pricing) — both feed a shared chain: `Reset accumulator` (clears a `$getWorkflowStaticData('global').rows` accumulator so a fresh run never inherits a prior errored run's leftovers) → `List worksheets` → `Filter state tabs` (drops the Overview/Instructions cover tab by name pattern, fans out one item per remaining tab) → `Batch tabs (loop)` (splitInBatches, batchSize 1) → per tab: `Read tab grid` (`usedRange(valuesOnly=true)`, `continueOnFail` so one bad tab doesn't kill the run) → `Parse tab` (appends parsed building-card records to the accumulator, loops back) → once the loop's "done" output fires: `Build Firestore patch` (snapshots + clears the accumulator, throws if 0 rows) → `Write pricing_bd to Firestore` (PATCH `pricing_bd/current`) → `Respond`.

**Parser (`Parse tab` node) — verified against the real AZ-tab grid, not guessed.** Each building is a numbered card: `[indexNum, "Name — Address [optional tag]", ...]` starts it (column 0 being a number is the card-boundary signal), then a `Building SF / Lease / Occupancy` row, then zero or more single-cell free-text rows (BELOW BREAK-EVEN / PROMO / other notes — classified by regex on content, since order varies card to card), then the `Band/Tier/Current/Recommended/Floor–Ceiling/Action` tier table (1-6 rows, stops naturally at the next marker rather than assuming 6), then an `ADD-ONS / ANCILLARY PRICING` marker followed by label/value pairs (3 per row across 6 cells — generic column-pair extraction with a keymap for known labels and a slugified fallback for unknown ones, so a label that varies by state still gets captured), then Conf Room policy and approval notes, then a blank spacer. The UNIS tab additionally has bare `— CA —` style state-divider rows between groups (its cards don't otherwise carry a state) — handled the same way. Ran the actual node code against the real AZ fixture (`node --check` + an execution harness mocking `$json`/`$input`/`$()`/`$getWorkflowStaticData`) before shipping — all 3 real cards (Glendale, Tolleson, Tucson, including Tucson's partially-blank tier table and missing office/dock add-ons) parsed correctly, and the Firestore field-value encoding round-tripped clean.

**App side (`index.html`) — new 📊 Rate Bands tab**, positioned right after 💲 Pricing in the tab bar. `PRICING_BD_SYNC_WEBHOOK` constant (→ `webhook/pricing-bd-sync`). New view (`#ratebands-view`) with search box, state filter, a Cubework/UNIS/All scope dropdown (`rateBandsScope`, default `cubework` — UNIS sites need Christine Wei confirmation before quoting, so they're opt-in to view, not default-shown), and a manual **⟳ Sync now** button (`syncRateBandsNow`, same `res.ok` error-surfacing pattern as `researchProspect`/`generateFollowup` — a 404 means workflow 18 isn't imported/active, any other failure points at n8n's Executions log). `renderRateBands()` groups by state; each building is a collapsible card (`renderBandCard`/`toggleBandCard`) showing name/address, an Inactive/Pending/UNIS-Ref tag badge, a below-break-even ⚠ flag and promo 🏷 flag (both with the full text on hover), and an occupancy % chip (green ≥90%, red <70%). Expanded: Building SF/Lease, promo/break-even banners in full, the tier band table (SF range/Tier/Current/Recommended/Floor–Ceiling/Action — Action colored RAISE=green/HOLD=neutral/LOWER=red), add-ons list, Conf Room policy, and any other free-text notes. Reads `pricing_bd/current` live via `liveSnapshot` — **no snapshot fallback** (internal-only, matches the Rate Bands collection's own no-seed design). Automatically inherits the existing `AVAIL_READONLY` lockdown (Lucy) for free — `lockToAvailReadOnly()` already hides every `.tab-btn` except Avail, and this is just another `.tab-btn`, so no Rate-Bands-specific gating code was needed.

**To deploy:** import `18_pricing_bd_sync.json` into n8n as a **new** workflow (brand-new webhook path, no conflict risk like the workflow-5 duplicate saga), pick the Microsoft (`n8n-cubework`) and Google (`Firebase_SDK_do_or_wait`) credentials on the three respective nodes, set Settings → Timezone → America/Los_Angeles, activate, then test-run once (manually or via the app's ⟳ Sync now) and check that all ~14+ state tabs plus UNIS parsed with non-zero row counts. `git push` the updated `index.html` for the new tab to go live (same as every other app-side change — nothing here happens from the sandbox).

**New files:** `n8n/18_pricing_bd_sync.json`, `n8n/pricing_bd_sync.nodes.js`, `n8n/pricing_bd_raw_grid.json` (the real AZ-tab fixture the parser was built and verified against — dev reference only, not read at runtime).

---

## 2026-07-07 (cont. #3) — 🗂️ Roster tab (staff contact directory)

Third new tab this session, from a third SharePoint file Justin surfaced: `Cubework_Locations_Master_Roster_2026.xlsx` (Sales_US site, same "Pricing Hub Files BD  Sales Team" folder as the Rate Bands source, different file). Scope was narrowed quickly: Justin only wants **PA (local staff) and facility manager per location** — not the Sales-team codes or the Overview tab's legend, even though the sheet has that data too.

**Much simpler sheet than Rate Bands.** Two tabs: "Overview" (skipped entirely per Justin's call — "we don't need overview tab information") and "Locations" (the actual roster). Confirmed via a real `usedRange()` pull (fixture at `n8n/roster_raw_grid.json`) that **Locations is a plain flat table** — title row, subtitle row, a real header row (`Code`/`State`/`Location`/`Status`/`Sales`/`Local Staff (Support Tours)`/`National Facility Manager`), then one data row per location. No card-dashboard layout to fight this time, so the parser just finds the header row by content (`Code`+`Location` column match, not a fixed row index) and reads straight down — the same tolerant-to-position philosophy as every other parser here, but a much shorter function. Verified against all 62 real rows, including edge cases: a numeric (not string) `Code` cell (806, Greenwood — normalized via `String(v).trim()`), a trailing space in a location name ("IN Greenwood "), UNIS entries with `facility_manager: "UNIS-managed"` and `local_staff: "—"`, and a `status` with an embedded date ("Moved Out 6/15/2026"). **Note:** the sheet's own subtitle says "61 locations" but the real data has 62 rows — a stale count in the source sheet itself, not a parsing bug; flagged, not fixed (not the app's data to correct).

**n8n workflow 19 — `19_roster_sync.json`** (+ no separate `.nodes.js` this time, the two Code nodes are simple enough to read straight from the JSON). **Manual webhook only, no cron** (Justin's call — staff/territory assignments change rarely, unlike daily pricing, so a scheduled sync would just be wasted Graph calls). Webhook `roster-sync` → `Read Locations tab` (`usedRange(valuesOnly=true)` on the "Locations" tab, same proven `genericCredentialType`/`oAuth2Api`/`n8n-cubework` auth pattern as workflow 18) → `Parse roster` → `Build Firestore patch` → `Write roster to Firestore` (PATCH `roster/current`) → `Respond`. No loop/accumulator needed (single tab, single read) — the simplest of the three new sync workflows this session.

**Access model — opened up to Lucy, unlike Pricing/Rate Bands.** Justin's call: this is just a contact directory (who to call for a location), not sensitive pricing or negotiating data, so the Avail-only restricted role should see it too. Two changes were needed (the same two-layer pattern as the original Lucy lockdown):
- **`index.html`**: `lockToAvailReadOnly()` now keeps both `data-tab="avail"` and `data-tab="roster"` visible (was Avail-only before) instead of hiding every tab but Avail. The Roster tab's **⟳ Sync now** button is still hidden for her specifically (`syncRosterNow` guards on `AVAIL_READONLY` and `renderRoster` hides the button element) — viewing the directory is fine, triggering a resync is still a staff action.
- **`firestore.rules`**: added `match /roster/current { allow read: if isAnyAllowed(); allow write: if isAllowed(); }`, the same pattern as `availability`/`config/properties`/`renewals/current`. Without this, hiding the tab in the UI would've been cosmetic only — her token could still read `roster/current` directly via the browser console if the rule weren't scoped, same reasoning as the original Lucy lockdown design.

**App side (`index.html`) — new 🗂️ Roster tab**, positioned right after 🏭 Avail in the tab bar (next to the other tab Lucy can see, and logically close since both are location-centric). `ROSTER_SYNC_WEBHOOK` constant (→ `webhook/roster-sync`). New view (`#roster-view`) with a search box, a state filter dropdown, and the sync button. `renderRoster()` groups by state into a plain table (Location / Local Staff / Facility Manager columns) — simpler than Rate Bands' card layout since there's much less per-location data. A status badge (color-coded: UNIS variants indigo, For Sale/Moved Out neutral gray, other statuses amber) shows next to the location name when `status` is non-blank, with the full status text on hover. Reads `roster/current` live via `liveSnapshot`; no snapshot fallback (matches the collection's own no-seed, sync-on-demand design).

**To deploy:** import `19_roster_sync.json` into n8n as a **new** workflow (brand-new webhook path, no conflict risk), pick the Microsoft (`n8n-cubework`) and Google (`Firebase_SDK_do_or_wait`) credentials on its two HTTP nodes, activate (no timezone setting needed — no cron trigger), then test-run once via the app's ⟳ Sync now and confirm all 62 rows land. Publish the updated `firestore.rules` in the Firebase console (adds the `roster/current` match block). `git push` the updated `index.html` for the new tab (and the Lucy-visibility change) to go live.

**New files:** `n8n/19_roster_sync.json`, `n8n/roster_raw_grid.json` (the real 62-row fixture the parser was built and verified against — dev reference only, not read at runtime). **Changed files:** `firestore.rules` (new `roster/current` match block + updated Avail-only comment), `index.html` (`lockToAvailReadOnly`, new Roster tab).

---

## 2026-07-09 — 🏢 Prop Outreach tab: migration, tab build, wave-priority fix, cold draft feature

### Migration: Ontario Airport call sheet, `outreach` → `property_outreach`
Resumed from the paused Ontario Airport migration (see `Sales/Ontario_Airport_Project/CLAUDE.md`). The 289-record Yardi call sheet had been seeded into the wrong collection (`outreach`, meant for unattached Apollo prospecting) with a placeholder `location: "ontario-airport"` tag. Built **`n8n/20_firestore_writer.json`** — a generic Firestore writer (webhook `firestore-write`, one `batchWrite` call per request, supports create/bulk_create/update/bulk_update against any collection) — using the `Firebase_SDK_do_or_wait` service-account credential pattern already proven in workflow 19. Re-parsed the 289 records straight from `Ontario Airport - Call Sheet.xlsx` (cleaner than round-tripping the already-imported docs), normalized `location` to the canonical `3950-airport-ontario`, and fired `bulk_create` into `property_outreach` — landed all 289, wave split verified exact (22/28/26/36/177). **Important correction made mid-migration:** the original migration spec assumed `outreach` held only the 289 misplaced records and could just be cleared wholesale — checked live data first and found `outreach` already holds real, unrelated Apollo prospect data. Built a **dedicated** one-off workflow instead, **`n8n/21_cleanup_ontario_outreach.json`**, that live-queries `outreach` for exactly `location==ontario-airport AND source==yardi` and deletes only those matches, with a `confirmDelete:false` dry-run safety step before the real delete. Two n8n paired-item/IF-node gotchas were hit and fixed in that workflow (documented in its own file): a "Parse matches" Code node aggregating many query-result items into one broke automatic item-linking downstream (fixed with `.first()` instead of `.item`), and the IF node's `conditions` parameter was missing its inner `options:{caseSensitive,leftValue,typeValidation}` object (n8n's filter component reads that internally). Dry run matched 289, real delete removed exactly 289, confirmed clean via `list_outreach`. **My sandbox can't reach `ailinker.item.com` directly** (network allowlist) — every webhook call in this session was run by Justin from PowerShell (`Invoke-RestMethod`), not curled from the sandbox as originally assumed possible.

### New tab: 🏢 Prop Outreach
Built the actual UI (the migration alone left the data with no tab to view it in — caught by Justin asking "am I going to see this in do-or-wait now?"). New view groups by property, then by **call wave**, with a click-through modal (`openPOModal`) for contact fields, status, call-outcome logging, call-sheet notes, call history, and promote-to-lead. Full field/handler list is in the `property_outreach/{id}` collection doc above and the tab list near the top of this file. `firestore.rules`' existing catch-all covered read/write with zero rules changes.

**Wave priority fix.** First pass sorted/displayed waves without addressing that Wave 2 ("5,000+ SF" by definition) will almost always sum to more total square footage than Wave 1 (active pipeline, which includes plenty of small deals) — Justin flagged that Wave 2 was visually reading as "better" than Wave 1 even though Wave 1 is the actual priority. Fixed by sub-grouping each property's table by wave (always rendered 1→5 regardless of SF), tagging Wave 1 with a 🔥 "Hottest — call these first" badge, and color-coding wave headers on a heat scale (red→gray) so priority reads correctly regardless of the raw SF numbers shown alongside (`PO_WAVE_INFO`). Wave itself is a **fixed, one-time classification** baked into the sheet when it was built (see `Ontario Airport - Outreach Game Plan.md`) — not something the app computes live.

### Cold outreach draft feature
Justin asked whether the existing Claude-drafting infrastructure (used for Outreach-tab prospects) could be reused for Property Outreach cold emails. **Workflow 13** (Research & Personalize) wasn't a fit — it requires a real company domain to do live web research, and most `company` values on this call sheet are actually a person's name straight from Yardi's Tenant/Company column, not a researchable business. **Workflow 17** (Generate Lead Follow-up) was the better architectural match — same Claude voice-rule engine, drafts from given context rather than web research — so **`n8n/22_property_outreach_draft.json`** clones that pattern for a first-touch cold email instead of a follow-up.
- **Scope/structure, per Justin's answers:** manual per-lead trigger only (no bulk-generate, no auto-scanning — deliberately separate from the automatic 3-day-cold Follow-up Scanner on Leads), single drafting engine for all waves (no web-research tier), and talking points ("new space available," "a discount," etc.) live at **both** the property level and per-lead: a `property_outreach_context/{locationId}` doc (`available_space`/`discount`/`other`) is set once per property via a panel shown above each property's wave groups (`savePOContextField`), and pre-fills the same 3 fields in every lead's draft modal at that property — editable inline just for that one draft without touching the shared default.
- **Voice/prompt** mirrors wf17's rules (short sentences, 5th-grade readability, no dash characters, banned-phrase list, "Best,\nJustin Cho\nBusiness Development, Cubework" sign-off) adapted for cold-not-follow-up: explicit handling of `company` sometimes being a person's name, an inverted honesty rule (`source_notes` = historical Yardi data, treat as background only; the fresh `available_space`/`discount`/`other` inputs ARE current and can be stated confidently), under-90-word body, 3-6 word subject.
- **App side:** `generatePODraft` POSTs to `PO_DRAFT_WEBHOOK`, same `res.ok` 404/500 error-surfacing pattern as `researchProspect`/`generateFollowup`. Draft renders with Open-in-email (gated on an email being on file, since the Yardi export never includes one — `savePOContact` is how it gets added), Copy, Dismiss.
- **Not yet imported** — needs the same Anthropic + Google credential picks as wf13/wf17.

**Tone + CTA tweak (2026-07-09, before first import).** Justin reviewed a sample draft and asked for two changes to the `Build Claude request` system prompt in `22_property_outreach_draft.json`: (1) **friendlier voice** — the VOICE line now opens "friendly, warm, plain, direct" (was "plain, warm, direct") plus an explicit line allowing "a little genuine warmth in the opening or closing line... as long as it stays short and natural," and point 1 of the email instructions now asks for an opener that "reads like a real person glad to check back in, not a stiff opener" (was just "a brief, honest reason for reaching out"); (2) **CTA changed from a tour/look invitation to a still-looking check** — point 3 now asks the model to end with a question about whether the lead is **still looking for space / still in the market**, explicitly **NOT** an invitation to tour or take a look at a unit (old wording offered "a quick call or a tour"). If the size on file is unknown, that question folds into the same line ("still looking, and if so what size space do you need") rather than being a separate ask. Both edits are inside the same `sys` string in the `Build Claude request` Code node; no other nodes changed.

**To deploy this session's work:** import `20_firestore_writer.json` and `21_cleanup_ontario_outreach.json` (already done, one-time migration use — safe to deactivate/delete now), import `22_property_outreach_draft.json` as a **new** workflow (fresh webhook path, no conflict risk), pick the Anthropic (`Anthropic API`) and Google (`Firebase_SDK_do_or_wait`) credentials on its two respective nodes, activate. `git push` the updated `index.html` for the tab + cold-draft UI to go live.

**New files:** `n8n/20_firestore_writer.json`, `n8n/21_cleanup_ontario_outreach.json`, `n8n/22_property_outreach_draft.json`. **Changed files:** `index.html` (new Prop Outreach tab + all its state/render/modal functions, `property_outreach`/`property_outreach_context` listeners), `Sales/Ontario_Airport_Project/CLAUDE.md` (migration marked complete).

---

## 2026-07-09 (cont.) — 2nd property (Pellissier), collapsible cards, auto-archive, contact-backfill bug fix

**Workflow 20 bug found and fixed.** The 271-item contact backfill from the migration session above (matching a fresh Ontario Airport Yardi export's contact columns onto the 289 existing records) came back `count: 271` but the data landed wrong: `bulk_update`'s Code node did `const fields = { ...item }; delete fields.id;` on items shaped `{id, fields:{...}}` — this only stripped `id`, leaving the nested `fields` key intact, so each doc got a stray top-level field literally named `"fields"` containing `{contact,phone,email}` instead of those three landing at the top level (`contact` stayed `null`). Fixed to `const fields = item.fields || {};` in `n8n/20_firestore_writer.json`. **To deploy:** re-import into the existing "CW — 20. Firestore Writer (generic)" workflow (in-place, from within the workflow, not the workflows list) and re-run the same 271-item payload at `Sales/Ontario_Airport_Project/property_outreach_contact_backfill_payload.json` — it'll correctly overwrite the still-null fields this time. The stray `fields` key left on those 271 docs from the bad run is harmless (nothing reads it) but can be cleaned up later if it bothers anyone browsing raw Firestore data.

**Collapsible per-property groups.** With a 2nd property about to join Ontario Airport on the same tab, `renderPropertyOutreach()` needed a way to collapse one property down while looking at another. Built as `poExpandedProps` (a `Set` of EXPANDED location ids, same pattern as Avail's `expandedAvailProps`) and `togglePOProp(loc)` — click a property's header to expand/collapse it; collapsed shows a one-line summary (lead count + a wave-count chip per wave present, e.g. `W1 · 4`). **Defaults to collapsed** (empty Set = nothing expanded) — first shipped defaulting to expanded, but with real data volume (289 Ontario + 75 Pellissier leads) that read as a wall of tables, so it was flipped same day to match Avail's own "none open by default" convention.

**Auto-archive on repeat decline.** Per Justin's call: the 2nd time a Prop Outreach lead logs "❌ Not interested" (`logPOAttempt` counts `not_interested` entries in `attempts[]`), the record auto-sets `archived:true` — soft-hidden, not deleted, recoverable via the Status filter's new **Archived** option or the modal's **↩ Restore** button. The 1st decline just records normally (a Wave 4 reactivation attempt is allowed one more no before being put away). A manual **🗄 Archive** button is also available any time (`setPOArchived`), for archiving sooner than two declines.

**Pellissier (`pellissier-2720`) added as the 2nd Prop Outreach dataset.** Justin got a new blueprint + a Yardi "Leasing Activity Report" export from Luis for 2720 Pellissier, saved to `Sales/Pellisier_new_070726/`. Same schema/parsing approach as the Ontario Airport migration: 75 rows, waves assigned by the same rule (Tour stage → Wave 1, Dead Deal → Wave 4, Lead stage bucketed by SF ≥/< 5,000 into Wave 2/3, no SF found → Wave 5 — SF pulled from the sheet's `Area` column when present, else parsed out of free-text Deal Notes and flagged `sqft_est:true`). Wave counts: 1→4, 2→7, 3→19, 4→12, 5→33. Contact/phone/email were already filled in on this export (unlike Ontario's, which needed a separate backfill) — 75/75 contact, 75/75 phone, 70/75 email. Two rows' deal notes reference a different Cubework property's address (a source-data mixup, not something the parser should silently paper over) — flagged in `Sales/Pellisier_new_070726/pellissier_property_outreach_summary.md`, not fixed. Payload (`bulk_create`, 75 items) sits at `Sales/Pellisier_new_070726/pellissier_property_outreach_payload.json`, run the same way as the Ontario migration via workflow 20's `firestore-write` webhook.

**The "blueprint" itself (`CA_2720 Pellissier_07.07.26.pdf`) is a floor-plan drawing**, not a data file — confirmed by extracting its text (unit boxes C01-C05/A01-A02/D01-D05, ~30,000 SF on the plan). The in-app floor-plan feature is abandoned (2026-06-17/18 decision above), so this wasn't parsed into anything structured. Cross-checked against the live `availability/pellissier-2720` doc instead: it already shows a large open bay (unit B01, 48,914 SF) and a smaller one (C01, 1,500 SF), both currently `hold:true` for Joe Lee — likely what the blueprint's "additional units" refers to. Not auto-populated into the property's Prop Outreach talking-points panel (`property_outreach_context`) since a hold isn't the same as ready-to-market space; left for Justin to set by hand if/when he wants it stated in cold drafts.

---

## 2026-07-09 (cont. #3) — 🔮 Future Plan (uploaded PDF of planned/future units, Avail tab)

Luis sent an updated Pellissier site plan showing units that will exist in the future but aren't built/available yet — separate from the abandoned floor-plan-reconstruction feature and separate from current availability. Justin's ask: **"I want to see the pdf in the website."** Built as a small, general (not Pellissier-only) per-location feature on the 🏭 Avail tab, since it ties directly into the Prop Outreach talking-points work (a "future capacity" signal Justin may eventually want reflected in cold-outreach context, though that link isn't wired automatically — same reasoning as the existing hold-vs-market-ready caution above).

**Data model — new fields on `availability/{propId}`** (see that collection's doc above): `future_plan_url` (Firebase Storage download URL), `future_plan_storage_path` (Storage path, kept so a replace/remove can delete the old file), `future_plan_note` (free text — e.g. "5 additional units planned (~8,000 SF), submitted by Luis, expected Q4 2026"), `future_plan_updated_at` (ISO timestamp). Written via the same **REST PATCH + `updateMask.fieldPaths`** pattern already proven for `pa`/`ownership`/`lease_expiration` — guarantees these fields are excluded from the SharePoint sync's own scoped write and survive re-syncs untouched, exactly like the other app-edited fields.

**Upload mechanism.** Reused the app's existing Firebase Storage SDK setup (`getStorage`/`ref`/`uploadBytes`/`getDownloadURL`/`deleteObject`, already imported for voice notes/file attachments — no new imports needed) rather than linking out to an externally-hosted file. PDFs are stored at `files/future-plan-{propId}-{timestamp}.pdf` — **not** a new `future_plans/` prefix (see bug note below).

**Bug hit + fixed same day: `storage/unauthorized` on first real upload.** The first version wrote to a brand-new `future_plans/{propId}-{timestamp}.pdf` path. Justin's first real upload (Pellissier) failed with `Firebase Storage: User does not have permission to access 'future_plans/pellissier-2720-...pdf'. (storage/unauthorized)`. Cause: Firebase Storage security rules are **path-scoped** (mirroring the Firestore rules' allowlist pattern) and, unlike `firestore.rules`, aren't checked into this repo — they're edited by hand in the Firebase console. The existing working uploads (`handleFileUpload`'s attachments at `files/{fileId}{ext}`, voice notes at `audio/{audioId}`) are covered because those exact path prefixes are already granted; a brand-new `future_plans/` prefix was not. **Fix:** changed the upload path to `files/future-plan-{propId}-{timestamp}.pdf` — same already-permitted `files/` prefix, just a distinguishable filename — so no Firebase console change is needed at all. This is the actual, verified fix (not just an assumption the way the original "no Storage rules change needed" line below turned out to be, before this bug surfaced it was wrong).

**Display — inline, not just a link-out.** Per Justin's explicit wording ("I want to see the pdf in the website"), the PDF renders as an inline `<iframe>` embed (480px tall) inside the location card, with an "Open in new tab ↗" link as a fallback/full-view option. This is deliberately different from the existing Floor Plan section (`renderFloorplans`, just above it in the card) — that one is link-only per the 2026-06-17/18 "abandon in-app floor plans" decision (a *recurring, error-prone reconstruction/sync* problem), whereas Future Plan is a simple one-off manual upload with nothing to reconstruct, so an inline embed was the right call here and doesn't reopen that earlier decision.

**App side (`index.html`):**
- New modal `#fplan-overlay` (mirrors the `pa-overlay`/`own-overlay` markup pattern) with a PDF file input, a free-text note `<textarea>`, and a status line.
- `renderFuturePlan(propId)` — renders into a new `<div id="future-plan-section-${propId}">` placeholder inside `renderLocBody` (right after the existing `fp-section-${propId}` Floor Plan placeholder). Empty state shows a "＋ Add future plan" button (hidden under `AVAIL_READONLY`, same gating as every other Avail edit control); populated state shows the note, an "Updated <date>" line, the iframe embed, "Open in new tab ↗", and Edit/Remove buttons (Remove also hidden under `AVAIL_READONLY`).
- `openFuturePlanModal(propId, e)` / `closeFuturePlanModal()` — standard modal open/close, pre-filling the note and a hint that leaving the file picker blank keeps the existing PDF.
- `saveFuturePlan()` — validates the picked file is a PDF, optionally uploads to Storage (skips upload entirely if no new file was chosen, so the note can be edited alone), then PATCHes `availability/{propId}` with all four fields via `updateMask.fieldPaths`.
- `removeFuturePlan(propId, e)` — confirms, deletes the Storage object (best-effort — a failed Storage delete doesn't block clearing the Firestore fields), then PATCHes all four fields back to empty strings.
- Wired into the same two call sites that already refresh `renderFloorplans(propId)` after a body re-render (`setAvailFilter`, `toggleAvailCard`) — `renderFuturePlan(propId)` is called alongside both.
- All four new handlers exposed on `window.*` (ES-module gotcha), next to the existing PA/Ownership exposures.

**To deploy:** `git push` the updated `index.html` — this is a pure app-side feature, no n8n workflow or Firestore rules change needed (the new fields fall under the existing `availability/{propId}` write rule, same access level as `pa`/`ownership`). No manual Firebase Storage rules change needed either, now that uploads go to the already-permitted `files/` prefix (see the bug note above — this was NOT true of the first version).

**Not yet done:** actually uploading Luis's updated Pellissier PDF through the feature (Justin will do this himself once the feature is live) and any explicit link from Future Plan data into the Prop Outreach cold-draft context (`property_outreach_context`) — flagged as a possible future connection, not built.

---

## 2026-07-09 (cont. #4) — Per-property cold-draft prompt A/B testing

Justin's ask, before doing the deploy checklist above: he wants to **save different cold-outreach prompts and compare which one actually works** — start with naming and saving today's friendly-tone prompt for Pellissier, then tomorrow write a differently-toned prompt for Ontario Airport, and over time compare outcomes between them to converge on the best one. This needed a way to (1) run a different system prompt per property instead of one prompt baked into workflow 22, and (2) permanently tag every generated draft with which prompt made it, so results stay comparable later even if a property's prompt is edited or renamed afterward.

**Design — extended `property_outreach_context`, no new collection.** Considered a separate `outreach_prompts` library collection (named, reusable, cross-property) but scoped it down: Justin only wants one prompt per property right now, not a shared library switchable mid-property, and copy-pasting text between properties covers reuse well enough without adding a CRUD modal. So it's just two new optional fields directly on the existing per-property `property_outreach_context/{locationId}` doc:
- `prompt_name` — a short label, e.g. "Friendly reconnect v1".
- `system_prompt` — the full system prompt text overriding workflow 22's own built-in default for that property's drafts.

Both blank (the default for every property until touched) means "keep using the workflow's shared default, unchanged" — zero behavior change for properties nobody has touched yet.

**App side (`index.html`):**
- `savePOContextField` allowlist extended to include `prompt_name`/`system_prompt`.
- New collapsible section under the existing talking-points panel (`poPromptEditOpen` Set of expanded location ids, `togglePOPromptEditor(loc)` — same collapsed-by-default pattern as `poExpandedProps`/`expandedAvailProps`). Collapsed, it's just a small button showing "✏️ Prompt: <name>" once one is saved, or "✏️ Draft prompt (using shared default)" before that. Expanded, it shows a name input, a 7-row textarea for the full prompt text, and a **📋 Load current default as a starting point** button (`loadDefaultPOPrompt`) that fills the textarea (and saves it) with `DEFAULT_PO_SYSTEM_PROMPT` — a plain JS constant in `index.html` that's a **manually-kept-in-sync mirror** of workflow 22's own `DEFAULT_SYS` (n8n/22_property_outreach_draft.json, "Build Claude request" node). This is the whole point of "save this one for Pellissier": open the editor, hit Load, it's already the tone in use today, name it, save — no retyping. If the two copies of the default text ever drift apart, the only consequence is the Load button prefilling stale text (cosmetic only, not a functional bug) — worth a periodic manual check but not urgent.
- `generatePODraft` reads `prompt_name`/`system_prompt` straight from `poContextMap[r.location]` (not a per-lead override input, unlike available_space/discount/other — a property's prompt choice doesn't vary lead to lead) and includes both in the webhook body to workflow 22.
- New window exposures: `togglePOPromptEditor`, `loadDefaultPOPrompt`.

**n8n side (`22_property_outreach_draft.json`, not yet imported — this rides along with the same first import as everything else in this file):**
- "Build Claude request": the previously-inline `sys` literal is renamed `DEFAULT_SYS`; a new line `const sys = b.system_prompt || DEFAULT_SYS;` picks the property's custom prompt when the app sent one, else falls back unchanged. `userMsg` (the lead-details/context data block) is untouched either way — only voice/tone differs between prompts, not what factual context Claude is given.
- The node's return now also includes `prompt_name: b.prompt_name || ''` and `system_prompt_used: sys` (the actually-resolved prompt text, whichever one it was) so the write-back step has both.
- "Build Firestore patch": `cold_draft` gains `prompt_name` and `system_prompt_used` fields (alongside the existing `subject`/`body`/`generated_at`) — a permanent record of exactly which named prompt, and exactly what text, produced that specific draft. This is deliberately captured **per draft**, not just read live off the property's current config, so editing or renaming a property's prompt later never rewrites history — old drafts stay correctly attributed to whatever they were actually generated with.

**Not built yet, flagged for later:** an actual comparison view (e.g. reply/interested/promoted rates grouped by `prompt_name`, per property). Justin hasn't sent anything with either prompt yet (Pellissier's is being named today, Ontario's is being written tomorrow), so there's no data to compare — once outcomes start accumulating over the next 1-2 weeks, a small breakdown (either a new section in the Prop Outreach tab or a live artifact reading `property_outreach` grouped by `cold_draft.prompt_name`) is the natural next step.

**To deploy:** no new deploy step beyond what's already pending — this rides along with the still-not-yet-imported workflow 22 (its very first import already includes this) and the still-pending `git push` of `index.html`. Once live: open Pellissier's talking-points panel, expand the prompt editor, hit "Load current default", name it (e.g. "Friendly reconnect v1"), save. Tomorrow, do the same for Ontario Airport but write a genuinely different-toned prompt before saving.

---

## 2026-07-10 — Outreach Reply Scanner (know when a cold email gets a reply — Outreach AND Property Outreach)

Justin's ask started as "the Outreach tab has no way to know when a prospect replies," but the real motivating case turned out to be **Property Outreach** — he confirmed mid-session that was the whole reason he brought it up. Both `outreach/{id}` (260+ Apollo prospects) and `property_outreach/{id}` (360+ records across Ontario Airport + Pellissier) send cold emails and had zero reply signal, unlike Leads (which gets this via the resolved `email_links[]` thread pattern). Wanted the most **efficient** design, brainstormed before building, then corrected mid-build once the numbers didn't hold up (see below).

**Design choice, v1 — a windowed inbox scan, not a per-prospect loop.** The obvious approach (clone workflow 6's Follow-up Scanner — one Graph `$search` call per record) doesn't scale: 260+ prospects means 260+ Graph calls per run, and the list only grows. A true Graph `/messages/delta` query was considered too (push-efficient, one call regardless of scale) but rejected — its first-ever call returns the ENTIRE mailbox history before yielding a `deltaLink`, and mail delta subscriptions/cursors add a new failure mode this codebase has consistently avoided (see the Cloud Run CI/CD deferral, `CLOUD_RUN_CONTINUOUS_DEPLOY.md`). Landed on: one windowed Graph GET per scan (`receivedDateTime ge <since-last-scan>`), matching against an in-memory map built by **listing** the outreach collection.

**Design correction — dropped the "list the whole collection" step entirely.** Extending v1 to also cover `property_outreach` exposed the flaw: that collection is already 360+ docs, past the practical ~300-doc single Firestore list-page boundary, so "list every prospect, build a map, match in code" would need real `pageToken` pagination just to enumerate the prospect list every single scan — wasted work, and a second pagination loop to get right for a second collection. **Flipped the lookup direction instead:** pull the small number of NEW inbox messages this window (one Graph call, as before), extract the handful of distinct sender addresses from just those messages, then ask Firestore directly — via a targeted `IN`-filter `runQuery` — whether either collection recognizes any of those addresses. Neither `outreach` nor `property_outreach` is ever listed in full; cost scales with new-mail volume per scan, not with how large either prospect list grows. Strictly more efficient than the original plan, and correctly handles two collections of very different size with no extra pagination logic. Two decisions confirmed with Justin before building: cadence = scheduled scan (not real-time push, not per-prospect), and on-detect action = **flag + banner only**, never auto-promote — promotion stays manual (the `promote_outreach_to_lead` MCP tool for Outreach; the existing in-app `promotePOToLead` button for Property Outreach) once Justin reviews a reply.

**n8n workflow 23 — `23_outreach_reply_scanner.json`** (+ `outreach_reply_scanner.nodes.js`, imported and active — confirmed 2026-07-14). Cron every 2 hours (`0 */2 * * *`; **set Settings → Timezone → America/Los_Angeles on import**, same lesson learned from every prior cron workflow here) — Justin considered widening it to 4 hours to cut Graph calls, but kept it at 2 since the cost is negligible and 2 hours means a reply is never more than 2 hours stale. Chain: `Get scan window` (Code — reads a persisted `last_scan_iso` from workflow static data, same accumulator mechanism as workflow 18's tab loop; first-ever run bounds itself to a 24h lookback rather than the whole mailbox history; fed by **both** the cron trigger and the manual-check webhook below) → **`Resolve 'me' folder`** (HTTP GET, `/me/mailFolders('inbox')/childFolders?$filter=displayName eq 'me'` — Justin's replies land in a subfolder of Inbox called **"me"**, not the Inbox root; Graph's `'inbox'` well-known-name shortcut only resolves top-level folders, so a custom child folder needs its real id looked up first. Resolved fresh every scan rather than cached, `continueOnFail` so a lookup miss just yields zero senders that run instead of failing it) → `Fetch inbox messages since window` (one Graph GET against `/me/mailFolders/{resolved id}/messages`, `$filter=receivedDateTime ge {since}&$select=from,receivedDateTime,subject,bodyPreview,webLink,conversationId&$top=250`, `continueOnFail` so a transient Graph hiccup doesn't kill the run — deliberately **no** `$orderby`, to sidestep the "InefficientFilter" class of error hit in workflow 17's `$filter`+`$orderby` combo) → `Extract distinct senders` (Code — dedupes to the newest message per sender address seen in the window; queries both the raw-cased and lowercased form of each address since Apollo-sourced Outreach emails are stored lowercase but Property Outreach contacts are hand-typed via `savePOContact` and may not be; capped at 15 distinct senders/scan — comfortably covers normal reply volume in a 2-hour window, Firestore's `IN` filter allows up to 30 values, not currently a known problem) → fans out to **`Save scan window`** (Code — always advances the cursor, with a 20-min overlap buffer against clock skew, regardless of whether anything matched), **`Respond to check`** (see manual-check button below), and `Has any new mail?` (IF on `values.length > 0`) → true branch fans out to **`Query outreach by sender`** and **`Query property_outreach by sender`** (two Firestore `:runQuery` POSTs, each a `structuredQuery` with a `fieldFilter` `op: IN` on `email` against the extracted address list — one call per collection, not a list) → both feed `Match + build items` (Code — decodes each `runQuery` response, tolerant of n8n's auto-splitting a top-level JSON array into multiple items, matches back to the sender map, builds two separate `{id, fields}` item lists) → fans out to `Has outreach items?` / `Has property_outreach items?` (IF nodes) → each true branch POSTs to `https://ailinker.item.com/webhook/firestore-write` (**reuses workflow 20's already-live generic writer**, `bulk_update` action, one call per collection — server-to-server, not through the Cloudflare CORS worker, same reasoning as workflow 15's callback).

**Manual "⟳ Check now" button (added 2026-07-10, same session, before first import).** Justin asked whether a manual trigger made sense given the app already has this pattern for Roster/Rate Bands; agreed it's a cheap, useful addition and built it. A second trigger node, **`Manual check webhook`** (webhook, path `outreach-reply-scan`, `responseMode: responseNode`), feeds into the exact same `Get scan window` chain as the cron — it just runs the next scan immediately instead of waiting for the clock, still resuming from wherever the last scan (cron or manual) left off, not from scratch. A **`Respond to check`** node (`respondToWebhook`), fed directly off `Extract distinct senders`, gives the button an immediate `{ok:true, checked:true, newSendersSeen}` acknowledgment without waiting for the Firestore `runQuery`/writer calls further down the chain to finish — the app's existing live Firestore listeners pick up any actual reply flags moments later regardless, same as everywhere else in this app. On cron-triggered runs `Respond to check` is a harmless no-op (nothing is listening for a webhook response). App side: `OUTREACH_REPLY_SCAN_WEBHOOK` constant (→ `webhook/outreach-reply-scan`) and a shared `checkOutreachRepliesNow(btnId, e)` handler (same disable-button / `res.ok` 404-vs-other-error pattern as `syncRosterNow`), with independent **⟳ Check now** buttons in both the Outreach and Prop Outreach toolbars (`#outreach-reply-check-btn` / `#po-reply-check-btn`, each passing its own id so the two tabs show independent loading states). Exposed as `window.checkOutreachRepliesNow`.

**Auth pattern:** Microsoft Graph call uses `genericCredentialType`/`oAuth2Api`/**`n8n-cubework`** — the pattern explicitly proven working for new workflows (11/18/19), not workflow 6's older `predefinedCredentialType`/`microsoftOAuth2Api` pattern (also proven, but for a different call shape). The two Firestore `runQuery` calls use the same `predefinedCredentialType`/`googleApi`/`Firebase_SDK_do_or_wait` service-account pattern as every other Firestore write in this project.

**New fields on both `outreach/{id}` and `property_outreach/{id}`** (see each collection's doc above — identical shape on both): `reply_detected`, `reply_detected_at`, `reply_received_at`, `reply_subject`, `reply_snippet`, `reply_web_link`, `reply_seen`.

**App side (`index.html`) — Outreach tab:**
- `outreachRows()` carries the new fields through (`replied`, `replySeen`, `replySubject`, `replySnippet`, `replyReceivedAt`, `replyWebLink`).
- `renderOutreach()`: syncs the `#outreach-filter` select's displayed value (needed since the banner button now sets the filter programmatically); computes `repliedUnseen` (replied AND not yet reviewed, excluding invalid-flagged prospects); writes a tab-bar badge on the 📧 Outreach tab button (same `.tab-badge` pattern as Leads/Tasks); shows a blue dashed **reply banner** (`#outreach-reply-banner`) with a "Review replies →" button that calls `setOutreachFilter('replied')`; the table's Status cell gets a small 📩 badge (darker/lighter shade distinguishes reviewed vs new) with the reply snippet on hover.
- New filter option `value="replied"` on `#outreach-filter`.
- `openOutreachModal`: new reply block (blue border, shown whenever `reply_detected` is true) with the received date, subject, snippet, an "Open thread ↗" link (via the existing `fixThreadUrl()` helper), and either a "Mark reviewed" button or a quiet "✓ Reviewed" label.
- `markOutreachReplySeen(id, e)` — new handler, sets `reply_seen:true` via `setDoc(...,{merge:true})`. Exposed on `window.*` (ES-module gotcha).

**App side (`index.html`) — Prop Outreach tab (the actual motivating use case):**
- `renderPropertyOutreach()`: syncs `#po-status`'s displayed value; computes `repliedUnseen` (non-archived, replied, not yet reviewed) across ALL properties; writes a tab-bar badge on the 🏢 Prop Outreach tab button; shows the same blue dashed **reply banner** (`#po-reply-banner`) with a "Review replies →" button that calls `setPOStatus('replied')` — a new special status-filter value (alongside the existing `archived` special value) rather than a real call-status, since `status` already drives the call-outcome workflow (`logPOAttempt`/`PO_STATUS_LABEL`) and shouldn't be overloaded.
- Row badge: each lead's status cell gets the same 📩 reply badge as Outreach's table.
- `openPOModal`: same reply block design as Outreach's modal (received date, subject, snippet, "Open thread ↗", Mark reviewed / ✓ Reviewed), placed right after the Archived banner. Because Property Outreach already has an in-app **`promotePOToLead`** button (unlike plain Outreach, which only has the MCP tool), the reply → review → promote loop is fully closed here: see a reply, review it, click the existing 🎯 Promote to lead button.
- `markPOReplySeen(id, e)` — new handler, same pattern as `markOutreachReplySeen`. Exposed on `window.*`.

**Deployed — imported into n8n and activated (confirmed 2026-07-14).** Deploy steps followed: import `23_outreach_reply_scanner.json` into n8n as a **new** workflow (cron trigger plus the `outreach-reply-scan` webhook for the manual check button — neither path is used anywhere else, so no conflict risk like the workflow-5 duplicate saga), pick the Microsoft (`n8n-cubework`) and Google (`Firebase_SDK_do_or_wait`) credentials on its Graph and two Firestore `runQuery` HTTP nodes, set Settings → Timezone → America/Los_Angeles, activate. Confirm workflow 20 ("CW — 20. Firestore Writer") stays Active — this scanner depends on it for both collections. `git push` the updated `index.html` for the tab badge/banner/modal/check-now-button changes (both tabs) to go live.

**Not yet done / flagged for later:** an actual "Promote to lead" button on the plain Outreach tab itself (promotion there still only happens via the MCP tool in chat) — out of scope for this reply-detection pass. Also not built: chunked queries if a single scan window ever sees more than 15 distinct inbox senders (not currently a known problem at Justin's volume).

**New files:** `n8n/23_outreach_reply_scanner.json`, `n8n/outreach_reply_scanner.nodes.js`. **Changed files:** `index.html` (Outreach tab AND Prop Outreach tab banner/badge/modal reply blocks), `CLAUDE.md` (this section + field additions on both collections).

---

## 2026-07-10 (cont.) — 🤖 Assistant tab: in-app Claude chat via the native MCP connector

Justin's ask: an in-app assistant, powered by Claude, that can "take care of lead stuff" — using the same do-or-wait MCP connector already proven in Claude Desktop/claude.ai chats. He'd tried this through claude.ai's conversation mode and didn't like it: that surface narrates every tool call in the transcript, which is noisy for a day-to-day ops tool.

**Root cause, and why building it in-app fixes it.** The verbose tool-call narration is a UI choice of Claude Desktop/claude.ai's conversation mode, not something the Claude API forces. If Justin owns the chat panel (renders it himself, in `index.html`), he controls exactly what's shown — only the assistant's final reply, nothing about which tools ran, unless he explicitly wants to peek.

**The enabling piece: Anthropic's native MCP connector.** Confirmed via the current Claude Platform Docs (`platform.claude.com/docs/en/agents-and-tools/mcp-connector`, beta header **`mcp-client-2025-11-20`** — the older `mcp-client-2025-04-04` is deprecated) that the Messages API can call a remote MCP server directly, server-side, in one request: `mcp_servers:[{type:'url', url, name}]` + `tools:[{type:'mcp_toolset', mcp_server_name}]`. This maps exactly onto the do-or-wait **Cloud Run remote MCP server** (`Sales/DoOrWait_MCP/remote-server.js`, see `[[do-or-wait-mcp-dual-deploy]]`) — it's already a plain `POST /mcp/<token>` HTTP endpoint (Streamable HTTP transport, bearer-or-path-token auth), which is exactly the shape the connector expects. No new bridging code was needed on the MCP side at all — the same URL+token already used as the claude.ai custom-connector Server URL (per `Sales/DoOrWait_MCP/DEPLOY.md` §3) is reused as-is.

**Where the Anthropic API call lives — n8n, not the Cloudflare Worker.** Initially assumed the existing Worker (`plain-credit-5962...`) would be the natural place for this, since it already holds the CORS-proxy role for every webhook. Checked its actual job first: it's a pure pass-through, forwarding any `/webhook/<path>` to the same-named n8n path — not a place with its own compute/secrets today. Building Anthropic-calling logic there would mean modifying shared, load-bearing infra every other feature depends on. n8n, on the other hand, already has a proven `anthropicApi` credential (used by workflows 13/17/22) and the same generic PROXY-forwarding convention every other feature already rides on. So this became **n8n workflow 24**, not a Worker route — `ASSISTANT_CHAT_WEBHOOK` in `index.html` is just `${PROXY}/webhook/assistant-chat`, identical in shape to every other webhook constant in the file.

### n8n workflow 24 — `24_assistant_chat.json` (+ `.README.md`, NOT yet imported)
Webhook `assistant-chat` (synchronous, `responseMode: responseNode`) → **Build Claude request** (system prompt describing the assistant's role and today's date, converts the app's `{role,text}` message history into Anthropic content blocks, attaches `mcp_servers`/`tools` per the connector docs above) → **Call Claude (MCP connector)** (`POST /v1/messages`, `anthropic-beta: mcp-client-2025-11-20`, `continueOnFail:true` so a Claude-side error surfaces cleanly instead of n8n's generic error page) → **Build response** (keeps ONLY `type:'text'` content blocks, joined, as `reply`; separately collects any `mcp_tool_use` block names into `toolsUsed` — an optional signal the app can show or ignore, never forced into the transcript) → **Respond** (`{ok,reply,toolsUsed,stopReason}`, or `{ok:false,error}` on failure — same `res.ok`-checked pattern as `researchProspect`/`generateFollowup`/`generatePODraft`).

**Tool scope:** everything the connector exposes (Tasks/Leads/Notes/Outreach/Property Outreach/Availability/Pricing/Rate Bands/Renewals/Roster) — no `default_config`/`configs` allow/deny list. Justin's call, since this is meant to be a general assistant, not leads-only. If a narrower or read-only version is ever wanted, the MCP connector's per-tool `configs` (enable/disable by name) is the built-in way to do it without touching `tools.js`.

**Model:** `claude-sonnet-4-6`, same default already established for wf13/17/22.

**Auth / cost note (accepted tradeoff, flagged in the README):** this webhook has no per-request caller auth, same as every other webhook in this project (resolve-thread, research-prospect, generate-followup, property-outreach-draft). It's a bit different in kind since every call spends real Anthropic API money — the practical safety net is a **spend cap in the Anthropic Console** (Settings → Limits), not endpoint auth. Worth reconsidering only if this URL needs to be shared more widely than it is today.

**History simplification (accepted tradeoff, flagged in the README):** the app sends plain `{role,text}` for every turn, including past assistant turns — so a prior turn's `mcp_tool_use`/`mcp_tool_result` blocks are never replayed back to Claude, only the final text. Keeps requests small and the transcript clean; the cost is Claude re-calling a tool if a later turn needs data it already fetched earlier in the conversation. Fine for v1.

### App side (`index.html`)
- New **🤖 Assistant** tab (last in the tab bar), `#assistant-view`: a scrollable message list (`#assistant-messages`), an error line, and an input `<textarea>` + Send button (Enter to send, Shift+Enter for a newline).
- One persisted thread, `assistant_threads/main` (see that collection's doc above) — live-synced via the same `liveSnapshot` pattern as every other collection, so it survives reloads and is consistent across devices/tabs.
- `renderAssistant()` renders each message as a simple chat bubble (user right-aligned in `--wait` blue, assistant left-aligned neutral) — **only `{role,text}`, nothing about tool calls**, which is the entire point of this feature. Assistant turns that used at least one tool get a small, collapsed **"🔧 used N tools"** toggle (`toggleAssistantToolsChip`) — click to reveal the tool names inline; collapsed by default so it never intrudes unless Justin wants to check.
- `sendAssistantMessage()` appends the user's message locally + to Firestore, POSTs the full `{role,text}` history to `ASSISTANT_CHAT_WEBHOOK`, and on success appends the assistant's reply (+ `toolsUsed`) back to Firestore. A 404 is surfaced as "workflow not imported/active" (same `res.ok` pattern as the rest of the app); other failures show the raw error inline rather than via `alert()`, since an inline error bubble fits a chat UI better.
- `newAssistantThread()` clears `assistant_threads/main.messages` after a confirm (only one thread exists — no thread picker was built, since nothing yet calls for more than one running conversation).
- All three new handlers exposed on `window.*` (ES-module gotcha, same as every other inline handler in this file).
- Automatically excluded from the Avail-only restricted role (Lucy) — `lockToAvailReadOnly()` only allow-lists `avail`/`roster` tab buttons, so any new tab (this one included) is hidden for her by default, no extra gating code needed.
- **Phone-friendliness pass (2026-07-10, same day)** — Justin flagged this is the tab he'll actually use mostly from his phone. Fixed: the input `<textarea>` was 14px, which triggers iOS Safari's auto-zoom-on-focus (any input/textarea under 16px does this) — bumped to 16px. The panel's height used a fixed `calc(100vh - 210px)`, which doesn't track a mobile browser's address bar hiding/showing or the keyboard opening — added a `calc(100dvh - 210px)` override right after it (dvh = dynamic viewport height; unsupported browsers just ignore that line and keep the vh one, so it's a safe progressive layer, not a hard dependency). Send button and the input both bumped to a 44px min-height touch target (Apple HIG minimum); "New thread" bumped to 36px. The tab bar itself needed no change — it already scrolls horizontally on narrow screens (`overflow-x:auto`, same mechanism used since the tab count first exceeded phone width).

**Firestore rules:** no change needed — `assistant_threads/{id}` falls under the existing catch-all `match /{document=**}` block (`isAllowed()`-only), same reasoning as `property_outreach`.

**To deploy:** import `24_assistant_chat.json` into n8n as a **new** workflow (brand-new webhook path, no conflict risk), pick the **Anthropic API** credential on "Call Claude (MCP connector)" (same one used by wf13/17/22 — a dropdown pick, no new setup), then **hardcode the MCP URL directly in the "Build Claude request" node** (see the correction below — do NOT rely on `$env.DOW_MCP_URL`), activate. Consider setting a spend cap in the Anthropic Console (see auth/cost note above). `git push` the updated `index.html` for the new tab to go live.

**Not yet done:** any UI-side allow/deny restriction on tool scope (deliberately deferred to the MCP connector's own `configs` mechanism if ever wanted), and multiple parallel threads (single `main` thread only, per above).

**New files:** `n8n/24_assistant_chat.json`, `n8n/24_assistant_chat.README.md`. **Changed files:** `index.html` (new 🤖 Assistant tab, `assistant_threads` listener, `ASSISTANT_CHAT_WEBHOOK`), `CLAUDE.md` (tab list + `assistant_threads/main` collection doc + this section).

### 2026-07-10 (cont. #2) — imported, mobile pass, two post-import fixes

Justin imported workflow 24 and worked through the deploy checklist live. Two things came up that the original build didn't anticipate, both are now fixed and documented in `n8n/24_assistant_chat.README.md`:

**1. `$env.DOW_MCP_URL` doesn't work on this n8n instance.** n8n's own "Environment variables" UI (Settings → Variables) turned out to be a paid/licensed feature this instance doesn't have — hitting an upgrade wall there is expected, not a misconfiguration. **Fix:** hardcode the MCP URL directly as a literal string inside the "Build Claude request" Code node (replacing the `$env.DOW_MCP_URL` line), done live in the n8n UI, never in the exported/committed JSON — this is actually the safer place for it anyway, since it means the secret only ever lives in Justin's private n8n database, not in the public GitHub repo. If host-level env vars are ever wired up later, the `$env.DOW_MCP_URL` line can be restored.

**2. First response felt slow.** Broken down for Justin: multiple hops (browser → Worker → n8n → Anthropic → Cloud Run MCP → Firestore → back), a real tool-call round trip when the assistant needs live data, Cloud Run's likely cold start given occasional/bursty usage (fixable with `gcloud run services update do-or-wait-mcp --region us-west1 --min-instances=1`, moving off the free scale-to-zero tier for a small ongoing cost), and no streaming (nothing renders until the whole reply is done).

**3. Prompt caching added (the actual fix for "why does every message resend the whole system prompt").** Justin's real observation: the system prompt + MCP tool definitions are identical on every turn and were being fully reprocessed and rebilled at full price each time. Added one field, `cache_control: { type: 'ephemeral' }`, at the top level of the request body in "Build Claude request" — this is Anthropic's "automatic caching," which finds the repeated prefix (tools → system → messages, in that order) and reuses it, moving the cache breakpoint forward itself as the conversation grows. Cache reads are ~10% of normal input price and faster to process than a full reprocess, so it cuts cost and latency on every turn after the first in a session (default 5-minute lifetime, refreshed free on each hit; the 1-hour variant exists if longer gaps between messages ever matter more than they do at Justin's usage level). **Build response** now also surfaces `cacheRead`/`cacheWrite` token counts so cache effectiveness is checkable directly in a test response. This does not reduce what the browser sends per message (unavoidable with a stateless API) — it stops Anthropic from fully rebilling/reprocessing the repeated part.

**Corrupted-then-fixed JSON, an unrelated hiccup during this session.** While editing `n8n/24_assistant_chat.json`'s Code node in-place twice in a row, an in-place text edit introduced literal unescaped characters into the middle of a JSON string value, breaking the exported file's JSON syntax (Python `json.load` failed). Root-caused via careful byte-offset inspection (the reported error location was far downstream of the actual defect, a common trait of JSON parsers), then fixed by rewriting the whole file cleanly in one pass and validating it in an isolated scratch copy (the do_or_wait-mounted sandbox path has its own stale-cache quirk that made in-place bash validation unreliable — a fresh copy in the scratch/outputs directory validates cleanly). **Important:** this only ever affected the on-disk export file, never Justin's already-imported, already-running n8n workflow — those are two disconnected copies once import happens, so no reimport was ever needed.

**Not yet done:** Justin hasn't decided yet whether to set Cloud Run `--min-instances=1` for the cold-start fix — flagged as optional, cost-bearing, his call.

**Prompt caching added same session (post-import).** Justin flagged that every message resends the identical system prompt + tool list, which gets fully reprocessed and rebilled every time. Fixed live in n8n (same hardcode-in-node pattern as the MCP URL): added `cache_control: { type: 'ephemeral' }` at the top level of the request body in "Build Claude request." This is Anthropic's "automatic caching" — it finds the repeated prefix (tools → system → messages, in that order) and reuses it, moving the cache breakpoint forward itself as the conversation grows, no manual multi-breakpoint bookkeeping needed. Cache reads are ~10% of normal input price and faster to process, cutting cost and latency on every turn after the first in a session (default 5-minute lifetime, refreshed free on each hit; the 1-hour variant exists if longer gaps between messages ever matter more). Does NOT reduce what the browser sends per message (unavoidable, stateless API) — it stops Anthropic from fully reprocessing/rebilling the repeated part. **Build response** now also surfaces `cacheRead`/`cacheWrite` (from `usage.cache_read_input_tokens`/`cache_creation_input_tokens`) so cache effectiveness is checkable directly in a test response. Documented in `n8n/24_assistant_chat.README.md`.

---

## 2026-07-10 (cont. #3) — Daily follow-up gate + queue (replacing banner fatigue)

Justin's frustration, stated directly: mixing manual work with partial automation makes it easy to lose track of who needs a follow-up, and the two banners at the top of Leads (🔔 Follow-ups, 🌱 Space Matches) don't actually motivate action — "I ignore the banner once then it becomes just a part of the app, nothing for me to take care of." This is banner blindness: a persistent, dismissable, in-tab element gets mentally filed as ambient furniture the first time it's consciously skipped, regardless of what's actually in it on a given day. The fix isn't a better banner, it's not a banner.

**Design (confirmed with Justin before building): a gate on every app open, not a banner in a tab.** If anything needs a decision, a modal appears before anything else, with exactly two options: **Review now** (opens a one-lead-at-a-time queue) or **Not now** (dismisses for this page load only — free/instant, writes nothing anywhere, so there's no "snooze debt" to track; it just re-evaluates fresh, from live data, the next time the app loads). Justin explicitly chose "every app open" over "once per day" — deliberately more insistent than a once-a-day check-in, since a once-per-day gate would itself become exactly the kind of thing that's easy to forget/skip. He also chose "always free" for Not now over tracking repeat-defers, to keep the point about respecting his time rather than guilting him.

**Combines the existing Follow-ups-due and Space-Match lists into one queue** instead of two separate signals in two separate colors competing for attention — reuses `isFollowupDue`/`followupMsgFor`/`lead.followup_draft` and `isDeadPoolLead`/`isSpaceMatchSnoozed`/`spaceMatches`/`spaceMatchMsgFor` exactly as they already work elsewhere on the Leads tab; nothing about the underlying due-detection or drafting logic was reimplemented, only the surfacing UX changed.

**App side (`index.html`):**
- Two new modals, `#fugate-overlay` (the gate) and `#fuqueue-overlay` (the queue), same `.overlay`/`.modal` pattern as every other modal in the app.
- `checkFollowupGate()` builds `fuQueue` via `dailyQueueItems()` (follow-up items + space-match items, each `{lead, kind, draft}`) and shows the gate if non-empty. Wired to fire exactly once per page load via a new `_leadsFirstSnapshotSeen` flag inside the `leads` Firestore listener's callback — fires right after the first real snapshot populates `leads`, not on every subsequent live update (so a mid-session Firestore update from another device doesn't re-pop the gate mid-session).
- `openFollowupQueue()`/`closeFollowupQueue()`/`fuqAdvance()` step through `fuQueue` one item (`fuqIdx`) at a time. `renderFuqCard()` re-filters `fuQueue` against **live** `leads` data on every card render (not just at queue-open) — if a lead got handled elsewhere while you're mid-queue, it quietly drops instead of showing you stale info you'd have to notice was wrong.
- Four actions per card, each just calling the SAME existing handlers the old banners used (or the existing lead-card Archive control), then advancing: **✉️ Send** (`fuqSend` → `openFollowupEmail`/`openSpaceMatchEmail`, unchanged `mailto:` behavior), **📁 Archive** (`fuqArchive`, added 2026-07-10 — calls the same `toggleArchive(lead.id,'leads')` the lead card's own Archive button uses, not a new archive mechanism), **Snooze** (`fuqSnooze` → `snoozeFollowup`(3d)/`snoozeSpaceMatch`(14d), same durations as before), **Skip** (`fuqSkip` → just advances, no state change, so it reappears next time it's still due — deliberately not a hidden "handled" flag).
- **Archive's effect on the queue is asymmetric, by design.** `isFollowupDue()` has an early `if (lead.archived) return false;`, so archiving removes a lead from the Follow-up side of the queue immediately and for good. But `isDeadPoolLead()` is `!!(lead.archived || lead.wl_on || stage==='lost')` — archived leads are explicitly INCLUDED in the Space Match dead-pool watch list (that's the entire point of Space Matches: proactively reconnecting archived/dead/lost leads if matching space opens up later). So an archived lead can still resurface in a later queue as a **space-match** item if a real match exists; it's only guaranteed gone from the **follow-up** item type. The Archive button's tooltip states this explicitly so it's not a surprise.
- A "N of M" progress counter on the queue header, for the "I'm clearing something" feedback Justin's original banners never gave him.
- All seven new handlers exposed on `window.*` (ES-module gotcha, same as every other inline handler in this file).

**Not yet built (flagged, not started):** auto-generating the follow-up draft the moment a lead goes cold (currently still reuses whichever draft already exists — Claude-generated via the manual ✨ button, or the generic client-side template — rather than proactively calling workflow 17 the instant `followup_due` flips true); auto-staging a ready-to-send Outlook draft so "handling" a follow-up is one click in Outlook instead of a mailto redirect; and the broader Do/Wait-column rethink tabled back on 2026-07-06 (next-action + date per lead instead of a generic clock) — this gate/queue is a surfacing fix layered on top of the existing due-detection, not a replacement for that tabled architectural idea.

**To deploy:** pure app-side change, no n8n/Firestore rules involved. `git push` the updated `index.html`.

**Banners removed same session, once the gate covered the same ground.** After the gate/queue shipped, Justin's call was that the two banners were now redundant and should go, not stay as a second copy of the same signal. Removed entirely from `index.html`:
- The `#followup-banner` div and its build logic in `renderLeadsSection()` (the amber "N leads have gone quiet" banner + "Review follow-ups →" button). The **🔔 Follow-ups filter button + badge stays** — that's a browsing filter on the Leads tab, not a banner, and still useful independent of the gate.
- The `#space-match-banner` div, **and** its previously-separate `#space-match-toggle`/`#space-match-list` inline expand-to-browse mechanism (the green "N past leads now have matching space" banner + "Review matches →" + the `▼ Show N space matches` toggle). All three were part of the same passive-discovery surface the queue now replaces, so removing the banner alone would have left an orphaned toggle button with nothing prompting it.
- Dead code cleaned up alongside: `showSpaceMatches`/`toggleSpaceMatches()` (only ever called by the two removed UI elements) and its `window.toggleSpaceMatches` exposure — deleted rather than left as unused code, matching this project's usual cleanup convention when a feature is retired.
- `dueLeads`/`spaceMatchLeads` computations inside `renderLeadsSection()` were trimmed to just what the filter button still needs (`dueLeads`) — the space-match-specific `deadPool`/`spaceMatchLeads` locals were removed entirely since nothing else in that function used them once the banner/toggle/list were gone. (The gate/queue's own `dailyQueueItems()` computes its own independent lists — it never shared state with `renderLeadsSection()`.)
- Per-lead card blocks (`renderFollowupBlock`, `renderFollowupDraftBlock`, `renderSpaceMatchBlock`, shown when you expand an individual lead's card) were left untouched — those are still useful when you open a card for some other reason, and the queue itself reuses their underlying draft functions rather than duplicating them.

---

## 2026-07-15 — No hardcoded sign-off on lead emails + full lead story in the daily queue

Two small fixes to the leads follow-up email flow, from reviewing the drafts Justin actually gets: he already has an email-client signature, so a hardcoded "Best, Justin Cho / Business Development, Cubework" was a duplicate; and the daily follow-up queue (2026-07-10 above) only ever showed the generated subject/body, with no way to see the lead's actual details or update history that supposedly produced it.

### Sign-off removed everywhere a lead/follow-up email is generated
- **n8n workflow 17 (Generate Lead Follow-up)** — the `Build Claude request` system prompt (in both `n8n/17_lead_followup_generator.json` and its paste-ready mirror `n8n/lead_followup_generator.nodes.js`) had an explicit instruction: "4) Signs off on its own line, exactly: Best,\nJustin Cho\nBusiness Development, Cubework." Replaced with an instruction to end right after the closing question and NOT add any sign-off, closing line, or name, since the email client already appends one. Also dropped the now-stale "not counting the sign-off" clause from the 100-word body-length instruction.
- **Client-side generic templates (`index.html`)** — `FOLLOWUP_BY_STAGE`'s four stage templates, the universal `FOLLOWUP_VALUE`/`FOLLOWUP_BREAKUP` templates, and `spaceMatchMsgFor`'s re-engagement draft all interpolated a shared `FU_SIGN` constant (`'\n\nBest,\nJustin Cho\nBusiness Development — Cubework'`). Emptied `FU_SIGN` to `''` rather than touching each of the ~7 call sites — every one of those drafts now simply ends at its closing question/line with no signature block.
- **Not touched:** `n8n/22_property_outreach_draft.json`'s `DEFAULT_PO_SYSTEM_PROMPT` (and its `index.html` mirror) still signs off "Best,\nJustin Cho\nBusiness Development, Cubework" — that's the Prop Outreach cold-draft engine, a separate tab/feature Justin didn't raise this time. Revisit if he wants the same change there.
- **To deploy:** open the existing **"CW — 17. Generate Lead Follow-up"** workflow in n8n and use its own **⋯ → Import from File** (in place, not from the workflows list — same anti-duplicate-webhook convention as every other re-import in this project) pointing at the updated `17_lead_followup_generator.json`, so the Anthropic/Microsoft/Google credential bindings carry over untouched. `git push` the updated `index.html` for the `FU_SIGN` change to go live.

### Daily follow-up queue now shows the lead's full story, not just the draft
Justin's ask: when reviewing a generated follow-up in the one-lead-at-a-time queue (`openFollowupQueue`/`renderFuqCard`), he wants to see all of that lead's information — the logged updates and "story" — that the draft is supposedly based on, not just the subject/body in isolation.

- New `renderFuqStory(lead)` renders a collapsible **"📖 Lead info & story (N updates)"** section above the subject/body fields in the queue card: the same read-only `renderLeadDetails(lead)` panel shown on a normal lead card (company/contact/email/phone/location/sqft/unit/rate/etc.), plus every one of the lead's logged entries (not just the last 5 `leadNotesContext`/`renderFollowupBlock` use elsewhere) sorted newest-first in a scrollable box — each entry shows its date, kind/Do-Wait/done state, full text, and a one-line summary of any attached notes (📝 text / 🎤 voice / 📎 file, without the full interactive note UI — this is a read-only review view, not the editable lead card).
- Defaults **expanded** (`fuqStoryOpen`, module-level state, persists across cards in a session) since visibility was the entire point of the ask; a header row toggles it via `toggleFuqStory()` (re-renders the current card) if it gets long on an old lead.
- The `#fuqueue-overlay` modal was widened (480px → 560px) and given `max-height:88vh;overflow-y:auto` to fit the extra content without the modal overflowing the viewport.
- New handler `toggleFuqStory` exposed on `window.*` (ES-module gotcha, same as every other inline handler here).
- **To deploy:** pure app-side change, no n8n/Firestore rules involved. `git push` the updated `index.html`.

**Changed files:** `n8n/17_lead_followup_generator.json`, `n8n/lead_followup_generator.nodes.js`, `index.html` (`FU_SIGN`, `renderFuqCard`/`renderFuqStory`/`toggleFuqStory`, `#fuqueue-overlay` modal sizing).

**Follow-up, same day: queue story gained a "drafted from" hint + the email thread link.** Justin asked whether the story section also surfaces the actual email thread the draft was based on. It doesn't include the parsed thread content (that text is never persisted — workflow 17 pulls it live from Graph, hands it to Claude, and only the resulting draft gets written back to Firestore, so there's nothing to display after the fact), but a link out to the real Outlook thread is cheap since that data already exists on the lead. Added `renderFuqSourceLine(lead)` (new, right after `renderLeadDetails` inside `renderFuqStory`): shows a small "✨ Drafted from the email thread / your logged notes / the email thread + your notes" hint off the already-stored `lead.followup_draft.source` field, plus the lead's `emailLinkChips(lead,'leads')` — the exact same compact resolved/unresolved thread-link chip the lead card's own collapsed summary uses, so it's the same `fixThreadUrl()`-repaired, click-to-open-Outlook behavior, nothing new built. No n8n change. **To deploy:** `git push` the updated `index.html`.

---

## 2026-07-20 — 🔗 Links tab (SharePoint/Google Sheet link directory)

Justin's ask: "I find myself constantly going to different sharepoint links to find docs and stuff. Would rather have everything in do or wait." New tab, positioned right after 🗂️ Roster in the tab bar, backed by a new Firestore collection **`sharepoint_links/{id}`** = `{id, label, url, category, notes, createdAt, updatedAt}`.

**Editable, not read-only** — the whole point is a link hub Justin can grow over time without a code change each time he wants to add one, so it's a full CRUD list (add/edit/delete), same interaction shape as the 💡 Notes tab (`persistItem`-style `setDoc`, `deleteDoc` for removal — no soft-delete `_deleted` flag here, a link directory doesn't need undo history the way Notes' trigger/why/fix entries do).

**Seeded once, on first load, from data already in this repo — not invented.** `SPLINKS_SEED` (in `index.html`) is a hardcoded array covering the 8 per-location links already collected in `sharepoint_location_links.txt` (category `Location Sheet`) plus the Renewals Google Sheet URL (pulled from `n8n/10_renewals_sync.json`'s hardcoded export URL, category `Renewals`). `seedSplinksIfEmpty(snap)` runs off the collection's live listener and only fires `setDoc` for each seed row the first time the collection snapshot comes back empty — so it's a one-time, client-side seed from whichever staff account happens to load the tab first, not an n8n workflow or a Firestore console import. **Deliberately did NOT seed placeholder entries for the Pricing/Rate Bands/Roster source sheets** — this repo only has their Graph `driveId`/`itemId`/site-name references (workflows 7/18/19), not a normal browsable `sharepoint.com` URL, and inventing one would be worse than leaving it blank. Justin can add those himself via ＋ Add link once he has the actual links handy.

**App side (`index.html`):** `renderSplinks()` — search box + a category dropdown (built from whatever categories actually exist in the data, not a fixed list) + grouped, alphabetized rows; each row is just a clickable label (opens the URL in a new tab) with optional notes underneath, and Edit/Delete buttons. `openAddSplink()`/`editSplinkModal()`/`saveSplinkModal()`/`deleteSplink()` handle the CRUD, `#splink-overlay` is the add/edit modal (same `.field`/`.modal-foot` markup pattern as the PA/Ownership modals). Category is a free-text input with a `<datalist>` of existing categories (so it's easy to reuse "Location Sheet" etc. without it being a rigid enum).

**Access — NOT opened up to Lucy (Avail-only role), unlike Roster.** First pass mirrored Roster's "just a directory, not sensitive" reasoning and opened it to her too, but Justin's call: she doesn't need it, and adding another `firestore.rules` match block + a console publish step for a tab she won't use isn't worth it. `lockToAvailReadOnly()` does NOT include `sharepoint` in its allow-list (only `avail`/`roster`), so the tab is hidden for her the same way Leads/Notes/Outreach/Pricing/Rate Bands are — and since there's no explicit rule for `sharepoint_links/{id}`, it falls through to the existing catch-all (`isAllowed()`-only), which already covers full read/write for the 3 staff accounts with zero rules changes needed. The ＋ Add link button and every row's Edit/Delete still carry `AVAIL_READONLY` guards defensively (harmless, matches the pattern used everywhere else) even though she'll never reach this view.

---

## 2026-07-20 (cont.) — Follow-up availability quoting: two separate bugs, not one

Justin reported the Leads daily follow-up queue (2026-07-10 above) was still quoting specific availability ("I've got 989 sf open right now at...") after re-importing the fixed **workflow 17** (which got the "AVAILABILITY AND PRICING HONESTY" rule back on 2026-07-07 — see that session). Investigating turned up two genuinely separate things, not one bug:

1. **The live n8n workflow was several versions stale.** Justin uploaded the workflow's current n8n export for comparison; diffing it against the repo's `17_lead_followup_generator.json` showed the running workflow was missing not just the 2026-07-07 availability/pricing-honesty + stage-guidance rules, but also the 2026-07-15 hardcoded-sign-off removal — it still had "4) Signs off on its own line, exactly: Best,\nJustin Cho\nBusiness Development, Cubework" baked in. Confirms the file was never actually re-imported after 2026-07-07 despite the note in that session. Re-imported and confirmed fixed.
2. **The daily queue can also surface a completely different, client-side template that workflow 17 has never touched.** `dailyQueueItems()`/`renderFuqCard()` use `lead.followup_draft` (the Claude/workflow-17 draft) ONLY if one has already been generated for that lead; otherwise they fall back to `followupMsgFor(lead)` — a plain JS template (`FOLLOWUP_BY_STAGE`/`FOLLOWUP_VALUE`/`FOLLOWUP_BREAKUP`) that has never gone through n8n or Claude at all. That template had its own **`fuAvail()`** helper (`followupUnitStillOpen`/`followupAvailMatches`) that deliberately injected a real, live-checked open unit into the email — a genuine feature, not stale data, but still exactly the kind of specific unit/sf quoting Justin doesn't want in these follow-ups. The screenshot he sent (Jorge Gomez, 🔔 Follow-up, no "✨ Drafted from" line) confirmed this was the path actually hit — no cached `followup_draft` existed yet for that lead, so it was 100% the generic template, unrelated to the n8n reimport.

**Fix (`index.html`):** removed `fuAvail()`/`followupUnitStillOpen()`/`followupAvailMatches()` entirely (dead code cleanup, not just unused) and stripped the `${fuAvail(l)}` interpolation out of all 5 templates that had it (`cold`/`contacted`/`toured`/`proposal` stage templates + `FOLLOWUP_VALUE`). `followupTargetSf()` was kept — it's still used by the separate, deliberate **Space Match** feature (`spaceMatches()`, 2026-07-03 session) to size-match archived/dead leads against live inventory, which is NOT the same thing being fixed here (Space Match's entire point is surfacing real open space to a dead lead) and was left untouched.

**Also fixed in the same pass, found while in there:** the `contacted` template said "Happy to hold a unit briefly while you decide" and the `toured` template said "And I can hold {unit} for you while you decide" — both violate the existing no-holds-before-agreement rule (never offer to hold space in client-facing copy; holds only happen after a signed agreement). Replaced with "Want me to set up a quick tour?" (contacted) and removed the hold sentence entirely (toured).

**To deploy:** pure app-side change, no n8n involved this time (that half was already fixed via the re-import). `git push` the updated `index.html`.

**Changed files:** `index.html` (`FOLLOWUP_BY_STAGE`, `FOLLOWUP_VALUE`, removed `fuAvail`/`followupUnitStillOpen`/`followupAvailMatches`).

**Follow-up, same day: dashes slipped back in, and the "contacted" CTA didn't fit.** Justin's actual Jorge Gomez draft still had an em dash in `FU_INCLUSIVE` ("Pricing's all-inclusive — one flat rate...") — the fix above touched the availability/hold issues but didn't re-check the surrounding copy against the existing no-dashes rule ([[no-dashes-in-copy]]), so it slipped back in. He also flagged the "Want me to set up a quick tour?" CTA on the `contacted` template as presumptuous ("why would i ask for a tour right now") — asking for an in-person visit isn't the right ask at every contacted-stage lead, especially early or out-of-area ones.

**Fix (`index.html`):** removed every em/en dash from the client-side follow-up value props and templates: `FU_INCLUSIVE` (now uses a colon), `FU_FAST`/`FU_TCO` (number ranges spelled out as "X to Y" instead of an en-dash range), `fuHook`'s importer/3pl cases (restructured with commas), all 5 `FOLLOWUP_BY_STAGE`/`FOLLOWUP_VALUE`/`FOLLOWUP_BREAKUP` subjects and bodies (dropped the decorative "Cubework — " subject prefixes entirely rather than swap the punctuation, split dash-joined clauses into two sentences), and `spaceMatchMsgFor`'s subject/body (same pattern). Also softened the `contacted` stage CTA from "Want me to set up a quick tour?" to "Still worth exploring, or is timing off right now?" — an open question that doesn't presume the lead is ready for a site visit.

**To deploy:** pure app-side change. `git push` the updated `index.html`.

**Follow-up, same session: `FOLLOWUP_BREAKUP` body rewritten after a few rounds of copy review.** Justin workshopped the last-touch ("Break-up") email body live in chat. The original was stiff ("regarding your interest," "learning more about our spaces"); a first MTM/fast-move-in rewrite bunched too many hyphenated compounds into one sentence (month-to-month, long-term, lock-in, move-in all together read cluttered even though none were actual em/en dashes); and a closing line, "is this a good place to leave it?", read awkward. Landed on: "Didn't want to stop reaching out without one more check in. If your plans ever call for space, ours is flexible: no long commitment required, and we can move you in within a day or two. Feel free to reach out whenever the timing's right, whether that's now or down the road." Justin's call: "simple and dependable to start with." `FOLLOWUP_BREAKUP` in `index.html` now uses this body verbatim (subject unchanged, "One last note").

**To deploy:** `git push` the updated `index.html`.

**To deploy:** just `git push` the updated `index.html` — no Firebase console step needed (no `firestore.rules` change this time, since the catch-all already covers it). First time any staff account opens the tab after that, the 9 seed links populate automatically.

---

## 2026-07-20 (cont. #2) — Explicit template picker in the daily follow-up queue, new "Nudge" template

Justin asked for named "Breakup"/"Nudge" buttons on the follow-ups. Turned out the per-lead-card follow-up block already has a template system (`followupMsgsFor`/`followupTplIdx`, cycled one at a time via the "↻ {label}" button — see 2026-06-10 session) — he just hadn't noticed it, and separately wanted the same picking ability inside the **daily follow-up queue modal** (2026-07-10 session), which had no way to switch templates at all before sending.

**New `FOLLOWUP_NUDGE` template** — a standalone-message option (`followupMsgsFor` now returns `base.concat([FOLLOWUP_NUDGE, FOLLOWUP_VALUE, FOLLOWUP_BREAKUP])`), a light, low-pressure bump distinct from the stage-specific first message and from Break-up: no value props, no ask beyond "any update since last time."

**Explicit picker in the queue (`renderFuqCard`, `index.html`).** Added `pickFollowupTpl(leadId, idx, e)` — writes directly to the SAME `followupTplIdx` map the per-card "↻ cycle" button already uses, then re-renders just the queue card (`renderFuqCard()`, not the whole Leads tab). Because `openFollowupEmail`/`snoozeFollowup` both call `followupMsgFor(lead)` again when you actually act on a queue item, whichever template you pick in the queue is exactly what gets sent/snoozed — no separate state to keep in sync. The queue card now shows a row of named buttons (one per template: the stage-specific one, Nudge, Value-add, Break-up), current selection highlighted, right above the Subject/Body preview. **Only shown for `kind==='followup'` items that do NOT already have a cached Claude draft** (`lead.followup_draft`) — a cached draft is a single message from workflow 17, not a set of templates to switch between, so the picker would be meaningless there.

**Not yet done:** Justin flagged general dissatisfaction with the wording on "each one" of the existing templates (cold/contacted/toured/proposal/value-add) but didn't specify which — parking that until he reviews them through the new picker and gives specific feedback per template, same iterative pattern used to land the Break-up copy earlier this session.

**To deploy:** pure app-side change. `git push` the updated `index.html`.

**Changed files:** `index.html` (`FOLLOWUP_NUDGE`, `followupMsgsFor`, `pickFollowupTpl`, `renderFuqCard`).

**Follow-up, same session: Nudge body workshopped through a few rounds, same live-copy-review pattern as Break-up.** First draft ("Just wanted to bump this back up...") was rejected outright. Iterated on a "just checking in" substitute without using that banned phrase or the word "reply" (which read awkward paired with "no need to"). Landed on: "Wondering if anything's changed on your end since we last talked. Feel free to call or text me with any questions or concerns." — honest, low pressure, gives an easy channel (phone) instead of just an email reply. `FOLLOWUP_NUDGE` in `index.html` now uses this body verbatim (subject unchanged, "Quick nudge").

**To deploy:** `git push` the updated `index.html`.

**Follow-up, same session: "Value-add" replaced with "Random value".** Justin's ask, once he had Breakup and Nudge in place: a third option, "random value", instead of the old `FOLLOWUP_VALUE` template that bulleted all 4 value props (all-inclusive pricing, month-to-month flex, fast move-in, segment hook) in one message. Confirmed he meant a full replacement, not a 4th addition. New `fuRandomValueLine(l)` picks ONE of those 4 at random every time the template is built (re-rolls on each render/regenerate, not fixed per lead) and the body features just that single line instead of a bulleted list, so repeated touches over time surface a different angle rather than repeating the same full dump. `FOLLOWUP_VALUE` constant name kept as-is (only referenced by `followupMsgsFor`), but its label is now "Random value" and subject is "One thing worth knowing".

**To deploy:** `git push` the updated `index.html`.

**Follow-up, same session: Random value replaced again, with real anonymized case studies + concrete numbers.** Justin's reaction to the single-value-prop line: "too short, doesn't really give the person any information." He asked for short case-study style stories instead ("Company A had overflow blah blah"), then wanted them to feature real numbers since "business people like numbers." Confirmed all four scenarios below have actually happened with real Cubework clients (not hypothetical), so they're grounded in the Sales Playbook's own published figures rather than invented stats: 60x faster move-in (2-day vs 3-6 month), ~75% less upfront capital, ~20-30% lower all-in TCO, and the playbook's own 10,000 SF TCO example ($160k vs $125k/yr, $175k over 5 years). Also discussed and dropped the initial "Company A/B/C/D" labeling as reading too generic/invented-sounding for real customer stories — landed on natural anonymized phrasing instead ("one of our clients," "a growing ecommerce brand we work with," etc.). Each case study ends with a direct invitation to reach Justin ("Feel free to call or text me anytime..."), since the point of resurfacing value later in a cadence is inviting an actual conversation, not just a stat drop.

New `FU_CASE_STUDIES` array (4 entries, one per pain point: speed/overflow, ecommerce flexibility+cost, 3PL upfront capital, TCO comparison) + `fuRandomCaseStudy()` picks one at random per build, replacing the short-lived `fuRandomValueLine`/single-value-prop version from earlier this session. `fuCap()` (only used by the old value-prop version) was removed as dead code, per this project's usual cleanup convention.

**To deploy:** `git push` the updated `index.html`.

**Follow-up, same session: closing line on the Random value template tightened.** Justin workshopped the CTA closing "Feel free to call or text me anytime if you want to learn more about how this could work for you" — landed on "...how Cubework could help with your plans" instead, a small wording tweak (help vs. work for you, tied to "your plans" specifically).

**To deploy:** `git push` the updated `index.html`.

**Follow-up, same session: case study selection now driven by lead size, not pure random.** Justin's ask: "the size should determine which random value template to show." Each of the 4 `FU_CASE_STUDIES` entries got a `maxSf` tag matching the situation it best fits (small overflow need ≤2,500 sf, mid-size seasonal ecommerce fulfillment ≤7,500 sf, the playbook's own 10,000 SF TCO example ≤15,000 sf, and a larger 3PL adding a full second site above that). `fuRandomCaseStudy(lead)` now takes the lead and reuses the existing `followupTargetSf(lead)` heuristic (same one Space Match uses) to pick the closest-matching entry; only falls back to a fully random pick when the lead has no size on file at all (`followupTargetSf` returns 0). Thresholds are a first pass based on which story reads like the right scale, not a hard rule from Justin, so flag if any lead's story doesn't feel like the right fit and the buckets can be adjusted.

**To deploy:** `git push` the updated `index.html`.

**Changed files:** `index.html` (new 🔗 Links tab, `sharepoint_links` listener/seed/render/CRUD).

---

## 2026-07-20 (cont. #3) — 3rd Prop Outreach property: 2680–2690 Pellissier, with a "last activity before May 2026" filter

Justin uploaded a fresh Yardi "Leasing Activity Report" (39 rows, all `COI Pellissier 2680 2690`) and asked for it to go into Property Outreach — a **new**, third property, distinct from the existing `pellissier-2720`. Confirmed via `list_locations` (Firestore MCP connector) that `2680-2690 Pellissier Pl` has no Avail/`config/properties` entry at all — unlike Ontario Airport and 2720 Pellissier, both of which were already-onboarded Avail locations before their Prop Outreach datasets were added. Assigned it a new id, **`pellissier-2680-2690`**, following the same slug convention; Property Outreach doesn't require a matching Avail entry to function (no live availability data feeds its talking-points panel until/unless the location is separately onboarded to Avail).

**New filter, specific to this import: exclude anything with recent activity.** Justin's ask: "Don't want anything where the last activity was recent may 2026. Anything after that nope" — i.e. only import leads last touched **before May 1, 2026** (deals still being actively worked in the last ~2.5 months are excluded; this batch is specifically meant to re-engage cold ones). Applied as a one-time filter in the parsing script, not a reusable app feature. **17 of 39 rows excluded**, 22 imported. One row (Mike Zhou) had a blank `Last Activity Date`; fell back to `Deal Modified` (11/26/2025) since it's well before the cutoff regardless.

**Same wave logic as Ontario Airport / 2720 Pellissier** (Tour→Wave 1, Dead Deal→Wave 4, Lead bucketed by SF ≥/< 5,000 into Wave 2/3, no SF found→Wave 5; SF pulled from the sheet's own `Area` field when nonzero, else parsed from free-text Deal Notes and flagged `sqft_est:true`). This export had **zero Dead Deal rows** (Stages were only Lead/Tour), so Wave 4 is empty here. Final counts: Wave 1→1, Wave 2→5, Wave 3→3, Wave 4→0, Wave 5→13. Contact/phone fill 22/22, email 18/22 — already filled in by the Yardi export, same as 2720 Pellissier (unlike Ontario's, which needed a separate backfill).

**Two data quirks flagged, not fixed** (same "flag, don't silently correct" convention as the prior two migrations): Allie Darmanian-Harris's Deal Notes list two overlapping size-range checkboxes with no way to tell which was actually selected — left `sqft` unset, filed Wave 5. Maryam Kilani's Deal Notes reference "Stimson" (347 Stimson, a different Cubework property) — kept the sf=1,500 from the sheet's own Area field as-is, flagged the mixup rather than guessing which property it actually concerns.

**Files:** `Sales/Pellissier_2680_2690/pellissier_2680_2690_property_outreach_payload.json` (22 items, `bulk_create` into `property_outreach`) + `pellissier_2680_2690_summary.md` (full writeup, same shape as the Ontario Airport / 2720 Pellissier summaries).

**Encoding gotcha hit on first load attempt.** The first PowerShell `Invoke-RestMethod` call failed with a 422 "Failed to parse request body" JSON syntax error. Root cause: the payload's free-text `source_notes` (copied verbatim from the Yardi export's Deal Notes) contained a few smart-quote/em-dash/en-dash characters (curly `'`, `—`, `–`), and Windows PowerShell's default `Get-Content -Raw` text encoding mangles those non-ASCII characters on read, corrupting the JSON before it's even sent. Fixed by stripping them to plain ASCII (`'`→`'`, `—`/`–`→` - `) directly in the payload file — cleaner than relying on everyone remembering `-Encoding UTF8` on every future `Get-Content` call. **Worth remembering for any future Yardi/CSV-sourced payload**: sanitize non-ASCII punctuation before handing off a PowerShell-loaded JSON file.

**Loaded successfully** (2026-07-20) via n8n workflow 20 (Firestore Writer) — `count: 22` confirmed. 2680–2690 Pellissier now shows up as a third collapsible property card on the 🏢 Prop Outreach tab (no `index.html` change was needed — the tab already renders whatever properties exist in the collection).

---

## 2026-07-20 (cont. #4) — 4th Prop Outreach batch: CA Banana (11179 Banana, Fontana) — existing Avail location, first Property Outreach dataset

Justin uploaded a second fresh Yardi "Leasing Activity Report" the same day (`Leasing Activity Report_2026-07-20_12-10-23.csv`, 90 rows, all "CA Banana") and confirmed via AskUserQuestion: same treatment as the Pellissier imports (Property Outreach, same wave logic) and the same "exclude last activity May 2026 or later" cutoff.

**Unlike both new Pellissier buildings, this is an already-onboarded 🏭 Avail location** (`11179-banana-fontana` — confirmed via `list_locations`), so the existing id was reused rather than minting a new one. This is the same shape as the very first migration (Ontario Airport, `3950-airport-ontario`) — an existing Avail property gaining its first Property Outreach call-sheet dataset, not a brand-new property.

**Same filter, same wave rule.** 48 of 90 rows excluded (Last Activity Date May 2026 or later, one row's blank date fell back to Deal Modified 2/18/2026 — still well before the cutoff). 42 rows imported. Wave counts: 1→1 (Tour), 2→9 (Lead, sf≥5,000), 3→4 (Lead, sf<5,000), 4→5 (Dead Deal), 5→23 (Lead, no sf found). sf pulled from the sheet's own `Area` column when nonzero, else parsed from free-text Deal Notes and flagged `sqft_est:true` (e.g. "3000 sf warehouse", "15,000 to 20,000 sq. ft.", "1k SF of office space", "30k-35k SF"). Contact fill 41/42 phone, 40/42 email — already present in the export, no backfill needed.

**Learned the lesson from the last encoding bug — sanitized proactively this time**, not after a failed load. Built the payload with a Python script (not hand-copied) that strips smart quotes/em-dashes/en-dashes to plain ASCII at generation time, then verified zero non-ASCII characters remained in the final JSON before writing it to disk. No PowerShell 422 this time.

**Data quirks flagged (not fixed), same convention as every prior migration:**
- **Nicholas Sablock / Nick Sablock — likely the same person, two separate deal records.** Identical phone number (562-293-6007); one record (Stage: Lead) uses email `nick.sablock@colliers.com`, the other (Stage: Dead Deal) uses `nick.sablock@am.jll.com` — possibly a job change (Colliers → JLL) logged as a new record instead of an update, or a straight duplicate. Kept both, matching the source system, filed at their own Stage-derived waves (5 and 4).
- **Francisco Escobar and Nicholas Sablock** both carry Stage="Lead" but a populated Dead Deal Reason field ("Price too High" / "No Longer Interested") — normally that field only appears on Dead Deal rows. Treated Stage as authoritative (both Wave 5, not Wave 4) per the standing rule, but preserved the stale reason in `source_notes`.
- **Stephanie Cheng** — Deal Notes reference a portfolio-scale survey figure ("3PL /500K-800K"), not a realistic single-building ask; not used as `sqft`, filed Wave 5.
- **Wilson Yun** — this is a **purchase/buy** inquiry ("I have a buyer interested... They want to buy a warehouse"), not a lease inquiry. Kept in the list (real contact, reached out) but flagged since a lease-focused cold outreach isn't the right angle here.
- **FalconX Carriers LLC** — Lead Source = "Current Tenant - Relocation," possibly an existing/former tenant looking to relocate rather than a cold prospect — a warmer signal worth noticing even though it's still filed Wave 5 (no sf on file) per the standard rule.
- **Patricia Romero and Ed Whittemore** — both inquiring about the entire ~96,000+ SF building (96K / 96,224 SF respectively), different brokers, both filed Wave 2 per the sf≥5,000 rule but clearly a different scale of ask than the rest of the list.

**Files:** `Sales/Banana_Fontana_070726/banana_fontana_property_outreach_payload.json` (42 items, `bulk_create` into `property_outreach`) + `banana_fontana_summary.md`.

**Loaded successfully** (2026-07-20) via n8n workflow 20 (Firestore Writer). CA Banana / Fontana now shows up as a fourth collapsible property card on the 🏢 Prop Outreach tab, alongside Ontario Airport and the two Pellissier buildings (no `index.html` change needed — the tab already renders whatever properties exist in the collection).

---

## 2026-07-20 (cont. #5) — 📍 Nearby Prospects tab: per-site discovery of NEW nearby businesses (CoreClaw)

Justin's ask: a per-site tool that finds NEW nearby businesses to cold-outreach around each Cubework property (importers, distributors, 3PLs, ecommerce fulfillment, manufacturers, etc — the kind of business that might need warehouse/flex industrial space) — distinct from both 📧 Outreach (dead Apollo-based industry prospecting; **the Apollo subscription is now cancelled**, do not use or reference Apollo going forward) and 🏢 Prop Outreach (Yardi call-sheet backlogs of EXISTING tenant leads at a property, not new-business discovery). An earlier idea to build this on Google Places API directly was superseded — Places only returns a business's main phone/website, no named contact/email, which would leave the "who do I actually email" gap wide open.

**Data source: CoreClaw (coreclaw.com), specifically its Google Maps Scraper Worker (slug `01KPD6M5YQADCQKGVKPDZVYC63`).** CoreClaw is a pay-per-successful-result web scraping platform with ready-made "Workers" and a REST API. The Google Maps Scraper searches by keyword + location and returns the base business record (name/address/phone/website/category/rating/lat-lng), and critically has a FREE toggle called **"business leads enrichment"** that adds a named contact (full name, job title), work email, phone, and LinkedIn profile per business — this is what actually solves the "who do I email" problem. Pricing: $3/1,000 results on the free tier ($1.80/1,000 on the $3/mo Starter plan); the enrichment toggle itself costs nothing extra. Auth: header `api-key: YOUR_API_KEY` (there's also a newer `Authorization: Bearer` v2 form, but the plain header style was chosen to match this project's own convention below).

**Pilot site: Ontario Airport** (`3950-airport-ontario`, `3950 E Airport Dr, Ontario, CA 91761`), but the feature is built generically (any propId) — same convention as every other per-property tab in this app (Avail, Roster, Rate Bands, Prop Outreach). Nothing about the workflow or the tab is Ontario-specific.

**Category list (v1, tunable later — same "start with a reasonable default, adjust in code as needed" pattern as every other category list in this codebase):** `importer`, `distribution company`, `wholesale distributor`, `3PL logistics company`, `freight forwarder`, `ecommerce fulfillment center`, `trucking company`, `manufacturer`, `moving and storage company`. One CoreClaw run per category (not one combined query) so each result can be tagged with which search surfaced it (`search_category`).

**No community node — plain HTTP Request nodes, matching this project's own convention.** This project has never installed an n8n community node anywhere — Apollo/Anthropic/Microsoft Graph/Firestore are all called via the generic HTTP Request node with credentials, not community packages. Workflow 25 does the same for CoreClaw rather than installing `n8n-nodes-coreclaw`, so Justin never needs to install anything new in n8n.

**Golden rule followed from CoreClaw's own docs: never hardcode a Worker's parameter names or version.** Every run starts with `GET /api/scraper?slug=...`, reading `data.version` and `data.parameters.custom.properties` (an array of `{name,type,title,required,default?}`) live, then matching candidate field names by substring (keyword/query/search_term for the search term, base_location/location/city for the location, business_leads_enrichment/leads_enrichment/enrichment for the contact-enrichment toggle) rather than assuming exact keys. If a match can't be confidently made, the workflow logs a warning in the n8n execution log (visible on the first real test run) and falls back to the best-guess key so a run still attempts something useful.

### ⚠ UNVERIFIED — read before the first real run
**No CoreClaw account existed at build time.** Every field name in this workflow — the Worker's own custom-parameter names, the base result record's field names (`title`/`address`/`cid`/`data_id`/etc — these came from a generic sample in CoreClaw's docs), the pagination response shape (`has_more`/`total`/`page_index` — guessed), and especially the **leads-enrichment contact fields** (`lead_name`/`lead_title`/`lead_email`/`lead_linkedin_url` — a pure best guess, since the docs' sample response didn't have enrichment turned on) — is built from CoreClaw's prose documentation, fetched live in June 2026, not from a real `/api/scraper` schema response or a real result record. This is the same class of risk the Rate Bands/Roster parsers had back on 2026-07-07 (a visual dashboard sheet that a flattened text pull scrambles) — **except those got a real `usedRange()` fixture pulled and verified against before shipping** (see `n8n/pricing_bd_raw_grid.json` / `n8n/roster_raw_grid.json`), and this one could NOT get that same treatment. That gap is stated here plainly, not glossed over. **The first time Justin actually clicks "🔎 Discover nearby companies," go check the real n8n execution log**: look at "Get scraper schema"'s output to confirm/correct the 3 matched parameter names in "Match schema fields," and look at a real "Get results page" response to confirm/correct the base-record and leads-enrichment field picks in "Tag + accumulate results" (`n8n/site_outreach_discovery.nodes.js`, same node). Fix and re-import as needed — this is the expected first-run follow-up, not a surprise.

### Idempotency design — why `bulk_update`, never `bulk_create`
A core requirement: **re-running discovery for a site must never reset Justin's own work** — `status`, `notes`, `attempts[]`, `archived` on an already-tracked company. Before writing anything, read `n8n/20_firestore_writer.json` (the generic Firestore writer, already LIVE and used by three other migrations this month) to see exactly what its actions do at the Firestore-commit level. Finding: `bulk_update`'s "Build batchWrite" node builds each write as a Firestore `update` with `updateMask.fieldPaths = Object.keys(fields)` — a genuinely masked, partial write. Firestore's update+updateMask semantics: for an existing doc, ONLY the listed field paths are touched (everything else — `status`/`notes`/`attempts`/`archived`/`promoted_lead_id` — is left completely alone); for a doc that doesn't exist yet, the same write auto-creates it with just those fields. This is **exactly** the idempotent write this feature needs, so workflow 25 always calls workflow 20's `firestore-write` webhook with **action `bulk_update`, never `bulk_create`** — `bulk_create` would build a plain `update` with no mask at all (touching every field, i.e. capable of stomping status/notes on a re-run) which is the wrong tool here. The write payload only ever includes the CoreClaw-sourced fields (`location`, `place_cid`, `name`, `address`, `city`, `state`, `website`, `phone`, `search_category`, `google_category`, `all_categories`, `latitude`, `longitude`, `rating`, `review_count`, `contact_name`, `contact_title`, `contact_email`, `contact_linkedin`, `last_seen_at`) — so `status`/`notes`/`attempts`/`archived`/`promoted_lead_id`/`discovered_at` are simply ABSENT from every write, and the app defaults missing `status`→`'not_contacted'`, missing `archived`→`false`, missing `attempts`→`[]` at read time, same convention already used for Roster/Rate Bands' optional fields. `discovered_at` (first-seen only) is deliberately left out of every write too, including the very first one — see the `site_outreach/{id}` collection doc above for how the app covers this without a second conditional-write pass in n8n.

### n8n workflow 25 — `n8n/25_site_outreach_discovery.json` (+ `n8n/site_outreach_discovery.nodes.js`, NEW, not yet imported)
Manual webhook only, path `site-outreach-discover`, `responseMode: responseNode` (synchronous — Justin clicks a button and waits, same pattern as `roster-sync`/`pricing-bd-sync`, NOT fire-and-forget like the daily-sync cron webhook). Input: `{propId, address, categories?}` (categories optional override array; defaults to the 9-item list above). Flow: **Parse request** (derives a CoreClaw `base_location` — "City, ST, USA" — from the raw address via a regex on the `City, ST ZIP` tail, e.g. "3950 E Airport Dr, Ontario, CA 91761" → "Ontario, CA, USA"; falls back to the raw address string rather than failing the whole run if the regex doesn't match) → **Get scraper schema** (once, `GET /api/scraper?slug=...`) → **Match schema fields** (the substring-matching described above, logs warnings) → **Reset accumulator + fan out categories** (clears `$getWorkflowStaticData('global').rows`, same cross-loop-accumulator pattern as workflow 18's tab loop, then emits one item per category) → **Batch categories (loop)** (`splitInBatches` batchSize 1, same v3 loop-off-index-1 convention as workflows 6/18: "done"/index 0 feeds the post-loop dedupe step, "loop"/index 1 feeds the per-category work) → per category: **Build custom params** (assembles `input.parameters.custom` from the live-matched field names) → **Run scraper** (`POST /api/v1/scraper/run`, `is_async:true`, `continueOnFail`) → **Extract run_slug** (checks the JSON body's own `code` field explicitly — CoreClaw docs warn a non-zero `code` means failure even on HTTP 200) → **Run started ok?** (IF — a failed run skips straight back to the category loop rather than polling) → **Wait before poll** (15s) → **Get run detail** (`POST /api/v1/run/detail`) → **Check poll status** (status 1 Ready/2 Running/3 Succeeded/4 Failed/5 Aborting; capped at 20 attempts × 15s = 5min per category — same capped-deadline convention as the Avail Export feature — then treated as a soft failure for THAT category only, not a hard workflow failure) → **Poll done?** (IF — loops back to Wait before poll while still running) → **Prep result paging** → **Get results page** (`POST /api/v1/run/result/list`, page_size 100) → **Tag + accumulate results** (tags each row with `search_category`, extracts the base + enrichment fields — see UNVERIFIED note above — and appends to the shared accumulator; also decides whether another page is needed, pagination field names also UNVERIFIED) → **More pages?** (IF — loops back to Get results page, else returns to the category loop) → once every category is done: **Dedupe accumulated rows** (by `cid`, falling back to `data_id` — same key used as the Firestore doc id; first occurrence wins for `search_category`, matching the "first-wins is fine" design call) → **Build write payload** (the masked `bulk_update` items described above) → **Write via workflow 20** (`POST https://ailinker.item.com/webhook/firestore-write`, server-to-server, same pattern as workflow 23's calls into workflow 20) → **Build response** → **Respond** `{ok:true, discovered, categoriesRun, propId}` (or a thrown-error 500 on failure, matching the `res.ok` pattern the app already checks for on every other webhook).

### App side (`index.html`) — new 📍 Nearby Prospects tab
Positioned in the tab bar right after 🏢 Prop Outreach and before 📞 Playbook (`data-tab="nearbyprospects"`). Mirrors Property Outreach's patterns closely (Prop Outreach's `renderPropertyOutreach`/`openPOModal`/`logPOAttempt`/`poExpandedProps` were read in full first and used as the template), adapted: groups by property, then by **search category** within a property (collapsible per property — `npExpandedProps`, same "none open by default" convention as `poExpandedProps`/`expandedAvailProps`), a search box, a category filter dropdown, a status filter (`not_contacted`/`attempted`/`reached`/`interested`/`not_interested`/`bad_number`/`do_not_call`/`promoted`/`archived` — same vocabulary as `PO_STATUS_LABEL`, this tab's map is `NP_STATUS_LABEL`), and a **🔎 Discover nearby companies** button. Because a property might have zero `site_outreach` records the very first time (there's nothing to group a card around yet), the toolbar also has a standalone **"Discover"** row — a dropdown of every known Avail location (`np-discover-loc`, built from `availMap`, works even for a property with no discovery data at all yet) plus the same button — so the very first run on a brand-new property doesn't require any special-cased UI. Once a property has records, its expanded card also gets its own inline "🔎 Re-discover this property" button plus a "Last discovered" timestamp (max `last_seen_at` across its rows). The button disables and shows "🔎 Searching…" plus a "this can take a few minutes — it's a real scrape, not an instant lookup" note (`discoverNearby()`, same `res.ok` 404-vs-other-error pattern as `researchProspect`/`generateFollowup`/`generatePODraft`/`syncRosterNow`).

Click a row → detail modal (`openNPModal`): name/address, website (clickable, auto-prefixes `https://` if the stored value lacks a scheme)/phone (`tel:`) links, a rating/review-count chip when present, the discovered **contact block** (name/title/email/LinkedIn — gracefully shows "No named contact found — try the main line" when CoreClaw's enrichment came back empty for that business, never fabricated), a **Status** dropdown (`saveNPStatus`), one-tap **Log a call** outcome buttons (`logNPAttempt` → No answer/Voicemail/Interested/Not interested/Bad number/Do not call — one fewer option than Property Outreach's `PO_OUTCOMES`, no "Callback requested," since these are cold never-before-contacted businesses, not existing leads with an established relationship; appends to `attempts[]`, auto-archives on the **2nd** "Not interested" copy-pasted straight from `logPOAttempt`'s convention since it was trivial to add), free-text **Notes** (`saveNPNotes`, plain textarea, onchange-saved), full **call history**, an **Archive/Restore** toggle (`setNPArchived`), and a **🎯 Promote to lead** button (`promoteNPToLead`, same create-a-lead-and-mark-promoted pattern as `promotePOToLead`, lead starts at `stage:'cold'` rather than `'contacted'` since — unlike a Property Outreach record, which already has call-attempt history baked into the Yardi source — a Nearby Prospects record starts as a genuinely cold, never-contacted business).

**Firestore rules — no change needed, confirmed by reading `firestore.rules`.** `site_outreach/{id}` isn't given its own `match` block (unlike `availability`/`config/properties`/`renewals/current`/`roster/current`, which ARE read-scoped for Lucy's Avail-only role) — it falls through to the existing catch-all `match /{document=**}` block, `isAllowed()`-only, same as `property_outreach`/`assistant_threads`/`sharepoint_links`. **Automatically excluded from Lucy's restricted role** — `lockToAvailReadOnly()` only allow-lists `avail`/`roster` tab buttons (confirmed unchanged, no edit needed), so this brand-new tab is hidden for her by default with zero extra gating code, same as every other non-Avail/non-Roster tab.

All new inline handlers exposed on `window.*` (the ES-module-scope gotcha this project's own CLAUDE.md flags as its most common bug): `onNPSearch`, `setNPLocation`, `setNPCategory`, `setNPStatus`, `toggleNPProp`, `discoverNearby`, `openNPModal`, `closeNPModal`, `npModalBgClick`, `saveNPStatus`, `saveNPNotes`, `logNPAttempt`, `setNPArchived`, `promoteNPToLead`.

**Validated the edit didn't break `index.html`'s structure** by extracting the exact new JS block (state vars through the `site_outreach` listener) into a standalone scratch file and running `node --check` on it — passed clean. (A whole-file `node --check` via the mounted sandbox path wasn't reliable — the `do_or_wait` bash mount in this session showed a stale, multi-day-old cached copy of the file even right after edits landed via the direct-filesystem Read/Edit tools, the same "mounted sandbox path has its own stale-cache quirk" this CLAUDE.md already flagged back in the 2026-07-10 (cont. #2) session; copying into the session's `outputs` scratch directory — which mounts live, unlike the project folders — and validating there is the workaround, same as that earlier note prescribes.) Also grep-confirmed every new tab-bar/view-div/switchTab wiring point (`data-tab="nearbyprospects"`, `nearbyprospects-view`, the `switchTab()` display-toggle line, the `renderNearbyProspects()` call) landed exactly once each, and diffed the JSON node-name graph in `25_site_outreach_discovery.json` against its own `connections` block (script-checked: every edge references a real node name, and every `$('Node Name')` lookup inside a Code node resolves to a node that actually exists) — no dangling references.

### Deploy checklist (Justin — none of this can be done from the sandbox)
1. Sign up at coreclaw.com if not already done (free $3 credit, no card required) — Google Maps Scraper is $3/1,000 results on the free tier ($1.80/1,000 on the $3/mo Starter plan); the leads-enrichment toggle is FREE. A full discovery run for one site (9 categories × up to ~20-100 results each) should cost well under the $3 free credit for a first test.
2. Get the API key from CoreClaw Console → Settings → API & Integrations.
3. In n8n, create an HTTP Header Auth credential (e.g. name it "CoreClaw API," header name `api-key`, value = the key) and pick it on the four HTTP Request nodes in workflow 25 that call `openapi.coreclaw.com` ("Get scraper schema," "Run scraper," "Get run detail," "Get results page" — all currently `REPLACE_WITH_CREDENTIAL_ID` placeholders).
4. Import `25_site_outreach_discovery.json` into n8n as a **new** workflow — brand-new webhook path `site-outreach-discover`, no conflict risk with any existing workflow (same anti-duplicate-webhook-path lesson learned the hard way with workflow 5's import saga).
5. Activate it.
6. Confirm workflow 20 ("CW — 20. Firestore Writer") is still Active — this new workflow depends on it for the actual Firestore write.
7. `git push` the updated `index.html` for the new tab to go live (nothing here happens from the sandbox).
8. **First real test run**: click "🔎 Discover nearby companies" on Ontario Airport, then immediately go check the actual n8n execution log for "Get scraper schema"'s real response and one real "Get results page" record, and fix the Code nodes' field-name assumptions per the UNVERIFIED section above if they don't match — this is the expected first-run follow-up, not a surprise.

**New files:** `n8n/25_site_outreach_discovery.json`, `n8n/site_outreach_discovery.nodes.js`. **Changed files:** `index.html` (new 📍 Nearby Prospects tab: tab button, view + modal markup, `renderNearbyProspects`/`openNPModal`/CRUD handlers, `site_outreach` listener, `SITE_OUTREACH_DISCOVER_WEBHOOK`), `CLAUDE.md` (tab list renumbered to add item 11, new `site_outreach/{id}` collection doc, new `n8n/` file-listing bullet, new webhook-URL bullet, this section).

---

## 2026-07-21 — Doc audit: reconciled CLAUDE.md against the real `index.html`/`n8n/` state

Ran a full audit (Justin: "update all docs... have it be current to the current state") comparing every claim in this file against the actual, current `index.html` (grep for `data-tab=`, function names, webhook constants) and the real `n8n/` directory listing — not the bash-mounted copy of this project, which has its own known stale-cache bug (see the 2026-07-20 Nearby Prospects note above); used the direct-filesystem Read/Grep tools instead. Findings and fixes:

- **Tab list was out of order and missing a tab.** The top "Tabs:" numbered list read as a feature-importance ordering that never matched the real tab-bar sequence, and never included the 🔗 Links tab at all (it only existed in its own dated section below). **Fixed:** renumbered 1-13 to match the actual `data-tab=` order in `index.html` (Tasks, Leads, Outreach, Prop Outreach, Nearby Prospects, Playbook, Avail, Roster, Links, Pricing, Rate Bands, Notes, Assistant) and added the missing Links entry.
- **Workflow 24 (Assistant Chat) status was stale in two spots.** The n8n file-listing bullet and this doc's mental model both still said "NEW, not yet imported," even though the 2026-07-10 (cont. #2) session already confirms Justin imported it and applied two post-import fixes directly in n8n. **Fixed both spots** to read "imported and confirmed live."
- **Quick reference webhook list was out of date.** It was missing `assistant-chat` and `outreach-reply-scan` (both live features with their own dated sections) and still listed a `floorplan-sync` webhook for a feature that was fully abandoned 2026-06-17/18 (`FLOORPLAN_WEBHOOK`/`triggerFloorplanSync` confirmed gone from `index.html`, 0 grep hits). **Fixed:** added the two missing entries, removed the dead floorplan-sync line, and added an explicit per-workflow live/not-confirmed status note to every entry.
- **`firestore-mcp/README.md`'s "rules are open" line** — this doc's own 2026-06-29 TODO flagged it as stale and said it "should be corrected," but never confirmed it was. Checked the actual README: **it was already fixed** (it now explicitly notes the 2026-06-24 rules lockdown and how the connector's email/password sign-in passes it). Updated this doc's note to reflect that.
- **Workflows never confirmed imported anywhere in this document's session log, despite dated sections describing their build in full:** **18** (Pricing BD Sync), **19** (Roster Sync), **22** (Property Outreach Draft — repeatedly said "rides along with the same first import" across multiple sessions but that first import is never itself confirmed), **25** (Site Outreach Discovery, also has the standing CoreClaw-field-names-UNVERIFIED caveat). These are genuinely unverifiable from files alone — only Justin's real n8n instance knows. **Flagged inline** on each affected tab's numbered-list entry and in the Quick Reference webhook list, rather than guessing. **If any of these are actually live, the fix is a one-line update to each flagged spot — ping to confirm and it'll get cleaned up next session.**
- **Confirmed accurate, no changes needed:** every `n8n/*.json`/`*.nodes.js`/`*.README.md` file CLAUDE.md references actually exists on disk, and every file on disk is mentioned somewhere in the doc (no orphans, no dangling references) — the earlier cleanup notes (16b, 14_15 README, floorplan-sync workflow files) are correctly reflected as deleted. Every specific function/constant name spot-checked (`lockToAvailReadOnly`, `renderNearbyProspects`, `renderSplinks`, `FU_SIGN`, `FOLLOWUP_NUDGE`, `fuRandomCaseStudy`, `renderFuqStory`, `dailyQueueItems`, `checkFollowupGate`, `renderAssistant`, `ASSISTANT_CHAT_WEBHOOK`, etc.) still exists under the same name — nothing was silently renamed or removed since it was documented.
- **Recent git history** (`fdb6b32` job posting leads · `087b888`/`16449f7`/`630c533`/`1fd44f3` follow-up template work · `517adbf` Sharepoint link tab) lines up cleanly with this doc's most recent dated sessions — no sign of a real deploy lagging behind what's written here.

**Not changed:** the detailed dated session-log entries themselves (2026-06-08 through 2026-07-20) — those are historical record and still accurate as written; only the top-of-file summary/reference sections and the two flagged doc-drift spots above were corrected. No `index.html`/n8n workflow edits were made in this pass — this was a documentation-only reconciliation.

---

## 2026-07-21 (cont.) — CoreClaw verified against a real account, workflow 25 fixed

Justin signed up for CoreClaw (see the standing UNVERIFIED caveat on workflow 25 from 2026-07-20 (cont. #5) — no account existed when that workflow was built). Rather than import the workflow blind and discover breakage on the first real click, verified the actual API shape first by running real calls by hand (PowerShell `curl.exe` from Justin's machine — the sandbox can't reach `openapi.coreclaw.com` either, same network-allowlist restriction as every other outside service) and comparing against every guessed field name in `n8n/site_outreach_discovery.nodes.js`/`25_site_outreach_discovery.json`.

**PowerShell gotchas hit along the way (worth remembering for next time):** `curl` is aliased to `Invoke-WebRequest` in PowerShell — use `curl.exe` for the real curl binary. Multi-line here-strings (`@'...'@`) didn't paste/execute as a single block in Justin's terminal (each line ran separately) — single-line `Set-Content -Value '...'` was the reliable fix. `-Encoding utf8` in Windows PowerShell silently prepends a UTF-8 BOM, which made CoreClaw's JSON parser reject an otherwise-valid request body with a generic `4000 Invalid request parameters` — switched to `-Encoding ascii` (safe since the payloads are pure ASCII) to avoid the BOM entirely. Same `4000` error resurfaced on an inline `--data '...'` call; switched that to the same write-to-file-then-`--data-binary "@file"` pattern used for the main request, which resolved it too.

**Real schema pulled (`GET /api/scraper?slug=01KPD6M5YQADCQKGVKPDZVYC63`), confirming/correcting every guessed field:**
- Search term param is **`keywords`** (plural, array of `{keyword:"..."}` objects) — not `keyword` as a plain string.
- `base_location` (string, format `"City,Country"` — comma, no space) — confirmed correct.
- **`max_results`** (integer) is **REQUIRED** by the Worker — the original workflow never sent it at all, which would have 4000'd on the very first real run.
- There is **no dedicated boolean toggle** for "business leads enrichment." It's controlled by **`max_leads_per_place`** (integer, default 2) — set to 0 to disable, or a count to enable. An optional **`leads_seniority`** array param (owner/board/c_suite/partner/vp/director/manager/supervisor) filters by job level.

**Two real test runs against the live API** (Worker version `v1.2.7`, cost $0.003 + $0.015 — trivial): one search (`importer`, Ontario CA) came back with an empty `leads_enrichment` array (no contact found for that business); a second (`logistics company`, Los Angeles) hit two businesses with real enriched contacts, confirming the actual output shape:
- Business name is **`title`**, not `name`. Category is **`primary_category`**, not `google_category`. `all_categories` is a real JSON **array**, not a joined string. Rating is **`review_rating`**, not `rating`.
- **There is no `cid` field anywhere in the real output** — the stable identifiers are **`place_id`** and `data_id`. The whole `site_outreach/{cid}` doc-id design was built around a field that doesn't exist on this Worker; switched the doc id (and the `place_cid` field's source) to `place_id`, falling back to `data_id`.
- Coordinates are nested as **`location:{lat,lng}`**, not top-level `latitude`/`longitude` fields.
- **`leads_enrichment`** is an array of contact objects, not flat `lead_*` fields on the row. Real per-contact fields: `person_id`, `first_name`, `last_name`, `full_name`, `linkedin_profile`, `email` (frequently **null** even for a real, named contact — these are LinkedIn-sourced, not always email-verified), `mobile_number`, `job_title` (also frequently null; `headline` is a decent fallback), `industry`, `city`/`country`, `company_id`/`company_name`/`company_website`/`company_size`/`company_linkedin`/`company_city`/`company_state`/`company_country`/`company_phone_number`, `seniority`/`seniority_tier` (both null in the sample), `educations_details`, `photo_url`.
- The **Get Results Page response shape** was also wrong — rows live at **`data.list`** (confirmed via a real call), not `data` or `data.items` as originally guessed. The real pagination signal is **`data.count`** (total matching results) + `data.page_size`, not the guessed `has_more`/`total`.
- Pricing: **$1.20 per 1,000 successful results** (Google Maps Scraper, confirmed on CoreClaw's own listing page 2026-07-21) — the earlier "$3/1,000 free tier" figure in this doc was a guess and is now corrected everywhere it appeared.

**Fixes applied to `n8n/site_outreach_discovery.nodes.js` AND `n8n/25_site_outreach_discovery.json`** (kept in sync — the `.nodes.js` file is meant to be a paste-ready mirror of the JSON's embedded Code-node strings):
- `Match schema fields`: tries the confirmed exact parameter name first (`keywords`/`base_location`/`max_results`/`max_leads_per_place`/`leads_seniority`), falling back to the old substring search only as a hedge against a future Worker version renaming something.
- `Build custom params`: sends `keywords` as `[{keyword: category}]` (was a bare string), adds the previously-missing **required** `max_results` (defaulted to 20/category — a full 9-category run costs well under 25¢), sets `max_leads_per_place: 2`, and adds `leads_seniority: ['owner','c_suite','vp','director','manager']` to bias results toward decision-makers rather than a random staffer (new — tune or drop if it narrows results too much for a given category).
- `Tag + accumulate results`: reads rows from `data.list` (was reading `data`/`data.items`, which don't exist), reads pagination off `data.count`/`data.page_size` (was guessing `has_more`/`total`), maps `place_id`→`place_cid` (was `cid`, doesn't exist), `title`→`name`, `primary_category`→`google_category`, nested `location.lat`/`location.lng`→`latitude`/`longitude`, `review_rating`→`rating`, and picks a contact from the real `leads_enrichment[]` array (preferring one with a non-null `email`, else the first available) instead of guessing flat `lead_*` field names. Also now extracts a new `contact_phone` (`mobile_number`) alongside the existing four contact fields.
- `Build write payload`: added `contact_phone` to the Firestore write.
- Verified both files with `node --check` on every extracted Code-node body (wrapped in a stub harness mocking `$`/`$json`/`$getWorkflowStaticData`) — all 11 nodes parse clean — and confirmed `25_site_outreach_discovery.json` is still valid JSON via `json.load`.

**App side (`index.html`):** `openNPModal`'s contact block now shows `contact_phone` as a `tel:` link, labeled "(direct — no email on file)" when there's a phone but no email — the common case per the real test data. `promoteNPToLead` now falls back to `contact_phone` for the created lead's `phone` field when the business's main line (`r.phone`) is blank.

**`site_outreach/{id}` collection doc (above) and the tab-list Nearby Prospects entry (top of file)** updated to describe the corrected, confirmed field mapping rather than the original guesses.

**Not yet done — still needs Justin, same as before:** sign up done, but the rest of the original deploy checklist is unchanged: create the n8n HTTP Header Auth credential (header `api-key`, value = the CoreClaw API key already generated), import `25_site_outreach_discovery.json` as a new workflow, activate, confirm workflow 20 stays active, and `git push` the updated `index.html`. The CoreClaw-field-names risk that motivated the "verify before importing" caution is now closed — the workflow reflects the real API, not a guess — so the first real n8n run should behave as designed, though it's still worth watching the first execution log per the original checklist's spirit.

**Changed files:** `n8n/site_outreach_discovery.nodes.js`, `n8n/25_site_outreach_discovery.json`, `index.html` (`openNPModal` contact block, `promoteNPToLead` phone fallback), `CLAUDE.md` (tab-list entry, `site_outreach/{id}` collection doc, this section).

---

## 2026-07-22 — 📍 Nearby Prospects gets a research/draft engine (n8n workflow 26)

Justin's ask, after reviewing the CoreClaw discovery results: "I want to have the Claude API research and generate an email, like what we do for the outreach tab modal." Confirmed via AskUserQuestion that he wanted full parity with Outreach's system, not a simplified single-draft version: live web research per business, plus the same 4 subject/opener variants (mismatch/forced-bet/freedom/commitment) with a confidence score, exactly like `outreach/{id}`'s workflow 13 — not the simpler single-draft pattern Prop Outreach's workflow 22 uses.

**Built as a fork of workflow 13, not a shared/modified workflow.** Consistent with this project's established convention (every new feature gets its own numbered workflow rather than touching an existing live one — see wf17/wf22/wf25 above), workflow 26 is a standalone copy of wf13's Claude-request/Firestore-patch logic, adapted for `site_outreach`'s own field shape (`name`/`website`/`search_category`/`google_category`/`address`/`city`/`state`) instead of Apollo's (`company`/`domain`/`industry`/`location`). The master voice/tone prompt (the actual IP — recency rule, tone filter, no-product-recap, anti-fake-personalization, banned-phrase list, 5th-grade readability, no dashes, no signature) is copied **verbatim** from wf13, since that prompt has been tuned across many sessions (2026-06-24 through 2026-06-29) and there was no reason to re-derive it.

**Category-fit map is much smaller than wf13's.** wf13's `GROUP_MAP` handles ~50 raw Apollo industry strings because Apollo people-search can return almost anything. Nearby Prospects never has that problem — CoreClaw only ever searches the same 9 pre-chosen warehouse-relevant categories (importer, distribution company, wholesale distributor, 3PL logistics company, freight forwarder, ecommerce fulfillment center, trucking company, manufacturer, moving and storage company), so workflow 26's `GROUPS` map is just those 9 keys, each with its own angle/pain/research-focus/fit tier (mostly High, trucking and moving/storage at Medium, manufacturer at Mixed since it could be a fixed-plant operation). The `FIT_RULE` instructions (High/Medium/Mixed/Low) are copied verbatim from wf13.

**Deliberately reuses `outreach/{id}`'s exact field names** (`research_brief`, `email_subject`, `email_body`, `email_variants`, `prospect_group`, `prospect_fit`, `draft_confidence`, `confidence_reason`, `body_words`, `body_flesch`, `researched_at`) when writing to `site_outreach/{id}` — see that collection's doc above. This was a deliberate design choice, not an accident: it means the app's `openNPModal` can reuse Outreach's exact variant-tab/confidence-chip rendering pattern (`VARLABEL`/`setOutreachVariant`/`useOutreachVariant`/`copyOutreachDraft`) almost verbatim rather than inventing a second parallel UI, cutting real build risk on the app side.

**n8n workflow 26 — `n8n/26_nearby_prospect_research.json`** (+ `n8n/nearby_prospect_research.nodes.js`, NEW, not yet imported). Same node chain shape as wf13: `Webhook` (path `nearby-prospect-research`, `responseNode` synchronous) → `Build Claude request` (system prompt + category module, `claude-sonnet-4-6`, `web_search_20250305` tool) → `Claude research + draft` (POST `api.anthropic.com/v1/messages`, same `anthropicApi` credential pattern as wf13/17/22/24 — Justin will need to pick the credential on import) → `Build Firestore patch` (parses the 4-hook JSON response, assembles `email_variants`, computes `body_words`/`body_flesch` the same way wf13 does) → `Write research to Firestore` (PATCH `site_outreach/{id}`, same `googleApi`/`Firebase_SDK_do_or_wait` service-account credential as every other Firestore write in this project) → `Respond`.

**App side (`index.html`):**
- `NP_RESEARCH_WEBHOOK` constant (→ `webhook/nearby-prospect-research`).
- `npVariant` state map (id → selected variant index), same pattern as `outreachVariant`.
- `NP_VARLABEL`, `setNPVariant`, `useNPVariant`, `copyNPDraft` — near-verbatim copies of Outreach's `VARLABEL`/`setOutreachVariant`/`useOutreachVariant`/`copyOutreachDraft`, redefined locally rather than shared (matching this project's usual per-tab-map convention — see `NP_STATUS_LABEL` vs `PO_STATUS_LABEL` for the same pattern elsewhere).
- `researchNearbyProspect(id, e)` — same `res.ok` 404-vs-other-error pattern as `researchProspect`/`generatePODraft`/`discoverNearby` (a 404 means workflow 26 isn't imported/active; any other failure points at n8n's Executions log).
- `openNPModal` gained a Research section (🔎 Research & draft / Re-research button + `renderResearchBrief`, reusing the exact same brief-rendering helper Outreach uses) and a Draft email section (variant tabs, confidence/fit/group chips, Open in email — gated on `contact_email` being on file, same as the business's contact block already requires — Copy, and a ★ Use this version button), inserted between the Discovered Contact block and the Status dropdown.
- All four new handlers exposed on `window.*` (the ES-module-scope gotcha this project's CLAUDE.md flags as its most common bug).
- **Validated without trusting the bash mount.** The do_or_wait sandbox mount showed a truncated, stale copy of `index.html` mid-edit (missing the file's actual closing `</script></body></html>`, a known recurring quirk this doc has flagged since 2026-07-10) — confirmed via the Read tool that the real file is intact (7972 lines, ends correctly). Syntax was validated instead by reconstructing the new/changed code (read via the Read tool, not bash) into a stubbed standalone script in the session's scratch/outputs directory and running `node --check` there — passed clean. Same workaround this doc has prescribed since the Nearby Prospects build in 2026-07-20.

**Deploy checklist (Justin):**
1. Import `26_nearby_prospect_research.json` into n8n as a **new** workflow (brand-new webhook path `nearby-prospect-research`, no conflict risk with any existing workflow).
2. Pick the **Anthropic API** credential on "Claude research + draft" (same one already used by wf13/17/22/24 — a dropdown pick, no new setup) and confirm the **Google/Firebase_SDK_do_or_wait** credential on "Write research to Firestore" (should carry over automatically since the credential id, `7IVvQuErxEJPoaMY`, is reused directly from wf13's export).
3. Activate.
4. `git push` the updated `index.html` for the new modal UI to go live.
5. Test on one business with a real website (e.g. one of the Ontario Airport importer results from the 2026-07-21 test batch) and confirm a real 4-variant draft lands in `site_outreach/{id}` — same "check the first real run" caution this doc has applied to every new Claude/n8n integration this project has shipped.

**New files:** `n8n/26_nearby_prospect_research.json`, `n8n/nearby_prospect_research.nodes.js`. **Changed files:** `index.html` (`NP_RESEARCH_WEBHOOK`, `npVariant`, `NP_VARLABEL`, `setNPVariant`/`useNPVariant`/`copyNPDraft`/`researchNearbyProspect`, `openNPModal` research/draft sections), `CLAUDE.md` (tab-list entry, `site_outreach/{id}` collection doc, n8n file-listing bullet, webhook quick-reference bullet, this section).
