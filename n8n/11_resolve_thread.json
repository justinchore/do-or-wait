{
  "name": "CW — 11. Resolve Thread Link",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "resolve-thread",
        "responseMode": "responseNode",
        "options": {
          "allowedOrigins": "*"
        }
      },
      "id": "cw-rt-001",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "resolve-thread"
    },
    {
      "parameters": {
        "jsCode": "// Turn the {subject, contact} the app posts into a Graph $search string.\n// Subject stays free-text (no subject:\"...\" KQL — nesting quotes inside the\n// already-quoted $search value breaks it). A contact that's an EMAIL uses the\n// participants: keyword (precise); a name is added as free text. KQL keywords\n// are fine inside the outer quotes; only nested double-quotes are the problem.\nconst b = ($json.body && typeof $json.body === 'object') ? $json.body : $json;\nconst clean = s => String(s || '').replace(/[\"\\r\\n]/g, ' ').trim();\nconst subject = clean(b.subject);\nconst contact = clean(b.contact);\nconst terms = [];\nif (subject) terms.push(subject);\nif (contact) terms.push(contact.includes('@') ? ('participants:' + contact) : contact);\nconst search = terms.join(' ').trim();\nreturn [{ json: { search, subject, contact, hasQuery: !!search } }];"
      },
      "id": "cw-rt-002",
      "name": "Build search",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300]
    },
    {
      "parameters": {
        "method": "GET",
        "url": "https://graph.microsoft.com/v1.0/me/messages",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "microsoftOAuth2Api",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            { "name": "$search", "value": "=\"{{ $json.search }}\"" },
            { "name": "$select", "value": "webLink,subject,from,receivedDateTime,sentDateTime,conversationId" },
            { "name": "$top",    "value": "25" }
          ]
        },
        "options": {}
      },
      "id": "cw-rt-003",
      "name": "Search messages",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [680, 300],
      "continueOnFail": true,
      "credentials": {
        "microsoftOAuth2Api": {
          "id": "REPLACE_WITH_CREDENTIAL_ID",
          "name": "Microsoft OAuth2 — Cubework Outlook"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Pick the newest message across the relevance-ranked hits and return its\n// webLink — the official Graph property that OPENS that message/thread in\n// Outlook on the web. $search can't be combined with $orderby, so we sort in\n// code. If Graph errored (continueOnFail) or returned nothing, found:false.\nconst resp = $input.first().json || {};\nconst msgs = Array.isArray(resp.value) ? resp.value : [];\nlet best = null, bestMs = null;\nfor (const m of msgs) {\n  if (!m) continue;\n  const t = Date.parse(m.receivedDateTime || m.sentDateTime || '');\n  if (isNaN(t)) continue;\n  if (bestMs === null || t > bestMs) { bestMs = t; best = m; }\n}\nif (!best || !best.webLink) {\n  return [{ json: { found: false } }];\n}\n// Graph now returns webLinks on the NEW Outlook host (outlook.cloud.microsoft),\n// whose deeplink reader fails with \"This message might have been moved or deleted\"\n// even for valid messages. The IDENTICAL deeplink on outlook.office365.com opens\n// fine, so rewrite the host before storing. (Confirmed 2026-06-25 — only the host\n// differs.) The app also rewrites at render time as a backstop for older saved links.\nconst webLink = String(best.webLink).replace(/^(https?:\\/\\/)outlook\\.cloud\\.microsoft\\//i, '$1outlook.office365.com/');\nreturn [{ json: {\n  found: true,\n  webLink,\n  conversationId: best.conversationId || '',\n  subject: best.subject || '',\n  from: (best.from && best.from.emailAddress && best.from.emailAddress.address) || '',\n  date: best.receivedDateTime || best.sentDateTime || ''\n} }];"
      },
      "id": "cw-rt-004",
      "name": "Pick newest hit",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [900, 300]
    },
    {
      "parameters": {
        "respondWith": "firstIncomingItem",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              { "name": "Access-Control-Allow-Origin", "value": "*" }
            ]
          }
        }
      },
      "id": "cw-rt-005",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1120, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{ "node": "Build search", "type": "main", "index": 0 }]]
    },
    "Build search": {
      "main": [[{ "node": "Search messages", "type": "main", "index": 0 }]]
    },
    "Search messages": {
      "main": [[{ "node": "Pick newest hit", "type": "main", "index": 0 }]]
    },
    "Pick newest hit": {
      "main": [[{ "node": "Respond", "type": "main", "index": 0 }]]
    }
  },
  "active": false,
  "settings": { "executionOrder": "v1" },
  "versionId": "cw-workflow-011"
}
